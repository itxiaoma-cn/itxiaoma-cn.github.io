<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>GO微服务01-Go语言基础</title>
    <url>/2022/03/01/GO%E5%BE%AE%E6%9C%8D%E5%8A%A101-Go%E8%AF%AD%E8%A8%80%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>GO微服务</category>
      </categories>
      <tags>
        <tag>GO微服务</tag>
        <tag>GO</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis01-基础及安装</title>
    <url>/2022/02/01/Redis01-%E5%9F%BA%E7%A1%80%E5%8F%8A%E5%AE%89%E8%A3%85/</url>
    <content><![CDATA[<h2 id="1-NoSQL数据库简介"><a href="#1-NoSQL数据库简介" class="headerlink" title="1.NoSQL数据库简介"></a>1.NoSQL数据库简介</h2><h3 id="1-1-技术分类"><a href="#1-1-技术分类" class="headerlink" title="1.1 技术分类"></a>1.1 技术分类</h3><ol>
<li>解决功能性问题</li>
<li>解决扩展性问题</li>
<li>解决性能问题</li>
</ol>
<h3 id="1-2-NoSQL数据库"><a href="#1-2-NoSQL数据库" class="headerlink" title="1.2 NoSQL数据库"></a>1.2 NoSQL数据库</h3><blockquote>
<p>NoSql = Not Only SQL 非关系型数据库，以key-value模式存储。</p>
</blockquote>
<h4 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h4><ul>
<li>不遵循SQL标准</li>
<li>不支持ACID</li>
<li>远超于SQL的性能</li>
</ul>
<h4 id="适用场景"><a href="#适用场景" class="headerlink" title="适用场景"></a>适用场景</h4><ul>
<li>数据高并发读写</li>
<li>海量数据读写</li>
<li>数据高可扩展性</li>
</ul>
<h4 id="不适用场景"><a href="#不适用场景" class="headerlink" title="不适用场景"></a>不适用场景</h4><ul>
<li>需要事务支持</li>
<li>结构化查询存储，处理复杂关系</li>
</ul>
<h4 id="常见NoSQL"><a href="#常见NoSQL" class="headerlink" title="常见NoSQL"></a>常见NoSQL</h4><ul>
<li>Memcache：数据在内存中，不支持持久化，一般作为缓存数据库。</li>
<li>Redis：数据在内存中，支持持久化，支持多种数据结构，一般作为缓存数据库。</li>
<li>MongoDB：<ul>
<li>文档型数据库</li>
<li>数据在内存中，内存不足时保存到硬盘</li>
<li>支持二进制数据和大型对象</li>
<li>配合/替代关系型数据库(RDBMS)存储特定数据</li>
</ul>
</li>
</ul>
<h2 id="2-Redis概述安装"><a href="#2-Redis概述安装" class="headerlink" title="2.Redis概述安装"></a>2.Redis概述安装</h2><p>官网：<a href="https://redis.io/">https://redis.io/</a></p>
<h4 id="Redis介绍"><a href="#Redis介绍" class="headerlink" title="Redis介绍"></a>Redis介绍</h4><ul>
<li>Redis 是一个开源的 key-value 存储系统。</li>
<li>和 Memcached 类似，它支持存储的 value 类型相对更多，包括 string (字符串)、list (链表)、set (集合)、zset (sorted set –有序集合) 和 hash（哈希类型）。</li>
<li>这些数据类型都支持 push/pop、add/remove 及取交集并集和差集及更丰富的操作，而且<strong>这些操作都是原子性的</strong>。</li>
<li>在此基础上，Redis 支持各种不同方式的排序。</li>
<li>与 memcached 一样，为了保证效率，数据都是缓存在内存中。</li>
<li>区别的是 Redis 会周期性的把更新的数据写入磁盘或者把修改操作写入追加的记录文件。</li>
<li>并且在此基础上实现了 master-slave (主从) 同步。</li>
</ul>
<h4 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h4><ul>
<li>高频次，热门访问的数据，降低数据库 IO。</li>
<li>分布式架构，做 session 共享。</li>
</ul>
<p><img src="https://cdn.jsdelivr.net/gh/zhangc233/pic@master/img/image-20210618160101040.png" alt="image-20210618160101040"></p>
<h4 id="Redis安装"><a href="#Redis安装" class="headerlink" title="Redis安装"></a>Redis安装</h4><p>官方教程：<a href="https://redis.io/docs/getting-started/installation/">https://redis.io/docs/getting-started/installation/</a></p>
<h5 id="docker安装redis"><a href="#docker安装redis" class="headerlink" title="docker安装redis"></a>docker安装redis</h5><p>docker-compose.yml</p>
<pre><code>version: &#39;3.5&#39;

services:
  redis:
    image: redis
    container_name: redis
    restart: always
    command: redis-server /usr/local/redis/redis.conf --requirepass &quot;123456&quot;
    ports:
      - 6379:6379
    volumes:
      - ./data:/data:rw
      - ./redis.conf:/usr/local/redis/redis.conf
      - ./logs:/logs
</code></pre>
<p>redis.conf</p>
<pre><code># Redis configuration file example.
#
# Note that in order to read the configuration file, Redis must be
# started with the file path as first argument:
#
# ./redis-server /path/to/redis.conf

# Note on units: when memory size is needed, it is possible to specify
# it in the usual form of 1k 5GB 4M and so forth:
#
# 1k =&gt; 1000 bytes
# 1kb =&gt; 1024 bytes
# 1m =&gt; 1000000 bytes
# 1mb =&gt; 1024*1024 bytes
# 1g =&gt; 1000000000 bytes
# 1gb =&gt; 1024*1024*1024 bytes
#
# units are case insensitive so 1GB 1Gb 1gB are all the same.

################################## INCLUDES ###################################

# Include one or more other config files here.  This is useful if you
# have a standard template that goes to all Redis servers but also need
# to customize a few per-server settings.  Include files can include
# other files, so use this wisely.
#
# Note that option &quot;include&quot; won&#39;t be rewritten by command &quot;CONFIG REWRITE&quot;
# from admin or Redis Sentinel. Since Redis always uses the last processed
# line as value of a configuration directive, you&#39;d better put includes
# at the beginning of this file to avoid overwriting config change at runtime.
#
# If instead you are interested in using includes to override configuration
# options, it is better to use include as the last line.
#
# include /path/to/local.conf
# include /path/to/other.conf

################################## MODULES #####################################

# Load modules at startup. If the server is not able to load modules
# it will abort. It is possible to use multiple loadmodule directives.
#
# loadmodule /path/to/my_module.so
# loadmodule /path/to/other_module.so

################################## NETWORK #####################################

# By default, if no &quot;bind&quot; configuration directive is specified, Redis listens
# for connections from all available network interfaces on the host machine.
# It is possible to listen to just one or multiple selected interfaces using
# the &quot;bind&quot; configuration directive, followed by one or more IP addresses.
# Each address can be prefixed by &quot;-&quot;, which means that redis will not fail to
# start if the address is not available. Being not available only refers to
# addresses that does not correspond to any network interfece. Addresses that
# are already in use will always fail, and unsupported protocols will always BE
# silently skipped.
#
# Examples:
#
# bind 192.168.1.100 10.0.0.1     # listens on two specific IPv4 addresses
# bind 127.0.0.1 ::1              # listens on loopback IPv4 and IPv6
# bind * -::*                     # like the default, all available interfaces
#
# ~~~ WARNING ~~~ If the computer running Redis is directly exposed to the
# internet, binding to all the interfaces is dangerous and will expose the
# instance to everybody on the internet. So by default we uncomment the
# following bind directive, that will force Redis to listen only on the
# IPv4 and IPv6 (if available) loopback interface addresses (this means Redis
# will only be able to accept client connections from the same host that it is
# running on).
#
# IF YOU ARE SURE YOU WANT YOUR INSTANCE TO LISTEN TO ALL THE INTERFACES
# JUST COMMENT OUT THE FOLLOWING LINE.
# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
#bind 127.0.0.1 -::1

# Protected mode is a layer of security protection, in order to avoid that
# Redis instances left open on the internet are accessed and exploited.
#
# When protected mode is on and if:
#
# 1) The server is not binding explicitly to a set of addresses using the
#    &quot;bind&quot; directive.
# 2) No password is configured.
#
# The server only accepts connections from clients connecting from the
# IPv4 and IPv6 loopback addresses 127.0.0.1 and ::1, and from Unix domain
# sockets.
#
# By default protected mode is enabled. You should disable it only if
# you are sure you want clients from other hosts to connect to Redis
# even if no authentication is configured, nor a specific set of interfaces
# are explicitly listed using the &quot;bind&quot; directive.
protected-mode no

# Accept connections on the specified port, default is 6379 (IANA #815344).
# If port 0 is specified Redis will not listen on a TCP socket.
port 6379

# TCP listen() backlog.
#
# In high requests-per-second environments you need a high backlog in order
# to avoid slow clients connection issues. Note that the Linux kernel
# will silently truncate it to the value of /proc/sys/net/core/somaxconn so
# make sure to raise both the value of somaxconn and tcp_max_syn_backlog
# in order to get the desired effect.
tcp-backlog 511

# Unix socket.
#
# Specify the path for the Unix socket that will be used to listen for
# incoming connections. There is no default, so Redis will not listen
# on a unix socket when not specified.
#
# unixsocket /run/redis.sock
# unixsocketperm 700

# Close the connection after a client is idle for N seconds (0 to disable)
timeout 0

# TCP keepalive.
#
# If non-zero, use SO_KEEPALIVE to send TCP ACKs to clients in absence
# of communication. This is useful for two reasons:
#
# 1) Detect dead peers.
# 2) Force network equipment in the middle to consider the connection to be
#    alive.
#
# On Linux, the specified value (in seconds) is the period used to send ACKs.
# Note that to close the connection the double of the time is needed.
# On other kernels the period depends on the kernel configuration.
#
# A reasonable value for this option is 300 seconds, which is the new
# Redis default starting with Redis 3.2.1.
tcp-keepalive 300

################################# TLS/SSL #####################################

# By default, TLS/SSL is disabled. To enable it, the &quot;tls-port&quot; configuration
# directive can be used to define TLS-listening ports. To enable TLS on the
# default port, use:
#
# port 0
# tls-port 6379

# Configure a X.509 certificate and private key to use for authenticating the
# server to connected clients, masters or cluster peers.  These files should be
# PEM formatted.
#
# tls-cert-file redis.crt 
# tls-key-file redis.key
#
# If the key file is encrypted using a passphrase, it can be included here
# as well.
#
# tls-key-file-pass secret

# Normally Redis uses the same certificate for both server functions (accepting
# connections) and client functions (replicating from a master, establishing
# cluster bus connections, etc.).
#
# Sometimes certificates are issued with attributes that designate them as
# client-only or server-only certificates. In that case it may be desired to use
# different certificates for incoming (server) and outgoing (client)
# connections. To do that, use the following directives:
#
# tls-client-cert-file client.crt
# tls-client-key-file client.key
#
# If the key file is encrypted using a passphrase, it can be included here
# as well.
#
# tls-client-key-file-pass secret

# Configure a DH parameters file to enable Diffie-Hellman (DH) key exchange:
#
# tls-dh-params-file redis.dh

# Configure a CA certificate(s) bundle or directory to authenticate TLS/SSL
# clients and peers.  Redis requires an explicit configuration of at least one
# of these, and will not implicitly use the system wide configuration.
#
# tls-ca-cert-file ca.crt
# tls-ca-cert-dir /etc/ssl/certs

# By default, clients (including replica servers) on a TLS port are required
# to authenticate using valid client side certificates.
#
# If &quot;no&quot; is specified, client certificates are not required and not accepted.
# If &quot;optional&quot; is specified, client certificates are accepted and must be
# valid if provided, but are not required.
#
# tls-auth-clients no
# tls-auth-clients optional

# By default, a Redis replica does not attempt to establish a TLS connection
# with its master.
#
# Use the following directive to enable TLS on replication links.
#
# tls-replication yes

# By default, the Redis Cluster bus uses a plain TCP connection. To enable
# TLS for the bus protocol, use the following directive:
#
# tls-cluster yes

# By default, only TLSv1.2 and TLSv1.3 are enabled and it is highly recommended
# that older formally deprecated versions are kept disabled to reduce the attack surface.
# You can explicitly specify TLS versions to support.
# Allowed values are case insensitive and include &quot;TLSv1&quot;, &quot;TLSv1.1&quot;, &quot;TLSv1.2&quot;,
# &quot;TLSv1.3&quot; (OpenSSL &gt;= 1.1.1) or any combination.
# To enable only TLSv1.2 and TLSv1.3, use:
#
# tls-protocols &quot;TLSv1.2 TLSv1.3&quot;

# Configure allowed ciphers.  See the ciphers(1ssl) manpage for more information
# about the syntax of this string.
#
# Note: this configuration applies only to &lt;= TLSv1.2.
#
# tls-ciphers DEFAULT:!MEDIUM

# Configure allowed TLSv1.3 ciphersuites.  See the ciphers(1ssl) manpage for more
# information about the syntax of this string, and specifically for TLSv1.3
# ciphersuites.
#
# tls-ciphersuites TLS_CHACHA20_POLY1305_SHA256

# When choosing a cipher, use the server&#39;s preference instead of the client
# preference. By default, the server follows the client&#39;s preference.
#
# tls-prefer-server-ciphers yes

# By default, TLS session caching is enabled to allow faster and less expensive
# reconnections by clients that support it. Use the following directive to disable
# caching.
#
# tls-session-caching no

# Change the default number of TLS sessions cached. A zero value sets the cache
# to unlimited size. The default size is 20480.
#
# tls-session-cache-size 5000

# Change the default timeout of cached TLS sessions. The default timeout is 300
# seconds.
#
# tls-session-cache-timeout 60

################################# GENERAL #####################################

# By default Redis does not run as a daemon. Use &#39;yes&#39; if you need it.
# Note that Redis will write a pid file in /var/run/redis.pid when daemonized.
# When Redis is supervised by upstart or systemd, this parameter has no impact.
daemonize no

# If you run Redis from upstart or systemd, Redis can interact with your
# supervision tree. Options:
#   supervised no      - no supervision interaction
#   supervised upstart - signal upstart by putting Redis into SIGSTOP mode
#                        requires &quot;expect stop&quot; in your upstart job config
#   supervised systemd - signal systemd by writing READY=1 to $NOTIFY_SOCKET
#                        on startup, and updating Redis status on a regular
#                        basis.
#   supervised auto    - detect upstart or systemd method based on
#                        UPSTART_JOB or NOTIFY_SOCKET environment variables
# Note: these supervision methods only signal &quot;process is ready.&quot;
#       They do not enable continuous pings back to your supervisor.
#
# The default is &quot;no&quot;. To run under upstart/systemd, you can simply uncomment
# the line below:
#
# supervised auto

# If a pid file is specified, Redis writes it where specified at startup
# and removes it at exit.
#
# When the server runs non daemonized, no pid file is created if none is
# specified in the configuration. When the server is daemonized, the pid file
# is used even if not specified, defaulting to &quot;/var/run/redis.pid&quot;.
#
# Creating a pid file is best effort: if Redis is not able to create it
# nothing bad happens, the server will start and run normally.
#
# Note that on modern Linux systems &quot;/run/redis.pid&quot; is more conforming
# and should be used instead.
pidfile /var/run/redis_6379.pid

# Specify the server verbosity level.
# This can be one of:
# debug (a lot of information, useful for development/testing)
# verbose (many rarely useful info, but not a mess like the debug level)
# notice (moderately verbose, what you want in production probably)
# warning (only very important / critical messages are logged)
loglevel notice

# Specify the log file name. Also the empty string can be used to force
# Redis to log on the standard output. Note that if you use standard
# output for logging but daemonize, logs will be sent to /dev/null
logfile /logs/redis.log

# To enable logging to the system logger, just set &#39;syslog-enabled&#39; to yes,
# and optionally update the other syslog parameters to suit your needs.
# syslog-enabled no

# Specify the syslog identity.
# syslog-ident redis

# Specify the syslog facility. Must be USER or between LOCAL0-LOCAL7.
# syslog-facility local0

# To disable the built in crash log, which will possibly produce cleaner core
# dumps when they are needed, uncomment the following:
#
# crash-log-enabled no

# To disable the fast memory check that&#39;s run as part of the crash log, which
# will possibly let redis terminate sooner, uncomment the following:
#
# crash-memcheck-enabled no

# Set the number of databases. The default database is DB 0, you can select
# a different one on a per-connection basis using SELECT &lt;dbid&gt; where
# dbid is a number between 0 and &#39;databases&#39;-1
databases 16

# By default Redis shows an ASCII art logo only when started to log to the
# standard output and if the standard output is a TTY and syslog logging is
# disabled. Basically this means that normally a logo is displayed only in
# interactive sessions.
#
# However it is possible to force the pre-4.0 behavior and always show a
# ASCII art logo in startup logs by setting the following option to yes.
always-show-logo no

# By default, Redis modifies the process title (as seen in &#39;top&#39; and &#39;ps&#39;) to
# provide some runtime information. It is possible to disable this and leave
# the process name as executed by setting the following to no.
set-proc-title yes

# When changing the process title, Redis uses the following template to construct
# the modified title.
#
# Template variables are specified in curly brackets. The following variables are
# supported:
#
# &#123;title&#125;           Name of process as executed if parent, or type of child process.
# &#123;listen-addr&#125;     Bind address or &#39;*&#39; followed by TCP or TLS port listening on, or
#                   Unix socket if only that&#39;s available.
# &#123;server-mode&#125;     Special mode, i.e. &quot;[sentinel]&quot; or &quot;[cluster]&quot;.
# &#123;port&#125;            TCP port listening on, or 0.
# &#123;tls-port&#125;        TLS port listening on, or 0.
# &#123;unixsocket&#125;      Unix domain socket listening on, or &quot;&quot;.
# &#123;config-file&#125;     Name of configuration file used.
#
proc-title-template &quot;&#123;title&#125; &#123;listen-addr&#125; &#123;server-mode&#125;&quot;

################################ SNAPSHOTTING  ################################

# Save the DB to disk.
#
# save &lt;seconds&gt; &lt;changes&gt;
#
# Redis will save the DB if both the given number of seconds and the given
# number of write operations against the DB occurred.
#
# Snapshotting can be completely disabled with a single empty string argument
# as in following example:
#
# save &quot;&quot;
#
# Unless specified otherwise, by default Redis will save the DB:
#   * After 3600 seconds (an hour) if at least 1 key changed
#   * After 300 seconds (5 minutes) if at least 100 keys changed
#   * After 60 seconds if at least 10000 keys changed
#
# You can set these explicitly by uncommenting the three following lines.
#
# save 3600 1
# save 300 100
# save 60 10000

# By default Redis will stop accepting writes if RDB snapshots are enabled
# (at least one save point) and the latest background save failed.
# This will make the user aware (in a hard way) that data is not persisting
# on disk properly, otherwise chances are that no one will notice and some
# disaster will happen.
#
# If the background saving process will start working again Redis will
# automatically allow writes again.
#
# However if you have setup your proper monitoring of the Redis server
# and persistence, you may want to disable this feature so that Redis will
# continue to work as usual even if there are problems with disk,
# permissions, and so forth.
stop-writes-on-bgsave-error yes

# Compress string objects using LZF when dump .rdb databases?
# By default compression is enabled as it&#39;s almost always a win.
# If you want to save some CPU in the saving child set it to &#39;no&#39; but
# the dataset will likely be bigger if you have compressible values or keys.
rdbcompression yes

# Since version 5 of RDB a CRC64 checksum is placed at the end of the file.
# This makes the format more resistant to corruption but there is a performance
# hit to pay (around 10%) when saving and loading RDB files, so you can disable it
# for maximum performances.
#
# RDB files created with checksum disabled have a checksum of zero that will
# tell the loading code to skip the check.
rdbchecksum yes

# Enables or disables full sanitation checks for ziplist and listpack etc when
# loading an RDB or RESTORE payload. This reduces the chances of a assertion or
# crash later on while processing commands.
# Options:
#   no         - Never perform full sanitation
#   yes        - Always perform full sanitation
#   clients    - Perform full sanitation only for user connections.
#                Excludes: RDB files, RESTORE commands received from the master
#                connection, and client connections which have the
#                skip-sanitize-payload ACL flag.
# The default should be &#39;clients&#39; but since it currently affects cluster
# resharding via MIGRATE, it is temporarily set to &#39;no&#39; by default.
#
# sanitize-dump-payload no

# The filename where to dump the DB
dbfilename dump.rdb

# Remove RDB files used by replication in instances without persistence
# enabled. By default this option is disabled, however there are environments
# where for regulations or other security concerns, RDB files persisted on
# disk by masters in order to feed replicas, or stored on disk by replicas
# in order to load them for the initial synchronization, should be deleted
# ASAP. Note that this option ONLY WORKS in instances that have both AOF
# and RDB persistence disabled, otherwise is completely ignored.
#
# An alternative (and sometimes better) way to obtain the same effect is
# to use diskless replication on both master and replicas instances. However
# in the case of replicas, diskless is not always an option.
rdb-del-sync-files no

# The working directory.
#
# The DB will be written inside this directory, with the filename specified
# above using the &#39;dbfilename&#39; configuration directive.
#
# The Append Only File will also be created inside this directory.
#
# Note that you must specify a directory here, not a file name.
dir ./

################################# REPLICATION #################################

# Master-Replica replication. Use replicaof to make a Redis instance a copy of
# another Redis server. A few things to understand ASAP about Redis replication.
#
#   +------------------+      +---------------+
#   |      Master      | ---&gt; |    Replica    |
#   | (receive writes) |      |  (exact copy) |
#   +------------------+      +---------------+
#
# 1) Redis replication is asynchronous, but you can configure a master to
#    stop accepting writes if it appears to be not connected with at least
#    a given number of replicas.
# 2) Redis replicas are able to perform a partial resynchronization with the
#    master if the replication link is lost for a relatively small amount of
#    time. You may want to configure the replication backlog size (see the next
#    sections of this file) with a sensible value depending on your needs.
# 3) Replication is automatic and does not need user intervention. After a
#    network partition replicas automatically try to reconnect to masters
#    and resynchronize with them.
#
# replicaof &lt;masterip&gt; &lt;masterport&gt;

# If the master is password protected (using the &quot;requirepass&quot; configuration
# directive below) it is possible to tell the replica to authenticate before
# starting the replication synchronization process, otherwise the master will
# refuse the replica request.
#
# masterauth &lt;master-password&gt;
#
# However this is not enough if you are using Redis ACLs (for Redis version
# 6 or greater), and the default user is not capable of running the PSYNC
# command and/or other commands needed for replication. In this case it&#39;s
# better to configure a special user to use with replication, and specify the
# masteruser configuration as such:
#
# masteruser &lt;username&gt;
#
# When masteruser is specified, the replica will authenticate against its
# master using the new AUTH form: AUTH &lt;username&gt; &lt;password&gt;.

# When a replica loses its connection with the master, or when the replication
# is still in progress, the replica can act in two different ways:
#
# 1) if replica-serve-stale-data is set to &#39;yes&#39; (the default) the replica will
#    still reply to client requests, possibly with out of date data, or the
#    data set may just be empty if this is the first synchronization.
#
# 2) If replica-serve-stale-data is set to &#39;no&#39; the replica will reply with
#    an error &quot;SYNC with master in progress&quot; to all commands except:
#    INFO, REPLICAOF, AUTH, PING, SHUTDOWN, REPLCONF, ROLE, CONFIG, SUBSCRIBE,
#    UNSUBSCRIBE, PSUBSCRIBE, PUNSUBSCRIBE, PUBLISH, PUBSUB, COMMAND, POST,
#    HOST and LATENCY.
#
replica-serve-stale-data yes

# You can configure a replica instance to accept writes or not. Writing against
# a replica instance may be useful to store some ephemeral data (because data
# written on a replica will be easily deleted after resync with the master) but
# may also cause problems if clients are writing to it because of a
# misconfiguration.
#
# Since Redis 2.6 by default replicas are read-only.
#
# Note: read only replicas are not designed to be exposed to untrusted clients
# on the internet. It&#39;s just a protection layer against misuse of the instance.
# Still a read only replica exports by default all the administrative commands
# such as CONFIG, DEBUG, and so forth. To a limited extent you can improve
# security of read only replicas using &#39;rename-command&#39; to shadow all the
# administrative / dangerous commands.
replica-read-only yes

# Replication SYNC strategy: disk or socket.
#
# New replicas and reconnecting replicas that are not able to continue the
# replication process just receiving differences, need to do what is called a
# &quot;full synchronization&quot;. An RDB file is transmitted from the master to the
# replicas.
#
# The transmission can happen in two different ways:
#
# 1) Disk-backed: The Redis master creates a new process that writes the RDB
#                 file on disk. Later the file is transferred by the parent
#                 process to the replicas incrementally.
# 2) Diskless: The Redis master creates a new process that directly writes the
#              RDB file to replica sockets, without touching the disk at all.
#
# With disk-backed replication, while the RDB file is generated, more replicas
# can be queued and served with the RDB file as soon as the current child
# producing the RDB file finishes its work. With diskless replication instead
# once the transfer starts, new replicas arriving will be queued and a new
# transfer will start when the current one terminates.
#
# When diskless replication is used, the master waits a configurable amount of
# time (in seconds) before starting the transfer in the hope that multiple
# replicas will arrive and the transfer can be parallelized.
#
# With slow disks and fast (large bandwidth) networks, diskless replication
# works better.
repl-diskless-sync no

# When diskless replication is enabled, it is possible to configure the delay
# the server waits in order to spawn the child that transfers the RDB via socket
# to the replicas.
#
# This is important since once the transfer starts, it is not possible to serve
# new replicas arriving, that will be queued for the next RDB transfer, so the
# server waits a delay in order to let more replicas arrive.
#
# The delay is specified in seconds, and by default is 5 seconds. To disable
# it entirely just set it to 0 seconds and the transfer will start ASAP.
repl-diskless-sync-delay 5

# -----------------------------------------------------------------------------
# WARNING: RDB diskless load is experimental. Since in this setup the replica
# does not immediately store an RDB on disk, it may cause data loss during
# failovers. RDB diskless load + Redis modules not handling I/O reads may also
# cause Redis to abort in case of I/O errors during the initial synchronization
# stage with the master. Use only if you know what you are doing.
# -----------------------------------------------------------------------------
#
# Replica can load the RDB it reads from the replication link directly from the
# socket, or store the RDB to a file and read that file after it was completely
# received from the master.
#
# In many cases the disk is slower than the network, and storing and loading
# the RDB file may increase replication time (and even increase the master&#39;s
# Copy on Write memory and salve buffers).
# However, parsing the RDB file directly from the socket may mean that we have
# to flush the contents of the current database before the full rdb was
# received. For this reason we have the following options:
#
# &quot;disabled&quot;    - Don&#39;t use diskless load (store the rdb file to the disk first)
# &quot;on-empty-db&quot; - Use diskless load only when it is completely safe.
# &quot;swapdb&quot;      - Keep a copy of the current db contents in RAM while parsing
#                 the data directly from the socket. note that this requires
#                 sufficient memory, if you don&#39;t have it, you risk an OOM kill.
repl-diskless-load disabled

# Replicas send PINGs to server in a predefined interval. It&#39;s possible to
# change this interval with the repl_ping_replica_period option. The default
# value is 10 seconds.
#
# repl-ping-replica-period 10

# The following option sets the replication timeout for:
#
# 1) Bulk transfer I/O during SYNC, from the point of view of replica.
# 2) Master timeout from the point of view of replicas (data, pings).
# 3) Replica timeout from the point of view of masters (REPLCONF ACK pings).
#
# It is important to make sure that this value is greater than the value
# specified for repl-ping-replica-period otherwise a timeout will be detected
# every time there is low traffic between the master and the replica. The default
# value is 60 seconds.
#
# repl-timeout 60

# Disable TCP_NODELAY on the replica socket after SYNC?
#
# If you select &quot;yes&quot; Redis will use a smaller number of TCP packets and
# less bandwidth to send data to replicas. But this can add a delay for
# the data to appear on the replica side, up to 40 milliseconds with
# Linux kernels using a default configuration.
#
# If you select &quot;no&quot; the delay for data to appear on the replica side will
# be reduced but more bandwidth will be used for replication.
#
# By default we optimize for low latency, but in very high traffic conditions
# or when the master and replicas are many hops away, turning this to &quot;yes&quot; may
# be a good idea.
repl-disable-tcp-nodelay no

# Set the replication backlog size. The backlog is a buffer that accumulates
# replica data when replicas are disconnected for some time, so that when a
# replica wants to reconnect again, often a full resync is not needed, but a
# partial resync is enough, just passing the portion of data the replica
# missed while disconnected.
#
# The bigger the replication backlog, the longer the replica can endure the
# disconnect and later be able to perform a partial resynchronization.
#
# The backlog is only allocated if there is at least one replica connected.
#
# repl-backlog-size 1mb

# After a master has no connected replicas for some time, the backlog will be
# freed. The following option configures the amount of seconds that need to
# elapse, starting from the time the last replica disconnected, for the backlog
# buffer to be freed.
#
# Note that replicas never free the backlog for timeout, since they may be
# promoted to masters later, and should be able to correctly &quot;partially
# resynchronize&quot; with other replicas: hence they should always accumulate backlog.
#
# A value of 0 means to never release the backlog.
#
# repl-backlog-ttl 3600

# The replica priority is an integer number published by Redis in the INFO
# output. It is used by Redis Sentinel in order to select a replica to promote
# into a master if the master is no longer working correctly.
#
# A replica with a low priority number is considered better for promotion, so
# for instance if there are three replicas with priority 10, 100, 25 Sentinel
# will pick the one with priority 10, that is the lowest.
#
# However a special priority of 0 marks the replica as not able to perform the
# role of master, so a replica with priority of 0 will never be selected by
# Redis Sentinel for promotion.
#
# By default the priority is 100.
replica-priority 100

# -----------------------------------------------------------------------------
# By default, Redis Sentinel includes all replicas in its reports. A replica
# can be excluded from Redis Sentinel&#39;s announcements. An unannounced replica
# will be ignored by the &#39;sentinel replicas &lt;master&gt;&#39; command and won&#39;t be
# exposed to Redis Sentinel&#39;s clients.
#
# This option does not change the behavior of replica-priority. Even with
# replica-announced set to &#39;no&#39;, the replica can be promoted to master. To
# prevent this behavior, set replica-priority to 0.
#
# replica-announced yes

# It is possible for a master to stop accepting writes if there are less than
# N replicas connected, having a lag less or equal than M seconds.
#
# The N replicas need to be in &quot;online&quot; state.
#
# The lag in seconds, that must be &lt;= the specified value, is calculated from
# the last ping received from the replica, that is usually sent every second.
#
# This option does not GUARANTEE that N replicas will accept the write, but
# will limit the window of exposure for lost writes in case not enough replicas
# are available, to the specified number of seconds.
#
# For example to require at least 3 replicas with a lag &lt;= 10 seconds use:
#
# min-replicas-to-write 3
# min-replicas-max-lag 10
#
# Setting one or the other to 0 disables the feature.
#
# By default min-replicas-to-write is set to 0 (feature disabled) and
# min-replicas-max-lag is set to 10.

# A Redis master is able to list the address and port of the attached
# replicas in different ways. For example the &quot;INFO replication&quot; section
# offers this information, which is used, among other tools, by
# Redis Sentinel in order to discover replica instances.
# Another place where this info is available is in the output of the
# &quot;ROLE&quot; command of a master.
#
# The listed IP address and port normally reported by a replica is
# obtained in the following way:
#
#   IP: The address is auto detected by checking the peer address
#   of the socket used by the replica to connect with the master.
#
#   Port: The port is communicated by the replica during the replication
#   handshake, and is normally the port that the replica is using to
#   listen for connections.
#
# However when port forwarding or Network Address Translation (NAT) is
# used, the replica may actually be reachable via different IP and port
# pairs. The following two options can be used by a replica in order to
# report to its master a specific set of IP and port, so that both INFO
# and ROLE will report those values.
#
# There is no need to use both the options if you need to override just
# the port or the IP address.
#
# replica-announce-ip 5.5.5.5
# replica-announce-port 1234

############################### KEYS TRACKING #################################

# Redis implements server assisted support for client side caching of values.
# This is implemented using an invalidation table that remembers, using
# a radix key indexed by key name, what clients have which keys. In turn
# this is used in order to send invalidation messages to clients. Please
# check this page to understand more about the feature:
#
#   https://redis.io/topics/client-side-caching
#
# When tracking is enabled for a client, all the read only queries are assumed
# to be cached: this will force Redis to store information in the invalidation
# table. When keys are modified, such information is flushed away, and
# invalidation messages are sent to the clients. However if the workload is
# heavily dominated by reads, Redis could use more and more memory in order
# to track the keys fetched by many clients.
#
# For this reason it is possible to configure a maximum fill value for the
# invalidation table. By default it is set to 1M of keys, and once this limit
# is reached, Redis will start to evict keys in the invalidation table
# even if they were not modified, just to reclaim memory: this will in turn
# force the clients to invalidate the cached values. Basically the table
# maximum size is a trade off between the memory you want to spend server
# side to track information about who cached what, and the ability of clients
# to retain cached objects in memory.
#
# If you set the value to 0, it means there are no limits, and Redis will
# retain as many keys as needed in the invalidation table.
# In the &quot;stats&quot; INFO section, you can find information about the number of
# keys in the invalidation table at every given moment.
#
# Note: when key tracking is used in broadcasting mode, no memory is used
# in the server side so this setting is useless.
#
# tracking-table-max-keys 1000000

################################## SECURITY ###################################

# Warning: since Redis is pretty fast, an outside user can try up to
# 1 million passwords per second against a modern box. This means that you
# should use very strong passwords, otherwise they will be very easy to break.
# Note that because the password is really a shared secret between the client
# and the server, and should not be memorized by any human, the password
# can be easily a long string from /dev/urandom or whatever, so by using a
# long and unguessable password no brute force attack will be possible.

# Redis ACL users are defined in the following format:
#
#   user &lt;username&gt; ... acl rules ...
#
# For example:
#
#   user worker +@list +@connection ~jobs:* on &gt;ffa9203c493aa99
#
# The special username &quot;default&quot; is used for new connections. If this user
# has the &quot;nopass&quot; rule, then new connections will be immediately authenticated
# as the &quot;default&quot; user without the need of any password provided via the
# AUTH command. Otherwise if the &quot;default&quot; user is not flagged with &quot;nopass&quot;
# the connections will start in not authenticated state, and will require
# AUTH (or the HELLO command AUTH option) in order to be authenticated and
# start to work.
#
# The ACL rules that describe what a user can do are the following:
#
#  on           Enable the user: it is possible to authenticate as this user.
#  off          Disable the user: it&#39;s no longer possible to authenticate
#               with this user, however the already authenticated connections
#               will still work.
#  skip-sanitize-payload    RESTORE dump-payload sanitation is skipped.
#  sanitize-payload         RESTORE dump-payload is sanitized (default).
#  +&lt;command&gt;   Allow the execution of that command
#  -&lt;command&gt;   Disallow the execution of that command
#  +@&lt;category&gt; Allow the execution of all the commands in such category
#               with valid categories are like @admin, @set, @sortedset, ...
#               and so forth, see the full list in the server.c file where
#               the Redis command table is described and defined.
#               The special category @all means all the commands, but currently
#               present in the server, and that will be loaded in the future
#               via modules.
#  +&lt;command&gt;|subcommand    Allow a specific subcommand of an otherwise
#                           disabled command. Note that this form is not
#                           allowed as negative like -DEBUG|SEGFAULT, but
#                           only additive starting with &quot;+&quot;.
#  allcommands  Alias for +@all. Note that it implies the ability to execute
#               all the future commands loaded via the modules system.
#  nocommands   Alias for -@all.
#  ~&lt;pattern&gt;   Add a pattern of keys that can be mentioned as part of
#               commands. For instance ~* allows all the keys. The pattern
#               is a glob-style pattern like the one of KEYS.
#               It is possible to specify multiple patterns.
#  allkeys      Alias for ~*
#  resetkeys    Flush the list of allowed keys patterns.
#  &amp;&lt;pattern&gt;   Add a glob-style pattern of Pub/Sub channels that can be
#               accessed by the user. It is possible to specify multiple channel
#               patterns.
#  allchannels  Alias for &amp;*
#  resetchannels            Flush the list of allowed channel patterns.
#  &gt;&lt;password&gt;  Add this password to the list of valid password for the user.
#               For example &gt;mypass will add &quot;mypass&quot; to the list.
#               This directive clears the &quot;nopass&quot; flag (see later).
#  &lt;&lt;password&gt;  Remove this password from the list of valid passwords.
#  nopass       All the set passwords of the user are removed, and the user
#               is flagged as requiring no password: it means that every
#               password will work against this user. If this directive is
#               used for the default user, every new connection will be
#               immediately authenticated with the default user without
#               any explicit AUTH command required. Note that the &quot;resetpass&quot;
#               directive will clear this condition.
#  resetpass    Flush the list of allowed passwords. Moreover removes the
#               &quot;nopass&quot; status. After &quot;resetpass&quot; the user has no associated
#               passwords and there is no way to authenticate without adding
#               some password (or setting it as &quot;nopass&quot; later).
#  reset        Performs the following actions: resetpass, resetkeys, off,
#               -@all. The user returns to the same state it has immediately
#               after its creation.
#
# ACL rules can be specified in any order: for instance you can start with
# passwords, then flags, or key patterns. However note that the additive
# and subtractive rules will CHANGE MEANING depending on the ordering.
# For instance see the following example:
#
#   user alice on +@all -DEBUG ~* &gt;somepassword
#
# This will allow &quot;alice&quot; to use all the commands with the exception of the
# DEBUG command, since +@all added all the commands to the set of the commands
# alice can use, and later DEBUG was removed. However if we invert the order
# of two ACL rules the result will be different:
#
#   user alice on -DEBUG +@all ~* &gt;somepassword
#
# Now DEBUG was removed when alice had yet no commands in the set of allowed
# commands, later all the commands are added, so the user will be able to
# execute everything.
#
# Basically ACL rules are processed left-to-right.
#
# For more information about ACL configuration please refer to
# the Redis web site at https://redis.io/topics/acl

# ACL LOG
#
# The ACL Log tracks failed commands and authentication events associated
# with ACLs. The ACL Log is useful to troubleshoot failed commands blocked 
# by ACLs. The ACL Log is stored in memory. You can reclaim memory with 
# ACL LOG RESET. Define the maximum entry length of the ACL Log below.
acllog-max-len 128

# Using an external ACL file
#
# Instead of configuring users here in this file, it is possible to use
# a stand-alone file just listing users. The two methods cannot be mixed:
# if you configure users here and at the same time you activate the external
# ACL file, the server will refuse to start.
#
# The format of the external ACL user file is exactly the same as the
# format that is used inside redis.conf to describe users.
#
# aclfile /etc/redis/users.acl

# IMPORTANT NOTE: starting with Redis 6 &quot;requirepass&quot; is just a compatibility
# layer on top of the new ACL system. The option effect will be just setting
# the password for the default user. Clients will still authenticate using
# AUTH &lt;password&gt; as usually, or more explicitly with AUTH default &lt;password&gt;
# if they follow the new protocol: both will work.
#
# The requirepass is not compatable with aclfile option and the ACL LOAD
# command, these will cause requirepass to be ignored.
#
requirepass 123456

# New users are initialized with restrictive permissions by default, via the
# equivalent of this ACL rule &#39;off resetkeys -@all&#39;. Starting with Redis 6.2, it
# is possible to manage access to Pub/Sub channels with ACL rules as well. The
# default Pub/Sub channels permission if new users is controlled by the 
# acl-pubsub-default configuration directive, which accepts one of these values:
#
# allchannels: grants access to all Pub/Sub channels
# resetchannels: revokes access to all Pub/Sub channels
#
# To ensure backward compatibility while upgrading Redis 6.0, acl-pubsub-default
# defaults to the &#39;allchannels&#39; permission.
#
# Future compatibility note: it is very likely that in a future version of Redis
# the directive&#39;s default of &#39;allchannels&#39; will be changed to &#39;resetchannels&#39; in
# order to provide better out-of-the-box Pub/Sub security. Therefore, it is
# recommended that you explicitly define Pub/Sub permissions for all users
# rather then rely on implicit default values. Once you&#39;ve set explicit
# Pub/Sub for all existing users, you should uncomment the following line.
#
# acl-pubsub-default resetchannels

# Command renaming (DEPRECATED).
#
# ------------------------------------------------------------------------
# WARNING: avoid using this option if possible. Instead use ACLs to remove
# commands from the default user, and put them only in some admin user you
# create for administrative purposes.
# ------------------------------------------------------------------------
#
# It is possible to change the name of dangerous commands in a shared
# environment. For instance the CONFIG command may be renamed into something
# hard to guess so that it will still be available for internal-use tools
# but not available for general clients.
#
# Example:
#
# rename-command CONFIG b840fc02d524045429941cc15f59e41cb7be6c52
#
# It is also possible to completely kill a command by renaming it into
# an empty string:
#
# rename-command CONFIG &quot;&quot;
#
# Please note that changing the name of commands that are logged into the
# AOF file or transmitted to replicas may cause problems.

################################### CLIENTS ####################################

# Set the max number of connected clients at the same time. By default
# this limit is set to 10000 clients, however if the Redis server is not
# able to configure the process file limit to allow for the specified limit
# the max number of allowed clients is set to the current file limit
# minus 32 (as Redis reserves a few file descriptors for internal uses).
#
# Once the limit is reached Redis will close all the new connections sending
# an error &#39;max number of clients reached&#39;.
#
# IMPORTANT: When Redis Cluster is used, the max number of connections is also
# shared with the cluster bus: every node in the cluster will use two
# connections, one incoming and another outgoing. It is important to size the
# limit accordingly in case of very large clusters.
#
# maxclients 10000

############################## MEMORY MANAGEMENT ################################

# Set a memory usage limit to the specified amount of bytes.
# When the memory limit is reached Redis will try to remove keys
# according to the eviction policy selected (see maxmemory-policy).
#
# If Redis can&#39;t remove keys according to the policy, or if the policy is
# set to &#39;noeviction&#39;, Redis will start to reply with errors to commands
# that would use more memory, like SET, LPUSH, and so on, and will continue
# to reply to read-only commands like GET.
#
# This option is usually useful when using Redis as an LRU or LFU cache, or to
# set a hard memory limit for an instance (using the &#39;noeviction&#39; policy).
#
# WARNING: If you have replicas attached to an instance with maxmemory on,
# the size of the output buffers needed to feed the replicas are subtracted
# from the used memory count, so that network problems / resyncs will
# not trigger a loop where keys are evicted, and in turn the output
# buffer of replicas is full with DELs of keys evicted triggering the deletion
# of more keys, and so forth until the database is completely emptied.
#
# In short... if you have replicas attached it is suggested that you set a lower
# limit for maxmemory so that there is some free RAM on the system for replica
# output buffers (but this is not needed if the policy is &#39;noeviction&#39;).
#
# maxmemory &lt;bytes&gt;

# MAXMEMORY POLICY: how Redis will select what to remove when maxmemory
# is reached. You can select one from the following behaviors:
#
# volatile-lru -&gt; Evict using approximated LRU, only keys with an expire set.
# allkeys-lru -&gt; Evict any key using approximated LRU.
# volatile-lfu -&gt; Evict using approximated LFU, only keys with an expire set.
# allkeys-lfu -&gt; Evict any key using approximated LFU.
# volatile-random -&gt; Remove a random key having an expire set.
# allkeys-random -&gt; Remove a random key, any key.
# volatile-ttl -&gt; Remove the key with the nearest expire time (minor TTL)
# noeviction -&gt; Don&#39;t evict anything, just return an error on write operations.
#
# LRU means Least Recently Used
# LFU means Least Frequently Used
#
# Both LRU, LFU and volatile-ttl are implemented using approximated
# randomized algorithms.
#
# Note: with any of the above policies, when there are no suitable keys for
# eviction, Redis will return an error on write operations that require
# more memory. These are usually commands that create new keys, add data or
# modify existing keys. A few examples are: SET, INCR, HSET, LPUSH, SUNIONSTORE,
# SORT (due to the STORE argument), and EXEC (if the transaction includes any
# command that requires memory).
#
# The default is:
#
# maxmemory-policy noeviction

# LRU, LFU and minimal TTL algorithms are not precise algorithms but approximated
# algorithms (in order to save memory), so you can tune it for speed or
# accuracy. By default Redis will check five keys and pick the one that was
# used least recently, you can change the sample size using the following
# configuration directive.
#
# The default of 5 produces good enough results. 10 Approximates very closely
# true LRU but costs more CPU. 3 is faster but not very accurate.
#
# maxmemory-samples 5

# Eviction processing is designed to function well with the default setting.
# If there is an unusually large amount of write traffic, this value may need to
# be increased.  Decreasing this value may reduce latency at the risk of 
# eviction processing effectiveness
#   0 = minimum latency, 10 = default, 100 = process without regard to latency
#
# maxmemory-eviction-tenacity 10

# Starting from Redis 5, by default a replica will ignore its maxmemory setting
# (unless it is promoted to master after a failover or manually). It means
# that the eviction of keys will be just handled by the master, sending the
# DEL commands to the replica as keys evict in the master side.
#
# This behavior ensures that masters and replicas stay consistent, and is usually
# what you want, however if your replica is writable, or you want the replica
# to have a different memory setting, and you are sure all the writes performed
# to the replica are idempotent, then you may change this default (but be sure
# to understand what you are doing).
#
# Note that since the replica by default does not evict, it may end using more
# memory than the one set via maxmemory (there are certain buffers that may
# be larger on the replica, or data structures may sometimes take more memory
# and so forth). So make sure you monitor your replicas and make sure they
# have enough memory to never hit a real out-of-memory condition before the
# master hits the configured maxmemory setting.
#
# replica-ignore-maxmemory yes

# Redis reclaims expired keys in two ways: upon access when those keys are
# found to be expired, and also in background, in what is called the
# &quot;active expire key&quot;. The key space is slowly and interactively scanned
# looking for expired keys to reclaim, so that it is possible to free memory
# of keys that are expired and will never be accessed again in a short time.
#
# The default effort of the expire cycle will try to avoid having more than
# ten percent of expired keys still in memory, and will try to avoid consuming
# more than 25% of total memory and to add latency to the system. However
# it is possible to increase the expire &quot;effort&quot; that is normally set to
# &quot;1&quot;, to a greater value, up to the value &quot;10&quot;. At its maximum value the
# system will use more CPU, longer cycles (and technically may introduce
# more latency), and will tolerate less already expired keys still present
# in the system. It&#39;s a tradeoff between memory, CPU and latency.
#
# active-expire-effort 1

############################# LAZY FREEING ####################################

# Redis has two primitives to delete keys. One is called DEL and is a blocking
# deletion of the object. It means that the server stops processing new commands
# in order to reclaim all the memory associated with an object in a synchronous
# way. If the key deleted is associated with a small object, the time needed
# in order to execute the DEL command is very small and comparable to most other
# O(1) or O(log_N) commands in Redis. However if the key is associated with an
# aggregated value containing millions of elements, the server can block for
# a long time (even seconds) in order to complete the operation.
#
# For the above reasons Redis also offers non blocking deletion primitives
# such as UNLINK (non blocking DEL) and the ASYNC option of FLUSHALL and
# FLUSHDB commands, in order to reclaim memory in background. Those commands
# are executed in constant time. Another thread will incrementally free the
# object in the background as fast as possible.
#
# DEL, UNLINK and ASYNC option of FLUSHALL and FLUSHDB are user-controlled.
# It&#39;s up to the design of the application to understand when it is a good
# idea to use one or the other. However the Redis server sometimes has to
# delete keys or flush the whole database as a side effect of other operations.
# Specifically Redis deletes objects independently of a user call in the
# following scenarios:
#
# 1) On eviction, because of the maxmemory and maxmemory policy configurations,
#    in order to make room for new data, without going over the specified
#    memory limit.
# 2) Because of expire: when a key with an associated time to live (see the
#    EXPIRE command) must be deleted from memory.
# 3) Because of a side effect of a command that stores data on a key that may
#    already exist. For example the RENAME command may delete the old key
#    content when it is replaced with another one. Similarly SUNIONSTORE
#    or SORT with STORE option may delete existing keys. The SET command
#    itself removes any old content of the specified key in order to replace
#    it with the specified string.
# 4) During replication, when a replica performs a full resynchronization with
#    its master, the content of the whole database is removed in order to
#    load the RDB file just transferred.
#
# In all the above cases the default is to delete objects in a blocking way,
# like if DEL was called. However you can configure each case specifically
# in order to instead release memory in a non-blocking way like if UNLINK
# was called, using the following configuration directives.

lazyfree-lazy-eviction no
lazyfree-lazy-expire no
lazyfree-lazy-server-del no
replica-lazy-flush no

# It is also possible, for the case when to replace the user code DEL calls
# with UNLINK calls is not easy, to modify the default behavior of the DEL
# command to act exactly like UNLINK, using the following configuration
# directive:

lazyfree-lazy-user-del no

# FLUSHDB, FLUSHALL, and SCRIPT FLUSH support both asynchronous and synchronous
# deletion, which can be controlled by passing the [SYNC|ASYNC] flags into the
# commands. When neither flag is passed, this directive will be used to determine
# if the data should be deleted asynchronously.

lazyfree-lazy-user-flush no

################################ THREADED I/O #################################

# Redis is mostly single threaded, however there are certain threaded
# operations such as UNLINK, slow I/O accesses and other things that are
# performed on side threads.
#
# Now it is also possible to handle Redis clients socket reads and writes
# in different I/O threads. Since especially writing is so slow, normally
# Redis users use pipelining in order to speed up the Redis performances per
# core, and spawn multiple instances in order to scale more. Using I/O
# threads it is possible to easily speedup two times Redis without resorting
# to pipelining nor sharding of the instance.
#
# By default threading is disabled, we suggest enabling it only in machines
# that have at least 4 or more cores, leaving at least one spare core.
# Using more than 8 threads is unlikely to help much. We also recommend using
# threaded I/O only if you actually have performance problems, with Redis
# instances being able to use a quite big percentage of CPU time, otherwise
# there is no point in using this feature.
#
# So for instance if you have a four cores boxes, try to use 2 or 3 I/O
# threads, if you have a 8 cores, try to use 6 threads. In order to
# enable I/O threads use the following configuration directive:
#
# io-threads 4
#
# Setting io-threads to 1 will just use the main thread as usual.
# When I/O threads are enabled, we only use threads for writes, that is
# to thread the write(2) syscall and transfer the client buffers to the
# socket. However it is also possible to enable threading of reads and
# protocol parsing using the following configuration directive, by setting
# it to yes:
#
# io-threads-do-reads no
#
# Usually threading reads doesn&#39;t help much.
#
# NOTE 1: This configuration directive cannot be changed at runtime via
# CONFIG SET. Aso this feature currently does not work when SSL is
# enabled.
#
# NOTE 2: If you want to test the Redis speedup using redis-benchmark, make
# sure you also run the benchmark itself in threaded mode, using the
# --threads option to match the number of Redis threads, otherwise you&#39;ll not
# be able to notice the improvements.

############################ KERNEL OOM CONTROL ##############################

# On Linux, it is possible to hint the kernel OOM killer on what processes
# should be killed first when out of memory.
#
# Enabling this feature makes Redis actively control the oom_score_adj value
# for all its processes, depending on their role. The default scores will
# attempt to have background child processes killed before all others, and
# replicas killed before masters.
#
# Redis supports three options:
#
# no:       Don&#39;t make changes to oom-score-adj (default).
# yes:      Alias to &quot;relative&quot; see below.
# absolute: Values in oom-score-adj-values are written as is to the kernel.
# relative: Values are used relative to the initial value of oom_score_adj when
#           the server starts and are then clamped to a range of -1000 to 1000.
#           Because typically the initial value is 0, they will often match the
#           absolute values.
oom-score-adj no

# When oom-score-adj is used, this directive controls the specific values used
# for master, replica and background child processes. Values range -2000 to
# 2000 (higher means more likely to be killed).
#
# Unprivileged processes (not root, and without CAP_SYS_RESOURCE capabilities)
# can freely increase their value, but not decrease it below its initial
# settings. This means that setting oom-score-adj to &quot;relative&quot; and setting the
# oom-score-adj-values to positive values will always succeed.
oom-score-adj-values 0 200 800


#################### KERNEL transparent hugepage CONTROL ######################

# Usually the kernel Transparent Huge Pages control is set to &quot;madvise&quot; or
# or &quot;never&quot; by default (/sys/kernel/mm/transparent_hugepage/enabled), in which
# case this config has no effect. On systems in which it is set to &quot;always&quot;,
# redis will attempt to disable it specifically for the redis process in order
# to avoid latency problems specifically with fork(2) and CoW.
# If for some reason you prefer to keep it enabled, you can set this config to
# &quot;no&quot; and the kernel global to &quot;always&quot;.

disable-thp yes

############################## APPEND ONLY MODE ###############################

# By default Redis asynchronously dumps the dataset on disk. This mode is
# good enough in many applications, but an issue with the Redis process or
# a power outage may result into a few minutes of writes lost (depending on
# the configured save points).
#
# The Append Only File is an alternative persistence mode that provides
# much better durability. For instance using the default data fsync policy
# (see later in the config file) Redis can lose just one second of writes in a
# dramatic event like a server power outage, or a single write if something
# wrong with the Redis process itself happens, but the operating system is
# still running correctly.
#
# AOF and RDB persistence can be enabled at the same time without problems.
# If the AOF is enabled on startup Redis will load the AOF, that is the file
# with the better durability guarantees.
#
# Please check https://redis.io/topics/persistence for more information.

appendonly no

# The name of the append only file (default: &quot;appendonly.aof&quot;)

appendfilename &quot;appendonly.aof&quot;

# The fsync() call tells the Operating System to actually write data on disk
# instead of waiting for more data in the output buffer. Some OS will really flush
# data on disk, some other OS will just try to do it ASAP.
#
# Redis supports three different modes:
#
# no: don&#39;t fsync, just let the OS flush the data when it wants. Faster.
# always: fsync after every write to the append only log. Slow, Safest.
# everysec: fsync only one time every second. Compromise.
#
# The default is &quot;everysec&quot;, as that&#39;s usually the right compromise between
# speed and data safety. It&#39;s up to you to understand if you can relax this to
# &quot;no&quot; that will let the operating system flush the output buffer when
# it wants, for better performances (but if you can live with the idea of
# some data loss consider the default persistence mode that&#39;s snapshotting),
# or on the contrary, use &quot;always&quot; that&#39;s very slow but a bit safer than
# everysec.
#
# More details please check the following article:
# http://antirez.com/post/redis-persistence-demystified.html
#
# If unsure, use &quot;everysec&quot;.

# appendfsync always
appendfsync everysec
# appendfsync no

# When the AOF fsync policy is set to always or everysec, and a background
# saving process (a background save or AOF log background rewriting) is
# performing a lot of I/O against the disk, in some Linux configurations
# Redis may block too long on the fsync() call. Note that there is no fix for
# this currently, as even performing fsync in a different thread will block
# our synchronous write(2) call.
#
# In order to mitigate this problem it&#39;s possible to use the following option
# that will prevent fsync() from being called in the main process while a
# BGSAVE or BGREWRITEAOF is in progress.
#
# This means that while another child is saving, the durability of Redis is
# the same as &quot;appendfsync none&quot;. In practical terms, this means that it is
# possible to lose up to 30 seconds of log in the worst scenario (with the
# default Linux settings).
#
# If you have latency problems turn this to &quot;yes&quot;. Otherwise leave it as
# &quot;no&quot; that is the safest pick from the point of view of durability.

no-appendfsync-on-rewrite no

# Automatic rewrite of the append only file.
# Redis is able to automatically rewrite the log file implicitly calling
# BGREWRITEAOF when the AOF log size grows by the specified percentage.
#
# This is how it works: Redis remembers the size of the AOF file after the
# latest rewrite (if no rewrite has happened since the restart, the size of
# the AOF at startup is used).
#
# This base size is compared to the current size. If the current size is
# bigger than the specified percentage, the rewrite is triggered. Also
# you need to specify a minimal size for the AOF file to be rewritten, this
# is useful to avoid rewriting the AOF file even if the percentage increase
# is reached but it is still pretty small.
#
# Specify a percentage of zero in order to disable the automatic AOF
# rewrite feature.

auto-aof-rewrite-percentage 100
auto-aof-rewrite-min-size 64mb

# An AOF file may be found to be truncated at the end during the Redis
# startup process, when the AOF data gets loaded back into memory.
# This may happen when the system where Redis is running
# crashes, especially when an ext4 filesystem is mounted without the
# data=ordered option (however this can&#39;t happen when Redis itself
# crashes or aborts but the operating system still works correctly).
#
# Redis can either exit with an error when this happens, or load as much
# data as possible (the default now) and start if the AOF file is found
# to be truncated at the end. The following option controls this behavior.
#
# If aof-load-truncated is set to yes, a truncated AOF file is loaded and
# the Redis server starts emitting a log to inform the user of the event.
# Otherwise if the option is set to no, the server aborts with an error
# and refuses to start. When the option is set to no, the user requires
# to fix the AOF file using the &quot;redis-check-aof&quot; utility before to restart
# the server.
#
# Note that if the AOF file will be found to be corrupted in the middle
# the server will still exit with an error. This option only applies when
# Redis will try to read more data from the AOF file but not enough bytes
# will be found.
aof-load-truncated yes

# When rewriting the AOF file, Redis is able to use an RDB preamble in the
# AOF file for faster rewrites and recoveries. When this option is turned
# on the rewritten AOF file is composed of two different stanzas:
#
#   [RDB file][AOF tail]
#
# When loading, Redis recognizes that the AOF file starts with the &quot;REDIS&quot;
# string and loads the prefixed RDB file, then continues loading the AOF
# tail.
aof-use-rdb-preamble yes

################################ LUA SCRIPTING  ###############################

# Max execution time of a Lua script in milliseconds.
#
# If the maximum execution time is reached Redis will log that a script is
# still in execution after the maximum allowed time and will start to
# reply to queries with an error.
#
# When a long running script exceeds the maximum execution time only the
# SCRIPT KILL and SHUTDOWN NOSAVE commands are available. The first can be
# used to stop a script that did not yet call any write commands. The second
# is the only way to shut down the server in the case a write command was
# already issued by the script but the user doesn&#39;t want to wait for the natural
# termination of the script.
#
# Set it to 0 or a negative value for unlimited execution without warnings.
lua-time-limit 5000

################################ REDIS CLUSTER  ###############################

# Normal Redis instances can&#39;t be part of a Redis Cluster; only nodes that are
# started as cluster nodes can. In order to start a Redis instance as a
# cluster node enable the cluster support uncommenting the following:
#
# cluster-enabled yes

# Every cluster node has a cluster configuration file. This file is not
# intended to be edited by hand. It is created and updated by Redis nodes.
# Every Redis Cluster node requires a different cluster configuration file.
# Make sure that instances running in the same system do not have
# overlapping cluster configuration file names.
#
# cluster-config-file nodes-6379.conf

# Cluster node timeout is the amount of milliseconds a node must be unreachable
# for it to be considered in failure state.
# Most other internal time limits are a multiple of the node timeout.
#
# cluster-node-timeout 15000

# A replica of a failing master will avoid to start a failover if its data
# looks too old.
#
# There is no simple way for a replica to actually have an exact measure of
# its &quot;data age&quot;, so the following two checks are performed:
#
# 1) If there are multiple replicas able to failover, they exchange messages
#    in order to try to give an advantage to the replica with the best
#    replication offset (more data from the master processed).
#    Replicas will try to get their rank by offset, and apply to the start
#    of the failover a delay proportional to their rank.
#
# 2) Every single replica computes the time of the last interaction with
#    its master. This can be the last ping or command received (if the master
#    is still in the &quot;connected&quot; state), or the time that elapsed since the
#    disconnection with the master (if the replication link is currently down).
#    If the last interaction is too old, the replica will not try to failover
#    at all.
#
# The point &quot;2&quot; can be tuned by user. Specifically a replica will not perform
# the failover if, since the last interaction with the master, the time
# elapsed is greater than:
#
#   (node-timeout * cluster-replica-validity-factor) + repl-ping-replica-period
#
# So for example if node-timeout is 30 seconds, and the cluster-replica-validity-factor
# is 10, and assuming a default repl-ping-replica-period of 10 seconds, the
# replica will not try to failover if it was not able to talk with the master
# for longer than 310 seconds.
#
# A large cluster-replica-validity-factor may allow replicas with too old data to failover
# a master, while a too small value may prevent the cluster from being able to
# elect a replica at all.
#
# For maximum availability, it is possible to set the cluster-replica-validity-factor
# to a value of 0, which means, that replicas will always try to failover the
# master regardless of the last time they interacted with the master.
# (However they&#39;ll always try to apply a delay proportional to their
# offset rank).
#
# Zero is the only value able to guarantee that when all the partitions heal
# the cluster will always be able to continue.
#
# cluster-replica-validity-factor 10

# Cluster replicas are able to migrate to orphaned masters, that are masters
# that are left without working replicas. This improves the cluster ability
# to resist to failures as otherwise an orphaned master can&#39;t be failed over
# in case of failure if it has no working replicas.
#
# Replicas migrate to orphaned masters only if there are still at least a
# given number of other working replicas for their old master. This number
# is the &quot;migration barrier&quot;. A migration barrier of 1 means that a replica
# will migrate only if there is at least 1 other working replica for its master
# and so forth. It usually reflects the number of replicas you want for every
# master in your cluster.
#
# Default is 1 (replicas migrate only if their masters remain with at least
# one replica). To disable migration just set it to a very large value or
# set cluster-allow-replica-migration to &#39;no&#39;.
# A value of 0 can be set but is useful only for debugging and dangerous
# in production.
#
# cluster-migration-barrier 1

# Turning off this option allows to use less automatic cluster configuration.
# It both disables migration to orphaned masters and migration from masters
# that became empty.
#
# Default is &#39;yes&#39; (allow automatic migrations).
#
# cluster-allow-replica-migration yes

# By default Redis Cluster nodes stop accepting queries if they detect there
# is at least a hash slot uncovered (no available node is serving it).
# This way if the cluster is partially down (for example a range of hash slots
# are no longer covered) all the cluster becomes, eventually, unavailable.
# It automatically returns available as soon as all the slots are covered again.
#
# However sometimes you want the subset of the cluster which is working,
# to continue to accept queries for the part of the key space that is still
# covered. In order to do so, just set the cluster-require-full-coverage
# option to no.
#
# cluster-require-full-coverage yes

# This option, when set to yes, prevents replicas from trying to failover its
# master during master failures. However the replica can still perform a
# manual failover, if forced to do so.
#
# This is useful in different scenarios, especially in the case of multiple
# data center operations, where we want one side to never be promoted if not
# in the case of a total DC failure.
#
# cluster-replica-no-failover no

# This option, when set to yes, allows nodes to serve read traffic while the
# the cluster is in a down state, as long as it believes it owns the slots. 
#
# This is useful for two cases.  The first case is for when an application 
# doesn&#39;t require consistency of data during node failures or network partitions.
# One example of this is a cache, where as long as the node has the data it
# should be able to serve it. 
#
# The second use case is for configurations that don&#39;t meet the recommended  
# three shards but want to enable cluster mode and scale later. A 
# master outage in a 1 or 2 shard configuration causes a read/write outage to the
# entire cluster without this option set, with it set there is only a write outage.
# Without a quorum of masters, slot ownership will not change automatically. 
#
# cluster-allow-reads-when-down no

# In order to setup your cluster make sure to read the documentation
# available at https://redis.io web site.

########################## CLUSTER DOCKER/NAT support  ########################

# In certain deployments, Redis Cluster nodes address discovery fails, because
# addresses are NAT-ted or because ports are forwarded (the typical case is
# Docker and other containers).
#
# In order to make Redis Cluster working in such environments, a static
# configuration where each node knows its public address is needed. The
# following four options are used for this scope, and are:
#
# * cluster-announce-ip
# * cluster-announce-port
# * cluster-announce-tls-port
# * cluster-announce-bus-port
#
# Each instructs the node about its address, client ports (for connections
# without and with TLS) and cluster message bus port. The information is then
# published in the header of the bus packets so that other nodes will be able to
# correctly map the address of the node publishing the information.
#
# If cluster-tls is set to yes and cluster-announce-tls-port is omitted or set
# to zero, then cluster-announce-port refers to the TLS port. Note also that
# cluster-announce-tls-port has no effect if cluster-tls is set to no.
#
# If the above options are not used, the normal Redis Cluster auto-detection
# will be used instead.
#
# Note that when remapped, the bus port may not be at the fixed offset of
# clients port + 10000, so you can specify any port and bus-port depending
# on how they get remapped. If the bus-port is not set, a fixed offset of
# 10000 will be used as usual.
#
# Example:
#
# cluster-announce-ip 10.1.1.5
# cluster-announce-tls-port 6379
# cluster-announce-port 0
# cluster-announce-bus-port 6380

################################## SLOW LOG ###################################

# The Redis Slow Log is a system to log queries that exceeded a specified
# execution time. The execution time does not include the I/O operations
# like talking with the client, sending the reply and so forth,
# but just the time needed to actually execute the command (this is the only
# stage of command execution where the thread is blocked and can not serve
# other requests in the meantime).
#
# You can configure the slow log with two parameters: one tells Redis
# what is the execution time, in microseconds, to exceed in order for the
# command to get logged, and the other parameter is the length of the
# slow log. When a new command is logged the oldest one is removed from the
# queue of logged commands.

# The following time is expressed in microseconds, so 1000000 is equivalent
# to one second. Note that a negative number disables the slow log, while
# a value of zero forces the logging of every command.
slowlog-log-slower-than 10000

# There is no limit to this length. Just be aware that it will consume memory.
# You can reclaim memory used by the slow log with SLOWLOG RESET.
slowlog-max-len 128

################################ LATENCY MONITOR ##############################

# The Redis latency monitoring subsystem samples different operations
# at runtime in order to collect data related to possible sources of
# latency of a Redis instance.
#
# Via the LATENCY command this information is available to the user that can
# print graphs and obtain reports.
#
# The system only logs operations that were performed in a time equal or
# greater than the amount of milliseconds specified via the
# latency-monitor-threshold configuration directive. When its value is set
# to zero, the latency monitor is turned off.
#
# By default latency monitoring is disabled since it is mostly not needed
# if you don&#39;t have latency issues, and collecting data has a performance
# impact, that while very small, can be measured under big load. Latency
# monitoring can easily be enabled at runtime using the command
# &quot;CONFIG SET latency-monitor-threshold &lt;milliseconds&gt;&quot; if needed.
latency-monitor-threshold 0

############################# EVENT NOTIFICATION ##############################

# Redis can notify Pub/Sub clients about events happening in the key space.
# This feature is documented at https://redis.io/topics/notifications
#
# For instance if keyspace events notification is enabled, and a client
# performs a DEL operation on key &quot;foo&quot; stored in the Database 0, two
# messages will be published via Pub/Sub:
#
# PUBLISH __keyspace@0__:foo del
# PUBLISH __keyevent@0__:del foo
#
# It is possible to select the events that Redis will notify among a set
# of classes. Every class is identified by a single character:
#
#  K     Keyspace events, published with __keyspace@&lt;db&gt;__ prefix.
#  E     Keyevent events, published with __keyevent@&lt;db&gt;__ prefix.
#  g     Generic commands (non-type specific) like DEL, EXPIRE, RENAME, ...
#  $     String commands
#  l     List commands
#  s     Set commands
#  h     Hash commands
#  z     Sorted set commands
#  x     Expired events (events generated every time a key expires)
#  e     Evicted events (events generated when a key is evicted for maxmemory)
#  t     Stream commands
#  d     Module key type events
#  m     Key-miss events (Note: It is not included in the &#39;A&#39; class)
#  A     Alias for g$lshzxetd, so that the &quot;AKE&quot; string means all the events
#        (Except key-miss events which are excluded from &#39;A&#39; due to their
#         unique nature).
#
#  The &quot;notify-keyspace-events&quot; takes as argument a string that is composed
#  of zero or multiple characters. The empty string means that notifications
#  are disabled.
#
#  Example: to enable list and generic events, from the point of view of the
#           event name, use:
#
#  notify-keyspace-events Elg
#
#  Example 2: to get the stream of the expired keys subscribing to channel
#             name __keyevent@0__:expired use:
#
#  notify-keyspace-events Ex
#
#  By default all notifications are disabled because most users don&#39;t need
#  this feature and the feature has some overhead. Note that if you don&#39;t
#  specify at least one of K or E, no events will be delivered.
notify-keyspace-events &quot;&quot;

############################### GOPHER SERVER #################################

# Redis contains an implementation of the Gopher protocol, as specified in
# the RFC 1436 (https://www.ietf.org/rfc/rfc1436.txt).
#
# The Gopher protocol was very popular in the late &#39;90s. It is an alternative
# to the web, and the implementation both server and client side is so simple
# that the Redis server has just 100 lines of code in order to implement this
# support.
#
# What do you do with Gopher nowadays? Well Gopher never *really* died, and
# lately there is a movement in order for the Gopher more hierarchical content
# composed of just plain text documents to be resurrected. Some want a simpler
# internet, others believe that the mainstream internet became too much
# controlled, and it&#39;s cool to create an alternative space for people that
# want a bit of fresh air.
#
# Anyway for the 10nth birthday of the Redis, we gave it the Gopher protocol
# as a gift.
#
# --- HOW IT WORKS? ---
#
# The Redis Gopher support uses the inline protocol of Redis, and specifically
# two kind of inline requests that were anyway illegal: an empty request
# or any request that starts with &quot;/&quot; (there are no Redis commands starting
# with such a slash). Normal RESP2/RESP3 requests are completely out of the
# path of the Gopher protocol implementation and are served as usual as well.
#
# If you open a connection to Redis when Gopher is enabled and send it
# a string like &quot;/foo&quot;, if there is a key named &quot;/foo&quot; it is served via the
# Gopher protocol.
#
# In order to create a real Gopher &quot;hole&quot; (the name of a Gopher site in Gopher
# talking), you likely need a script like the following:
#
#   https://github.com/antirez/gopher2redis
#
# --- SECURITY WARNING ---
#
# If you plan to put Redis on the internet in a publicly accessible address
# to server Gopher pages MAKE SURE TO SET A PASSWORD to the instance.
# Once a password is set:
#
#   1. The Gopher server (when enabled, not by default) will still serve
#      content via Gopher.
#   2. However other commands cannot be called before the client will
#      authenticate.
#
# So use the &#39;requirepass&#39; option to protect your instance.
#
# Note that Gopher is not currently supported when &#39;io-threads-do-reads&#39;
# is enabled.
#
# To enable Gopher support, uncomment the following line and set the option
# from no (the default) to yes.
#
# gopher-enabled no

############################### ADVANCED CONFIG ###############################

# Hashes are encoded using a memory efficient data structure when they have a
# small number of entries, and the biggest entry does not exceed a given
# threshold. These thresholds can be configured using the following directives.
hash-max-ziplist-entries 512
hash-max-ziplist-value 64

# Lists are also encoded in a special way to save a lot of space.
# The number of entries allowed per internal list node can be specified
# as a fixed maximum size or a maximum number of elements.
# For a fixed maximum size, use -5 through -1, meaning:
# -5: max size: 64 Kb  &lt;-- not recommended for normal workloads
# -4: max size: 32 Kb  &lt;-- not recommended
# -3: max size: 16 Kb  &lt;-- probably not recommended
# -2: max size: 8 Kb   &lt;-- good
# -1: max size: 4 Kb   &lt;-- good
# Positive numbers mean store up to _exactly_ that number of elements
# per list node.
# The highest performing option is usually -2 (8 Kb size) or -1 (4 Kb size),
# but if your use case is unique, adjust the settings as necessary.
list-max-ziplist-size -2

# Lists may also be compressed.
# Compress depth is the number of quicklist ziplist nodes from *each* side of
# the list to *exclude* from compression.  The head and tail of the list
# are always uncompressed for fast push/pop operations.  Settings are:
# 0: disable all list compression
# 1: depth 1 means &quot;don&#39;t start compressing until after 1 node into the list,
#    going from either the head or tail&quot;
#    So: [head]-&gt;node-&gt;node-&gt;...-&gt;node-&gt;[tail]
#    [head], [tail] will always be uncompressed; inner nodes will compress.
# 2: [head]-&gt;[next]-&gt;node-&gt;node-&gt;...-&gt;node-&gt;[prev]-&gt;[tail]
#    2 here means: don&#39;t compress head or head-&gt;next or tail-&gt;prev or tail,
#    but compress all nodes between them.
# 3: [head]-&gt;[next]-&gt;[next]-&gt;node-&gt;node-&gt;...-&gt;node-&gt;[prev]-&gt;[prev]-&gt;[tail]
# etc.
list-compress-depth 0

# Sets have a special encoding in just one case: when a set is composed
# of just strings that happen to be integers in radix 10 in the range
# of 64 bit signed integers.
# The following configuration setting sets the limit in the size of the
# set in order to use this special memory saving encoding.
set-max-intset-entries 512

# Similarly to hashes and lists, sorted sets are also specially encoded in
# order to save a lot of space. This encoding is only used when the length and
# elements of a sorted set are below the following limits:
zset-max-ziplist-entries 128
zset-max-ziplist-value 64

# HyperLogLog sparse representation bytes limit. The limit includes the
# 16 bytes header. When an HyperLogLog using the sparse representation crosses
# this limit, it is converted into the dense representation.
#
# A value greater than 16000 is totally useless, since at that point the
# dense representation is more memory efficient.
#
# The suggested value is ~ 3000 in order to have the benefits of
# the space efficient encoding without slowing down too much PFADD,
# which is O(N) with the sparse encoding. The value can be raised to
# ~ 10000 when CPU is not a concern, but space is, and the data set is
# composed of many HyperLogLogs with cardinality in the 0 - 15000 range.
hll-sparse-max-bytes 3000

# Streams macro node max size / items. The stream data structure is a radix
# tree of big nodes that encode multiple items inside. Using this configuration
# it is possible to configure how big a single node can be in bytes, and the
# maximum number of items it may contain before switching to a new node when
# appending new stream entries. If any of the following settings are set to
# zero, the limit is ignored, so for instance it is possible to set just a
# max entries limit by setting max-bytes to 0 and max-entries to the desired
# value.
stream-node-max-bytes 4096
stream-node-max-entries 100

# Active rehashing uses 1 millisecond every 100 milliseconds of CPU time in
# order to help rehashing the main Redis hash table (the one mapping top-level
# keys to values). The hash table implementation Redis uses (see dict.c)
# performs a lazy rehashing: the more operation you run into a hash table
# that is rehashing, the more rehashing &quot;steps&quot; are performed, so if the
# server is idle the rehashing is never complete and some more memory is used
# by the hash table.
#
# The default is to use this millisecond 10 times every second in order to
# actively rehash the main dictionaries, freeing memory when possible.
#
# If unsure:
# use &quot;activerehashing no&quot; if you have hard latency requirements and it is
# not a good thing in your environment that Redis can reply from time to time
# to queries with 2 milliseconds delay.
#
# use &quot;activerehashing yes&quot; if you don&#39;t have such hard requirements but
# want to free memory asap when possible.
activerehashing yes

# The client output buffer limits can be used to force disconnection of clients
# that are not reading data from the server fast enough for some reason (a
# common reason is that a Pub/Sub client can&#39;t consume messages as fast as the
# publisher can produce them).
#
# The limit can be set differently for the three different classes of clients:
#
# normal -&gt; normal clients including MONITOR clients
# replica  -&gt; replica clients
# pubsub -&gt; clients subscribed to at least one pubsub channel or pattern
#
# The syntax of every client-output-buffer-limit directive is the following:
#
# client-output-buffer-limit &lt;class&gt; &lt;hard limit&gt; &lt;soft limit&gt; &lt;soft seconds&gt;
#
# A client is immediately disconnected once the hard limit is reached, or if
# the soft limit is reached and remains reached for the specified number of
# seconds (continuously).
# So for instance if the hard limit is 32 megabytes and the soft limit is
# 16 megabytes / 10 seconds, the client will get disconnected immediately
# if the size of the output buffers reach 32 megabytes, but will also get
# disconnected if the client reaches 16 megabytes and continuously overcomes
# the limit for 10 seconds.
#
# By default normal clients are not limited because they don&#39;t receive data
# without asking (in a push way), but just after a request, so only
# asynchronous clients may create a scenario where data is requested faster
# than it can read.
#
# Instead there is a default limit for pubsub and replica clients, since
# subscribers and replicas receive data in a push fashion.
#
# Both the hard or the soft limit can be disabled by setting them to zero.
client-output-buffer-limit normal 0 0 0
client-output-buffer-limit replica 256mb 64mb 60
client-output-buffer-limit pubsub 32mb 8mb 60

# Client query buffers accumulate new commands. They are limited to a fixed
# amount by default in order to avoid that a protocol desynchronization (for
# instance due to a bug in the client) will lead to unbound memory usage in
# the query buffer. However you can configure it here if you have very special
# needs, such us huge multi/exec requests or alike.
#
# client-query-buffer-limit 1gb

# In the Redis protocol, bulk requests, that are, elements representing single
# strings, are normally limited to 512 mb. However you can change this limit
# here, but must be 1mb or greater
#
# proto-max-bulk-len 512mb

# Redis calls an internal function to perform many background tasks, like
# closing connections of clients in timeout, purging expired keys that are
# never requested, and so forth.
#
# Not all tasks are performed with the same frequency, but Redis checks for
# tasks to perform according to the specified &quot;hz&quot; value.
#
# By default &quot;hz&quot; is set to 10. Raising the value will use more CPU when
# Redis is idle, but at the same time will make Redis more responsive when
# there are many keys expiring at the same time, and timeouts may be
# handled with more precision.
#
# The range is between 1 and 500, however a value over 100 is usually not
# a good idea. Most users should use the default of 10 and raise this up to
# 100 only in environments where very low latency is required.
hz 10

# Normally it is useful to have an HZ value which is proportional to the
# number of clients connected. This is useful in order, for instance, to
# avoid too many clients are processed for each background task invocation
# in order to avoid latency spikes.
#
# Since the default HZ value by default is conservatively set to 10, Redis
# offers, and enables by default, the ability to use an adaptive HZ value
# which will temporarily raise when there are many connected clients.
#
# When dynamic HZ is enabled, the actual configured HZ will be used
# as a baseline, but multiples of the configured HZ value will be actually
# used as needed once more clients are connected. In this way an idle
# instance will use very little CPU time while a busy instance will be
# more responsive.
dynamic-hz yes

# When a child rewrites the AOF file, if the following option is enabled
# the file will be fsync-ed every 32 MB of data generated. This is useful
# in order to commit the file to the disk more incrementally and avoid
# big latency spikes.
aof-rewrite-incremental-fsync yes

# When redis saves RDB file, if the following option is enabled
# the file will be fsync-ed every 32 MB of data generated. This is useful
# in order to commit the file to the disk more incrementally and avoid
# big latency spikes.
rdb-save-incremental-fsync yes

# Redis LFU eviction (see maxmemory setting) can be tuned. However it is a good
# idea to start with the default settings and only change them after investigating
# how to improve the performances and how the keys LFU change over time, which
# is possible to inspect via the OBJECT FREQ command.
#
# There are two tunable parameters in the Redis LFU implementation: the
# counter logarithm factor and the counter decay time. It is important to
# understand what the two parameters mean before changing them.
#
# The LFU counter is just 8 bits per key, it&#39;s maximum value is 255, so Redis
# uses a probabilistic increment with logarithmic behavior. Given the value
# of the old counter, when a key is accessed, the counter is incremented in
# this way:
#
# 1. A random number R between 0 and 1 is extracted.
# 2. A probability P is calculated as 1/(old_value*lfu_log_factor+1).
# 3. The counter is incremented only if R &lt; P.
#
# The default lfu-log-factor is 10. This is a table of how the frequency
# counter changes with a different number of accesses with different
# logarithmic factors:
#
# +--------+------------+------------+------------+------------+------------+
# | factor | 100 hits   | 1000 hits  | 100K hits  | 1M hits    | 10M hits   |
# +--------+------------+------------+------------+------------+------------+
# | 0      | 104        | 255        | 255        | 255        | 255        |
# +--------+------------+------------+------------+------------+------------+
# | 1      | 18         | 49         | 255        | 255        | 255        |
# +--------+------------+------------+------------+------------+------------+
# | 10     | 10         | 18         | 142        | 255        | 255        |
# +--------+------------+------------+------------+------------+------------+
# | 100    | 8          | 11         | 49         | 143        | 255        |
# +--------+------------+------------+------------+------------+------------+
#
# NOTE: The above table was obtained by running the following commands:
#
#   redis-benchmark -n 1000000 incr foo
#   redis-cli object freq foo
#
# NOTE 2: The counter initial value is 5 in order to give new objects a chance
# to accumulate hits.
#
# The counter decay time is the time, in minutes, that must elapse in order
# for the key counter to be divided by two (or decremented if it has a value
# less &lt;= 10).
#
# The default value for the lfu-decay-time is 1. A special value of 0 means to
# decay the counter every time it happens to be scanned.
#
# lfu-log-factor 10
# lfu-decay-time 1

########################### ACTIVE DEFRAGMENTATION #######################
#
# What is active defragmentation?
# -------------------------------
#
# Active (online) defragmentation allows a Redis server to compact the
# spaces left between small allocations and deallocations of data in memory,
# thus allowing to reclaim back memory.
#
# Fragmentation is a natural process that happens with every allocator (but
# less so with Jemalloc, fortunately) and certain workloads. Normally a server
# restart is needed in order to lower the fragmentation, or at least to flush
# away all the data and create it again. However thanks to this feature
# implemented by Oran Agra for Redis 4.0 this process can happen at runtime
# in a &quot;hot&quot; way, while the server is running.
#
# Basically when the fragmentation is over a certain level (see the
# configuration options below) Redis will start to create new copies of the
# values in contiguous memory regions by exploiting certain specific Jemalloc
# features (in order to understand if an allocation is causing fragmentation
# and to allocate it in a better place), and at the same time, will release the
# old copies of the data. This process, repeated incrementally for all the keys
# will cause the fragmentation to drop back to normal values.
#
# Important things to understand:
#
# 1. This feature is disabled by default, and only works if you compiled Redis
#    to use the copy of Jemalloc we ship with the source code of Redis.
#    This is the default with Linux builds.
#
# 2. You never need to enable this feature if you don&#39;t have fragmentation
#    issues.
#
# 3. Once you experience fragmentation, you can enable this feature when
#    needed with the command &quot;CONFIG SET activedefrag yes&quot;.
#
# The configuration parameters are able to fine tune the behavior of the
# defragmentation process. If you are not sure about what they mean it is
# a good idea to leave the defaults untouched.

# Enabled active defragmentation
# activedefrag no

# Minimum amount of fragmentation waste to start active defrag
# active-defrag-ignore-bytes 100mb

# Minimum percentage of fragmentation to start active defrag
# active-defrag-threshold-lower 10

# Maximum percentage of fragmentation at which we use maximum effort
# active-defrag-threshold-upper 100

# Minimal effort for defrag in CPU percentage, to be used when the lower
# threshold is reached
# active-defrag-cycle-min 1

# Maximal effort for defrag in CPU percentage, to be used when the upper
# threshold is reached
# active-defrag-cycle-max 25

# Maximum number of set/hash/zset/list fields that will be processed from
# the main dictionary scan
# active-defrag-max-scan-fields 1000

# Jemalloc background thread for purging will be enabled by default
jemalloc-bg-thread yes

# It is possible to pin different threads and processes of Redis to specific
# CPUs in your system, in order to maximize the performances of the server.
# This is useful both in order to pin different Redis threads in different
# CPUs, but also in order to make sure that multiple Redis instances running
# in the same host will be pinned to different CPUs.
#
# Normally you can do this using the &quot;taskset&quot; command, however it is also
# possible to this via Redis configuration directly, both in Linux and FreeBSD.
#
# You can pin the server/IO threads, bio threads, aof rewrite child process, and
# the bgsave child process. The syntax to specify the cpu list is the same as
# the taskset command:
#
# Set redis server/io threads to cpu affinity 0,2,4,6:
# server_cpulist 0-7:2
#
# Set bio threads to cpu affinity 1,3:
# bio_cpulist 1,3
#
# Set aof rewrite child process to cpu affinity 8,9,10,11:
# aof_rewrite_cpulist 8-11
#
# Set bgsave child process to cpu affinity 1,10,11
# bgsave_cpulist 1,10-11

# In some cases redis will emit warnings and even refuse to start if it detects
# that the system is in bad state, it is possible to suppress these warnings
# by setting the following config which takes a space delimited list of warnings
# to suppress
#
# ignore-warnings ARM64-COW-BUG
</code></pre>
<p>测试</p>
<pre><code>docker-compose up -d

docker exec -it redis /bin/bash

redis-cli -a 123456
</code></pre>
<h4 id="相关知识"><a href="#相关知识" class="headerlink" title="相关知识"></a>相关知识</h4><ol>
<li>默认有16个数据库，所有库密码相同，初始默认使用0号库</li>
<li>切换数据库：<code>select 数据库ID</code></li>
<li>查看当前数据key数量：<code>dbsize</code></li>
<li>清空当前库：<code>flushdb</code></li>
<li>清空全部库：<code>flushall</code></li>
</ol>
<h5 id="Redis是单线程-多路IO复用"><a href="#Redis是单线程-多路IO复用" class="headerlink" title="Redis是单线程+多路IO复用"></a>Redis是单线程+多路IO复用</h5><ul>
<li>Memcache是多线程 + 锁</li>
<li>Redis与 Memcache 三点不同：支持多数据类型，支持持久化，单线程 + 多路 IO 复用</li>
</ul>
<p>多路复用是指使用一个线程来检查多个文件描述符（Socket）的就绪状态，比如调用 select 和 poll 函数，传入多个文件描述符，如果有一个文件描述符就绪，则返回，否则阻塞直到超时。得到就绪状态后进行真正的操作可以在同一个线程里执行，也可以启动线程执行（比如使用线程池）。</p>
<h2 id="参考-Reference"><a href="#参考-Reference" class="headerlink" title="参考 Reference"></a>参考 Reference</h2><p>参考资料：《Redis开发与运维》</p>
<p>视频教程：<a href="https://www.bilibili.com/video/BV1Rv41177Af">https://www.bilibili.com/video/BV1Rv41177Af</a></p>
<p>参考笔记：<a href="https://zhangc233.github.io/2021/05/02/Redis/">https://zhangc233.github.io/2021/05/02/Redis/</a></p>
<p>参考笔记：<a href="https://blog.csdn.net/weixin_47872288/article/details/118410080">https://blog.csdn.net/weixin_47872288/article/details/118410080</a></p>
]]></content>
      <categories>
        <category>REDIS</category>
      </categories>
      <tags>
        <tag>REDIS</tag>
        <tag>后端</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis02-常用五大数据类型</title>
    <url>/2022/02/02/Redis02-%E4%BA%94%E5%A4%A7%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B/</url>
    <content><![CDATA[<h2 id="3-常用五大数据类型"><a href="#3-常用五大数据类型" class="headerlink" title="3.常用五大数据类型"></a>3.常用五大数据类型</h2><p>Redis常见数据类型操作命令：<a href="http://redis.cn/commands.html">http://redis.cn/commands.html</a></p>
<h3 id="3-1-键-Key"><a href="#3-1-键-Key" class="headerlink" title="3.1 键 Key"></a>3.1 键 Key</h3><p>Key值操作：</p>
<pre><code>keys * 查看当前库所有key

set key value 设置key值与value

exists key 判断key是否存在

type key 查看key是什么类型

del key 删除指定的key数据

unlink key 根据value选择非阻塞删除
------仅将keys从keyspace元数据中删除，真正的删除会在后续异步操作。

expire key 10 10秒钟：为给定的key设置过期时间

ttl key 查看还有多少秒过期，-1表示永不过期，-2表示已过期
</code></pre>
<h3 id="3-2字符串-String"><a href="#3-2字符串-String" class="headerlink" title="3.2字符串 String"></a>3.2字符串 String</h3><blockquote>
<p>一个Redis字符串value最多可以是512M</p>
</blockquote>
<blockquote>
<p>原子操作，不会被打断，从开始到结束（单线程不会被打断）</p>
</blockquote>
<h4 id="基本操作"><a href="#基本操作" class="headerlink" title="基本操作"></a>基本操作</h4><pre><code>set key value 设置key值

get key 查询key值

append key value 将给定的value追加到原值末尾

strlen key 获取值的长度

setnx key value 只有在key不存在的时候，设置key值

incr key 将key值存储的数字增1，只对数字值操作，如果为空，结果为1
decr key 将key值存储的数字减1，只对数字值操作，如果为空，结果为-1

incrby/decrby key &lt;步长&gt; 将key值存储的数字增减如步长
</code></pre>
<h4 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h4><pre><code>mset key value key value..同时设置一个或者多个key-value
mget key key...同时获取一个或多个value
msetnx key value key value..同时设置一个或者多个key-value.当且仅当所有给定key都不存在
getrange key &lt;起始位置&gt; &lt;结束位置&gt; 获取key的起始位置和结束位置的值(起始0)
setrange key &lt;起始位置&gt; value 将value的值覆盖起始位置开始
setex key &lt;&gt; value 设置键值的同时,设置过期时间
getset key value 用新值换旧值
</code></pre>
<h4 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h4><p>String 的数据结构为简单动态字符串 (Simple Dynamic String, 缩写 SDS)，是可以修改的字符串，内部结构实现上类似于 Java 的 ArrayList，采用预分配冗余空间的方式来减少内存的频繁分配。</p>
<p>当前字符串实际分配的空间 capacity 一般要高于实际字符串长度 len。当字符串长度小于 1M 时，扩容都是加倍现有的空间，如果超过 1M，扩容时一次只会多扩 1M 的空间。需要注意的是字符串最大长度为 512M。</p>
<h3 id="3-3-列表-List"><a href="#3-3-列表-List" class="headerlink" title="3.3 列表 List"></a>3.3 列表 List</h3><h4 id="单键多值"><a href="#单键多值" class="headerlink" title="单键多值"></a>单键多值</h4><ul>
<li>Redis列表是简单的字符串列表，按照插入顺序排序，可以添加元素到列表的头(左)尾(右)。</li>
<li>Redis列表底层是双向链表，对两端的操作性能很高，通过索引下标操作中间节点性能较差。</li>
</ul>
<h4 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h4><pre><code>lpush/rpush key value value...从左或者右插入一个或者多个值(头插与尾插)

lpop/rpop key 从左或者右吐出一个或者多个值(值在键在,值都没,键都没)

rpoplpush key1 key2 从key1列表右边吐出一个值,插入到key2的左边

lrange key start stop 按照索引下标获取元素(从左到右)

lrange key 0 -1 获取所有值

lindex key index 按照索引下标获得元素

llen key 获取列表长度

linsert key before/after value newvalue 在value的前面插入一个新值

lrem key n value 从左边删除n个value值

lset key index value 在列表key中的下标index中修改值value
</code></pre>
<h4 id="数据结构-1"><a href="#数据结构-1" class="headerlink" title="数据结构"></a>数据结构</h4><p><img src="https://cdn.jsdelivr.net/gh/zhangc233/pic@master/img/image-20210618164459005.png" alt="image-20210618164459005"></p>
<ul>
<li>List 的数据结构为快速链表 quickList。</li>
<li>首先在列表元素较少的情况下会使用一块连续的内存存储，这个结构是 ziplist，也即是压缩列表。它将所有的元素紧挨着一起存储，分配的是一块连续的内存。</li>
<li>当数据量比较多的时候才会改成 quicklist。因为普通的链表需要的附加指针空间太大，会比较浪费空间。比如这个列表里存 的只是 int 类型的数据，结构上还需要两个额外的指针 prev 和 next。</li>
<li>Redis 将链表和 ziplist 结合起来组成了 quicklist。也就是将多个 ziplist 使用双向指针串起来使用。这样既满足了快速的插入删除性能，又不会出现太大的空间冗余。</li>
</ul>
<h3 id="3-4-集合-Set"><a href="#3-4-集合-Set" class="headerlink" title="3.4 集合 Set"></a>3.4 集合 Set</h3><ul>
<li>Set是自动排重的列表</li>
<li>Redis的Set是string类型的无序集合，底层是value为null的hash表，所以添加，删除，查找的复杂度都是O(1)</li>
<li><strong>O(1)</strong> : 常量级的时间复杂度,并不是说只执行了一条语句,只要执行语句的数量是常量,都可以用它来表示。</li>
</ul>
<h4 id="常用命令-1"><a href="#常用命令-1" class="headerlink" title="常用命令"></a>常用命令</h4><pre><code>sadd key value value... 将一个或者多个member元素加入集合key中,已经存在的member元素被忽略

smembers key 取出该集合的所有值

sismember key value 判断该集合key是否含有该值

scard key 返回该集合的元素个数

srem key value value 删除集合中的某个元素

spop key 随机从集合中取出一个元素

srandmember key n 随即从该集合中取出n个值，不会从集合中删除

smove &lt;一个集合a&gt;&lt;一个集合b&gt;value 将一个集合a的某个value移动到另一个集合b

sinter key1 key2 返回两个集合的交集元素

sunion key1 key2 返回两个集合的并集元素

sdiff key1 key2 返回两个集合的差集元素（key1有的，key2没有）
</code></pre>
<h4 id="数据结构-2"><a href="#数据结构-2" class="headerlink" title="数据结构"></a>数据结构</h4><ul>
<li>Set 数据结构是 dict 字典，字典是用哈希表实现的。</li>
<li>Java 中 HashSet 的内部实现使用的是 HashMap，只不过所有的 value 都指向同一个对象。Redis 的 set 结构也是一样，它的内部也使用 hash 结构，所有的 value 都指向同一个内部值。</li>
</ul>
<h3 id="3-5-哈希-Hash"><a href="#3-5-哈希-Hash" class="headerlink" title="3.5 哈希 Hash"></a>3.5 哈希 Hash</h3><ul>
<li><p>Redis hash 是一个键值对集合。</p>
</li>
<li><p>Redis hash 是一个 string 类型的 field 和 value 的映射表，hash 特别适合用于存储对象。</p>
</li>
<li><p>类似 Java 里面的 Map&lt;String,Object&gt;。</p>
</li>
<li><p>通过 key (用户 ID) + field (属性标签) 就可以操作对应属性数据了，既不需要重复存储数据，也不会带来序列化和并发修改控制的问题。</p>
<p><img src="https://cdn.jsdelivr.net/gh/zhangc233/pic@master/img/image-20210618203718979.png" alt="image-20210618203718979"></p>
</li>
</ul>
<h4 id="常用命令-2"><a href="#常用命令-2" class="headerlink" title="常用命令"></a>常用命令</h4><pre><code>hset key field value 给key集合中的filed键赋值value

hget key1 field 集合field取出value

hmset key1 field1 value1 field2 value2 批量设置hash的值

hexists key1 field 查看哈希表key中，给定域field是否存在

hkeys key 列出该hash集合的所有field

hvals key 列出该hash集合的所有value

hincrby key field increment 为哈希表key中的域field的值加上增量1 -1

hsetnx key field value 将哈希表key中的域field的值设置为value，当且仅当域field不存在
</code></pre>
<p>示例</p>
<pre><code>127.0.0.1:6379&gt; hset user:1001 id 1
(integer) 1
127.0.0.1:6379&gt; hset user:1002 id 2
(integer) 1
127.0.0.1:6379&gt; hset user:1001 name AAA
(integer) 1
127.0.0.1:6379&gt; hset user:1001 name AAA age 20
(integer) 1
127.0.0.1:6379&gt; hkeys user:1001
1) &quot;id&quot;
2) &quot;name&quot;
3) &quot;age&quot;
127.0.0.1:6379&gt; hkeys user:1002
1) &quot;id&quot;
127.0.0.1:6379&gt; hexists user:1001 id
(integer) 1
127.0.0.1:6379&gt; hvals user:1001
1) &quot;1&quot;
2) &quot;AAA&quot;
3) &quot;20&quot;
127.0.0.1:6379&gt; hincrby user:1001 age 2
(integer) 22
127.0.0.1:6379&gt; hsetnx user:1002 age 18
(integer) 1
127.0.0.1:6379&gt; hkeys user:1002
1) &quot;id&quot;
2) &quot;age&quot;
</code></pre>
<h4 id="数据结构-3"><a href="#数据结构-3" class="headerlink" title="数据结构"></a>数据结构</h4><p>Hash 类型对应的数据结构是两种：ziplist（压缩列表），hashtable（哈希表）。当 field-value 长度较短且个数较少时，使用 ziplist，否则使用 hashtable。</p>
<h3 id="3-6-有序集合-Zset-Sorted-Set"><a href="#3-6-有序集合-Zset-Sorted-Set" class="headerlink" title="3.6 有序集合 Zset (Sorted Set)"></a>3.6 有序集合 Zset (Sorted Set)</h3><ul>
<li>Redis 有序集合 zset 与普通集合 set 非常相似，是一个没有重复元素的字符串集合。</li>
<li>不同之处是有序集合的每个成员都关联了一个<strong>评分（</strong>score），这个评分（score）被用来按照从最低分到最高分的方式排序集合中的成员。集合的成员是唯一的，但是评分可以是重复了 。</li>
<li>因为元素是有序的，所以你也可以很快的根据评分（score）或者次序（position）来获取一个范围的元素。</li>
<li>访问有序集合的中间元素也是非常快的，因此你能够使用有序集合作为一个没有重复成员的智能列表。</li>
</ul>
<h4 id="常用命令-3"><a href="#常用命令-3" class="headerlink" title="常用命令"></a>常用命令</h4><pre><code>zadd key score1 value1 score2 value2 将一个或多个member元素及其score值加入到有序key中

zrange key start stop (withscores) 返回有序集key，下标在start与stop之间的元素，带withscores，可以让分数一起和值返回到结果集。

zrangebyscore key min max(withscores) 返回有序集key，所有score值介于min和max之间（包括等于min或max）的成员。有序集成员按score的值递增次序排列

zrevrangebyscore key max min （withscores）同上，改为从大到小排列

zincrby key increment value 为元素的score加上增量

zrem key value 删除该集合下，指定值的元素

zcard key 返回集合中元素个数

zcount key min max 统计该集合，分数区间内的元素个数

zrank key value 返回该值在集合中的排名，从0开始
</code></pre>
<h4 id="数据结构-4"><a href="#数据结构-4" class="headerlink" title="数据结构"></a>数据结构</h4><p>zset 底层使用了两个数据结构：</p>
<ol>
<li>hash，hash 的作用就是关联元素 value 和权重 score，保障元素 value 的唯一性，可以通过元素 value 找到相应的 score 值。</li>
<li>跳跃表(skiplist)，跳跃表的目的在于给元素 value 排序，根据 score 的范围获取元素列表。</li>
</ol>
<h2 id="参考-Reference"><a href="#参考-Reference" class="headerlink" title="参考 Reference"></a>参考 Reference</h2><p>参考资料：《Redis开发与运维》</p>
<p>视频教程：<a href="https://www.bilibili.com/video/BV1Rv41177Af">https://www.bilibili.com/video/BV1Rv41177Af</a></p>
<p>参考笔记：<a href="https://zhangc233.github.io/2021/05/02/Redis/">https://zhangc233.github.io/2021/05/02/Redis/</a></p>
<p>参考笔记：<a href="https://blog.csdn.net/weixin_47872288/article/details/118410080">https://blog.csdn.net/weixin_47872288/article/details/118410080</a></p>
]]></content>
      <categories>
        <category>REDIS</category>
      </categories>
      <tags>
        <tag>REDIS</tag>
        <tag>后端</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis03-配置文件、发布和订阅</title>
    <url>/2022/02/03/Redis03-%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6%E3%80%81%E5%8F%91%E5%B8%83%E5%92%8C%E8%AE%A2%E9%98%85/</url>
    <content><![CDATA[<h2 id="4-配置文件"><a href="#4-配置文件" class="headerlink" title="4.配置文件"></a>4.配置文件</h2><p>官网下载：<a href="http://download.redis.io/redis-stable/redis.conf">http://download.redis.io/redis-stable/redis.conf</a></p>
<h4 id="Units-单位"><a href="#Units-单位" class="headerlink" title="Units 单位"></a>Units 单位</h4><h5 id="配置大小单位，只支持bytes，不支持bit"><a href="#配置大小单位，只支持bytes，不支持bit" class="headerlink" title="配置大小单位，只支持bytes，不支持bit"></a>配置大小单位，只支持bytes，不支持bit</h5><p>大小写不敏感</p>
<h4 id="INCLUDES-包含"><a href="#INCLUDES-包含" class="headerlink" title="INCLUDES 包含"></a>INCLUDES 包含</h4><h5 id="提取公共配置文件"><a href="#提取公共配置文件" class="headerlink" title="提取公共配置文件"></a>提取公共配置文件</h5><p>####NETWORK 网络</p>
<h5 id="配置支持原创连接"><a href="#配置支持原创连接" class="headerlink" title="配置支持原创连接"></a>配置支持原创连接</h5><pre><code>#bind 127.0.0.1 -::1 //注释绑定本地连接
protected-mode no         //关闭保护模式
</code></pre>
<p>端口号：默认6379</p>
<pre><code>port 6379
</code></pre>
<h4 id="GENERAL-通用"><a href="#GENERAL-通用" class="headerlink" title="GENERAL 通用"></a>GENERAL 通用</h4><h5 id="开启后台启动"><a href="#开启后台启动" class="headerlink" title="开启后台启动"></a>开启后台启动</h5><pre><code>daemonize yes
</code></pre>
<h5 id="进程号的路径"><a href="#进程号的路径" class="headerlink" title="进程号的路径"></a>进程号的路径</h5><pre><code>pidfile /var/run/redis_6379.pid
</code></pre>
<h5 id="日志级别"><a href="#日志级别" class="headerlink" title="日志级别"></a>日志级别</h5><pre><code>loglevel notice
</code></pre>
<h5 id="日志输出路径"><a href="#日志输出路径" class="headerlink" title="日志输出路径"></a>日志输出路径</h5><pre><code>logfile &quot;&quot;
</code></pre>
<h5 id="数据库数数量"><a href="#数据库数数量" class="headerlink" title="数据库数数量"></a>数据库数数量</h5><pre><code>databases 16
</code></pre>
<h4 id="SECURITY-安全"><a href="#SECURITY-安全" class="headerlink" title="SECURITY 安全"></a>SECURITY 安全</h4><h5 id="开启访问密码"><a href="#开启访问密码" class="headerlink" title="开启访问密码"></a>开启访问密码</h5><pre><code>requirepass foobared
</code></pre>
<h4 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h4><h5 id="最大客户端连接数"><a href="#最大客户端连接数" class="headerlink" title="最大客户端连接数"></a>最大客户端连接数</h5><pre><code>maxclients 10000
</code></pre>
<h5 id="设置最大内存"><a href="#设置最大内存" class="headerlink" title="设置最大内存"></a>设置最大内存</h5><pre><code>maxmemory &lt;bytes&gt;
</code></pre>
<h5 id="内存超出上限：按规则移除"><a href="#内存超出上限：按规则移除" class="headerlink" title="内存超出上限：按规则移除"></a>内存超出上限：按规则移除</h5><pre><code>maxmemory-policy noeviction
</code></pre>
<p>注：如果redis无法按规则移除，或者设置了“不允许移除”，则：</p>
<ul>
<li><p>需要申请内存的指令(SET/LPUSH等)会返回错误信息</p>
</li>
<li><p>无内存申请的指令(GET等)仍会正常响应</p>
</li>
</ul>
<h5 id="样本数量"><a href="#样本数量" class="headerlink" title="样本数量"></a>样本数量</h5><pre><code>maxmemory-samples 5
</code></pre>
<p>注：数值越小越不准确，但性能消耗较小</p>
<h5 id="内存移除规则"><a href="#内存移除规则" class="headerlink" title="内存移除规则"></a>内存移除规则</h5><pre><code># volatile-lru -&gt; 使用LRU算法移除key，只对设置了过期时间的键(少用)
# allkeys-lru -&gt; 在所有集合key中，使用LRU算法移除key
# volatile-lfu -&gt; LFU算法
# allkeys-lfu -&gt; LFU算法
# volatile-random -&gt; 在过期集合中移除随机的key，只对设置了过期时间的键
# allkeys-random -&gt; 在所有集合中移除随机的key，只对设置了过期时间的键
# volatile-ttl -&gt; 移除TTL最小(快要过期)的key
# noeviction -&gt; 不进行移除
</code></pre>
<h5 id="Redis淘汰算法"><a href="#Redis淘汰算法" class="headerlink" title="Redis淘汰算法"></a>Redis淘汰算法</h5><ul>
<li>随机</li>
<li>TTL 过期时间</li>
<li>LRU（Least Recently Used，最近最少使用）</li>
<li>LFU（Least Frequently Used，最不经常使用）</li>
</ul>
<h2 id="5-发布和订阅"><a href="#5-发布和订阅" class="headerlink" title="5.发布和订阅"></a>5.发布和订阅</h2><blockquote>
<p>Redis发布订阅(pub/sub)是一种消息通信模式：发送者(pub)发送消息，订阅者(sub)接收消息</p>
</blockquote>
<blockquote>
<p>Redis客户端可以订阅任意数量的频道</p>
</blockquote>
<h5 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h5><p>客户端1：</p>
<pre><code>127.0.0.1:6379&gt; publish channel1 hello
(integer) 1
</code></pre>
<p>客户端2：</p>
<pre><code>127.0.0.1:6379&gt; subscribe channel1
Reading messages... (press Ctrl-C to quit)
1) &quot;subscribe&quot;
2) &quot;channel1&quot;
3) (integer) 1
1) &quot;message&quot;
2) &quot;channel1&quot;
3) &quot;hello&quot;
</code></pre>
<p>注：多个客户端订阅相同频道，都可以收到消息</p>
<h2 id="参考-Reference"><a href="#参考-Reference" class="headerlink" title="参考 Reference"></a>参考 Reference</h2><p>参考资料：《Redis开发与运维》</p>
<p>视频教程：<a href="https://www.bilibili.com/video/BV1Rv41177Af">https://www.bilibili.com/video/BV1Rv41177Af</a></p>
<p>参考笔记：<a href="https://zhangc233.github.io/2021/05/02/Redis/">https://zhangc233.github.io/2021/05/02/Redis/</a></p>
<p>参考笔记：<a href="https://blog.csdn.net/weixin_47872288/article/details/118410080">https://blog.csdn.net/weixin_47872288/article/details/118410080</a></p>
]]></content>
      <categories>
        <category>REDIS</category>
      </categories>
      <tags>
        <tag>REDIS</tag>
        <tag>后端</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis04-新数据类型</title>
    <url>/2022/02/04/Redis04-%E6%96%B0%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B/</url>
    <content><![CDATA[<h2 id="6-新数据类型"><a href="#6-新数据类型" class="headerlink" title="6.新数据类型"></a>6.新数据类型</h2><h3 id="Bitmaps-位操作字符串"><a href="#Bitmaps-位操作字符串" class="headerlink" title="Bitmaps 位操作字符串"></a>Bitmaps 位操作字符串</h3><ul>
<li>Bitmaps实际上就是字符串(k-v)，但是可以对字符串进行位操作</li>
<li>可以把Bitmas想象成以位为单位，只能存0和1的数组，数组下标叫做偏移量</li>
</ul>
<h4 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h4><pre><code>setbit key offset value  //设置值

getbit key offset              //取值

bitcount （start end     //统计数值

bitop and/or/not/xor destkey key1 key2  //复合操作，交并非异或，结果保存在destkey
</code></pre>
<h4 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h4><pre><code>127.0.0.1:6379&gt; setbit k1 1 1
(integer) 0
127.0.0.1:6379&gt; setbit k1 2 1
(integer) 0
127.0.0.1:6379&gt; setbit k1 4 1
(integer) 0
127.0.0.1:6379&gt; getbit k1 1
(integer) 1
127.0.0.1:6379&gt; getbit k1 3
(integer) 0
127.0.0.1:6379&gt; bitcount k1
(integer) 3
127.0.0.1:6379&gt; setbit k2 3 1
(integer) 0
127.0.0.1:6379&gt; bitop and k1_and_k2 k1 k2 //k1和k2都有值 
(integer) 1
127.0.0.1:6379&gt; bitcount k1_and_k2
(integer) 0
127.0.0.1:6379&gt; bitop or k1_or_k2 k1 k2 //k1或k2有值 
(integer) 1
127.0.0.1:6379&gt; bitcount k1_or_k2
(integer) 4
</code></pre>
<h4 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h4><p>但 Bitmaps 并不是万金油， 假如该网站每天的独立访问用户很少， 例如只有 10 万（大量的僵尸用户） ， 那么两者的对比如下表所示， 很显然， 这时候使用 Bitmaps 就不太合适了， 因为基本上大部分位都是 0。</p>
<p>Bitmaps适合百万级以上数据量存储。</p>
<h3 id="HyperLogLog"><a href="#HyperLogLog" class="headerlink" title="HyperLogLog"></a>HyperLogLog</h3><h4 id="基数问题"><a href="#基数问题" class="headerlink" title="基数问题"></a>基数问题</h4><blockquote>
<p>求集合中不重复元素个数的问题称为基数问题</p>
</blockquote>
<p>解决方案：</p>
<ul>
<li>Mysql使用distinct count计算</li>
<li>Redis使用hash，set，bitmaos等数据结构处理</li>
</ul>
<h4 id="基数统计"><a href="#基数统计" class="headerlink" title="基数统计"></a>基数统计</h4><p>基数统计就是在误差可接受的范围内，快速计算基数。</p>
<ul>
<li>Redis HyperLogLog是用来做基数统计的算法，优点是输入大量数据时，所需空间固定(很小)。</li>
<li>在 Redis 里面，每个 HyperLogLog 键只需要花费 12 KB 内存，就可以计算接近 2^64 个不同元素的基数。</li>
<li>HyperLogLog 只会根据输入元素来计算基数，而不会储存输入元素本身，所以 HyperLogLog 不能像集合那样，返回输入的各个元素。</li>
</ul>
<h4 id="常用操作"><a href="#常用操作" class="headerlink" title="常用操作"></a>常用操作</h4><pre><code>pfadd key element //添加元素 基数变化返回1，不变返回0

pfcount key                //统计基数

pfmerge destkey key1 key2  //合并
</code></pre>
<h3 id="Geospatial-经纬度"><a href="#Geospatial-经纬度" class="headerlink" title="Geospatial 经纬度"></a>Geospatial 经纬度</h3><p>Redis3.2新增，提供经纬度设置，查询范围，距离查询等操作</p>
<blockquote>
<p>求集合中不重复元素个数的问题称为基数问题</p>
</blockquote>
<p>解决方案：</p>
<ul>
<li>Mysql使用distinct count计算</li>
<li>Redis使用hash，set，bitmaos等数据结构处理</li>
</ul>
<h4 id="常用操作-1"><a href="#常用操作-1" class="headerlink" title="常用操作"></a>常用操作</h4><pre><code>geoadd key longitude latitude member //设置经纬度

geopos key member                        //获取经纬度

geodist key member1 member2 (m km ft mi) //计算两个位置之间的直线距离

georadius key longitude latitude radius (m km ft mi) //以给定的经纬度为中心，找出某一半径的内元素
</code></pre>
<h2 id="参考-Reference"><a href="#参考-Reference" class="headerlink" title="参考 Reference"></a>参考 Reference</h2><p>参考资料：《Redis开发与运维》</p>
<p>视频教程：<a href="https://www.bilibili.com/video/BV1Rv41177Af">https://www.bilibili.com/video/BV1Rv41177Af</a></p>
<p>参考笔记：<a href="https://zhangc233.github.io/2021/05/02/Redis/">https://zhangc233.github.io/2021/05/02/Redis/</a></p>
<p>参考笔记：<a href="https://blog.csdn.net/weixin_47872288/article/details/118410080">https://blog.csdn.net/weixin_47872288/article/details/118410080</a></p>
]]></content>
      <categories>
        <category>REDIS</category>
      </categories>
      <tags>
        <tag>REDIS</tag>
        <tag>后端</tag>
      </tags>
  </entry>
  <entry>
    <title>Mysql45讲01-基础架构</title>
    <url>/2021/06/01/Mysql45%E8%AE%B201-%E5%9F%BA%E7%A1%80%E6%9E%B6%E6%9E%84/</url>
    <content><![CDATA[<h2 id="01基础架构"><a href="#01基础架构" class="headerlink" title="01基础架构"></a>01基础架构</h2><h3 id="逻辑架构"><a href="#逻辑架构" class="headerlink" title="逻辑架构"></a>逻辑架构</h3><h4 id="Server层"><a href="#Server层" class="headerlink" title="Server层"></a>Server层</h4><ul>
<li>包括连接器，查询缓存，分析器，优化器，执行器等</li>
<li>涵盖Mysql大多数核心服务功能，和所有内置函数（日期，时间，数学，加密等）</li>
<li>实现所有跨存储引擎的功能（存储过程，触发器，视图等）</li>
</ul>
<h4 id="存储引擎层"><a href="#存储引擎层" class="headerlink" title="存储引擎层"></a>存储引擎层</h4><ul>
<li>负责数据的存储和提取</li>
<li>架构模式是插件式的，从Mysql5.5.5开始InnoDB成为默认存储引擎</li>
</ul>
<h3 id="1-连接器"><a href="#1-连接器" class="headerlink" title="1.连接器"></a>1.连接器</h3><h5 id="连接Mysql"><a href="#连接Mysql" class="headerlink" title="连接Mysql"></a>连接Mysql</h5><pre><code>mysql -h$ip -P$port -u$user -p
</code></pre>
<p>一个用户成功建立连接后，即使你用管理员账号对这个用户的权限做了修改，也不会影响已经存在连接的权限。修改完成后，只有再新建的连接才会使用新的权限设置。</p>
<h5 id="空闲连接"><a href="#空闲连接" class="headerlink" title="空闲连接"></a>空闲连接</h5><blockquote>
<p>连接完成后，如果你没有后续的动作，这个连接就处于空闲状态</p>
</blockquote>
<pre><code>show processlist
</code></pre>
<ul>
<li>Command 列显示为“Sleep”的这一行，就表示现在系统里面有一个空闲连接。</li>
<li>客户端如果太长时间没动静，连接器就会自动将它断开。这个时间是由参数 wait_timeout 控制的，默认值是 8 小时。</li>
</ul>
<h5 id="短连接"><a href="#短连接" class="headerlink" title="短连接"></a>短连接</h5><blockquote>
<p>短连接则是指每次执行完很少的几次查询就断开连接，下次查询再重新建立一个。</p>
</blockquote>
<h5 id="长连接-推荐"><a href="#长连接-推荐" class="headerlink" title="长连接(推荐)"></a>长连接(推荐)</h5><blockquote>
<p>长连接是指连接成功后，如果客户端持续有请求，则一直使用同一个连接。</p>
</blockquote>
<p>MySQL 在执行过程中临时使用的内存是管理在连接对象里面的。这些资源会在连接断开的时候才释放。如果长连接累积下来，可能导致内存占用太大，被系统强行杀掉（OOM），从现象看就是 MySQL 异常重启了。</p>
<p>解决办法：</p>
<ol>
<li>定期断开长连接。使用一段时间，或者程序里面判断执行过一个占用内存的大查询后，断开连接，之后要查询再重连。</li>
<li>MySQL 5.7 或更新版本，可以在每次执行一个比较大的操作后，通过执行 mysql_reset_connection 来重新初始化连接资源。这个过程不需要重连和重新做权限验证，但是会将连接恢复到刚刚创建完时的状态。</li>
</ol>
<h3 id="2-查询缓存（Mysql8-0已删除）"><a href="#2-查询缓存（Mysql8-0已删除）" class="headerlink" title="2.查询缓存（Mysql8.0已删除）"></a>2.查询缓存（Mysql8.0已删除）</h3><ul>
<li>MySQL拿到一个查询请求后，会先到查询缓存看看，之前是不是执行过这条语句。</li>
<li>MySQL之前执行过的语句及其结果可能会以 key-value 对的形式，被直接缓存在内存中。</li>
<li>查询缓存的失效非常频繁，只要有对一个表的更新，这个表上所有的查询缓存都会被清空。</li>
<li>可以将参数 query_cache_type 设置成 DEMAND，这样对于默认的 SQL 语句都不使用查询缓存。</li>
</ul>
<p>强制查询缓存语句</p>
<pre><code>select SQL_CACHE * from T where ID=10；
</code></pre>
<h3 id="3-分析器"><a href="#3-分析器" class="headerlink" title="3.分析器"></a>3.分析器</h3><blockquote>
<p>分析器先会做“词法分析”。你输入的是由多个字符串和空格组成的一条 SQL 语句，MySQL 需要识别出里面的字符串分别是什么，代表什么。</p>
</blockquote>
<p>如果没有命中查询缓存，就要开始真正执行语句了。</p>
<p>语法错误提醒：ERROR 1064 (42000): You have an error in your SQL syntax;</p>
<h3 id="4-优化器"><a href="#4-优化器" class="headerlink" title="4.优化器"></a>4.优化器</h3><blockquote>
<p>优化器是在表里面有多个索引的时候，决定使用哪个索引；或者在一个语句有多表关联（join）的时候，决定各个表的连接顺序。</p>
</blockquote>
<h3 id="5-执行器"><a href="#5-执行器" class="headerlink" title="5.执行器"></a>5.执行器</h3><ul>
<li>开始执行的时候，要先判断一下有没有执行权限，如果没有，就会返回没有权限的错误</li>
<li>如果有权限，就继续执行，执行器会根据表的引擎定义，去使用这个引擎提供的接口。</li>
</ul>
<p>示例：</p>
<pre><code>mysql&gt; select * from T where ID=10;
</code></pre>
<p>执行流程：</p>
<ol>
<li>调用 InnoDB 引擎接口取这个表的第一行，判断 ID 值是不是 10，如果不是则跳过，如果是则将这行存在结果集中；</li>
<li>调用引擎接口取“下一行”，重复相同的判断逻辑，直到取到这个表的最后一行。</li>
<li>执行器将上述遍历过程中所有满足条件的行组成的记录集作为结果集返回给客户端。</li>
</ol>
<p>数据库的慢查询日志中有一个<code>rows_examined</code>字段，表示这个语句执行过程中扫描了多少行。这个值就是在执行器每次调用引擎获取数据行的时候累加的。</p>
<h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>问题：如果表 T 中没有字段 k，而你执行了这个语句 select * from T where k=1, 那肯定是会报“不存在这个列”的错误： “Unknown column ‘k’ in ‘where clause’”。你觉得这个错误是在我们上面提到的哪个阶段报出来的呢？<br>答案：分析器。Oracle会在分析阶段判断语句是否正确，表是否存在，列是否存在等。<br>其他答案：预处理器。<br>解析器处理语法和解析查询, 生成一课对应的解析树。<br>预处理器进一步检查解析树的合法。比如: 数据表和数据列是否存在, 别名是否有歧义等。如果通过则生成新的解析树，再提交给优化器。</p>
]]></content>
      <categories>
        <category>MYSQL</category>
        <category>Mysql45讲</category>
      </categories>
      <tags>
        <tag>后端</tag>
        <tag>MYSQL</tag>
      </tags>
  </entry>
  <entry>
    <title>单词本</title>
    <url>/2022/01/01/%E5%8D%95%E8%AF%8D%E6%9C%AC/</url>
    <content><![CDATA[<hr>

<table>
<thead>
<tr>
<th>单词</th>
<th>翻译</th>
<th>原文</th>
<th>翻译</th>
</tr>
</thead>
<tbody><tr>
<td>reliable</td>
<td>可信赖的&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</td>
<td>Build fast, reliable, and efficient software at scale 大规模构建快速、可靠、高效的软件</td>
<td><a href="https://go.dev/">go.dev</a></td>
</tr>
<tr>
<td>scale</td>
<td>规模</td>
<td></td>
<td></td>
</tr>
<tr>
<td>concurrency</td>
<td>并发性</td>
<td>Built-in concurrency and a robust standard library 内置并发性和强大的标准库</td>
<td></td>
</tr>
<tr>
<td>robust</td>
<td>强大的</td>
<td></td>
<td></td>
</tr>
<tr>
<td>ecosystem</td>
<td>生态环境</td>
<td>Growing ecosystem of partners, communities, and tools 不断增长的合作伙伴、社区和工具生态系统</td>
<td></td>
</tr>
<tr>
<td>communities</td>
<td>社区</td>
<td>community 社区</td>
<td></td>
</tr>
<tr>
<td>organization</td>
<td>组织机构</td>
<td></td>
<td></td>
</tr>
<tr>
<td>flexibility</td>
<td>灵活性</td>
<td></td>
<td></td>
</tr>
<tr>
<td>concept</td>
<td>概念</td>
<td></td>
<td></td>
</tr>
<tr>
<td>duplicate</td>
<td>重复</td>
<td></td>
<td></td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>单词本</category>
      </categories>
      <tags>
        <tag>单词本</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis05-Jedis、SprintBoot整合Redis</title>
    <url>/2022/02/05/Redis05-Jedis%E3%80%81SprintBoot%E6%95%B4%E5%90%88Redis/</url>
    <content><![CDATA[<h2 id="7-Jedis"><a href="#7-Jedis" class="headerlink" title="7.Jedis"></a>7.Jedis</h2><h3 id="基本操作"><a href="#基本操作" class="headerlink" title="基本操作"></a>基本操作</h3><h4 id="IDEA新建工程"><a href="#IDEA新建工程" class="headerlink" title="IDEA新建工程"></a>IDEA新建工程</h4><p>New Project  &gt;  Maven  &gt;  Next  &gt;  配置项目名 &gt; Finish</p>
<pre><code>GroupId: cn.itxiaoma
ArtifactId: jedis_redis_demo
</code></pre>
<h4 id="添加依赖"><a href="#添加依赖" class="headerlink" title="添加依赖"></a>添加依赖</h4><ol>
<li><p>Jedis 所需要的jar包到pom.xml</p>
<pre><code>&lt;!-- https://mvnrepository.com/artifact/redis.clients/jedis --&gt;
    &lt;dependencies&gt;
        &lt;dependency&gt;
            &lt;groupId&gt;redis.clients&lt;/groupId&gt;
            &lt;artifactId&gt;jedis&lt;/artifactId&gt;
            &lt;version&gt;3.6.1&lt;/version&gt;
        &lt;/dependency&gt;
    &lt;/dependencies&gt;
</code></pre>
</li>
<li><p>刷新Maven</p>
</li>
</ol>
<h4 id="新建测试类"><a href="#新建测试类" class="headerlink" title="新建测试类"></a>新建测试类</h4><p>New package:  cn.itxiaoma.jedis</p>
<p>New Java Class:  JedisDemo1.java</p>
<pre><code>package cn.itxiaoma.jedis;

import redis.clients.jedis.Jedis;

public class JedisDemo1 &#123;
    public static void main(String[] args) &#123;
        //创建Jedis对象
        Jedis jedis = new Jedis(&quot;192.168.31.51&quot;, 6379);
        //测试
        String res = jedis.ping();
        System.out.println(res);
    &#125;
&#125;
</code></pre>
<h4 id="测试相关数据类型"><a href="#测试相关数据类型" class="headerlink" title="测试相关数据类型"></a>测试相关数据类型</h4><pre><code>package cn.itxiaoma.jedis;

import org.junit.jupiter.api.Test;
import redis.clients.jedis.Jedis;

import java.util.List;
import java.util.Set;

public class JedisDemo1 &#123;
    public static void main(String[] args) &#123;
        //创建Jedis对象
        Jedis jedis = new Jedis(&quot;192.168.31.51&quot;, 6379);
        //测试
        String res = jedis.ping();
        System.out.println(res);
    &#125;

    //操作key string
    @Test
    public void demo1() &#123;
        //创建Jedis对象
        Jedis jedis = new Jedis(&quot;192.168.31.51&quot;, 6379);

        //添加
        jedis.set(&quot;name&quot;, &quot;lucy&quot;);

        //获取
        String name = jedis.get(&quot;name&quot;);
        System.out.println(name);

        //设置多个key-value
        jedis.mset(&quot;k1&quot;, &quot;v1&quot;, &quot;k2&quot;, &quot;v2&quot;);
        List&lt;String&gt; mget = jedis.mget(&quot;k1&quot;, &quot;k2&quot;);
        System.out.println(mget);

        Set&lt;String&gt; keys = jedis.keys(&quot;*&quot;);
        for (String key : keys) &#123;
            System.out.println(key);
        &#125;
        jedis.close();
    &#125;

    //操作list
    @Test
    public void demo2() &#123;
        //创建Jedis对象
        Jedis jedis = new Jedis(&quot;192.168.31.51&quot;, 6379);

        jedis.lpush(&quot;key1&quot;, &quot;lucy&quot;, &quot;mary&quot;, &quot;jack&quot;);
        List&lt;String&gt; values = jedis.lrange(&quot;key1&quot;, 0, -1);
        System.out.println(values);
        jedis.close();
    &#125;

    //操作set
    @Test
    public void demo3() &#123;
        //创建Jedis对象
        Jedis jedis = new Jedis(&quot;192.168.31.51&quot;, 6379);

        jedis.sadd(&quot;names&quot;,&quot;lucy&quot;);
        jedis.sadd(&quot;names&quot;,&quot;mary&quot;);

        Set&lt;String&gt; names = jedis.smembers(&quot;names&quot;);
        System.out.println(names);
        jedis.close();
    &#125;

    //操作hash
    @Test
    public void demo4() &#123;
        //创建Jedis对象
        Jedis jedis = new Jedis(&quot;192.168.31.51&quot;, 6379);

        jedis.hset(&quot;users&quot;,&quot;age&quot;,&quot;20&quot;);
        String hget = jedis.hget(&quot;users&quot;, &quot;age&quot;);
        System.out.println(hget);
        jedis.close();
    &#125;

    //操作zset
    @Test
    public void demo5() &#123;
        //创建Jedis对象
        Jedis jedis = new Jedis(&quot;192.168.31.51&quot;, 6379);

        jedis.zadd(&quot;china&quot;,100d,&quot;shanghai&quot;);

        Set&lt;String&gt; china = jedis.zrange(&quot;china&quot;, 0, -1);
        System.out.println(china);

        jedis.close();
    &#125;
&#125;
</code></pre>
<h3 id="模拟验证码发送"><a href="#模拟验证码发送" class="headerlink" title="模拟验证码发送"></a>模拟验证码发送</h3><blockquote>
<p>1、输入手机号，点击发送后随机生成6位数字码，2分钟有效<br>2、输入验证码，点击验证，返回成功或失败<br>3、每个手机号每天只能输入3次</p>
</blockquote>
<pre><code>package cn.itxiaoma.jedis;

import redis.clients.jedis.Jedis;

import java.util.Random;

public class PhoneCode &#123;

    public static void main(String[] args) &#123;
        //模拟验证码发送
        verifyCode(&quot;13678765435&quot;);

        //模拟验证码校验
        //getRedisCode(&quot;13678765435&quot;,&quot;4444&quot;);
    &#125;

    //3 验证码校验
    public static void getRedisCode(String phone, String code) &#123;
        //从redis获取验证码
        Jedis jedis = new Jedis(&quot;192.168.31.51&quot;, 6379);
        //验证码key
        String codeKey = &quot;VerifyCode&quot; + phone + &quot;:code&quot;;
        String redisCode = jedis.get(codeKey);
        //判断
        if (redisCode.equals(code)) &#123;
            System.out.println(&quot;成功&quot;);
        &#125; else &#123;
            System.out.println(&quot;失败&quot;);
        &#125;
        jedis.close();
    &#125;

    //2 每个手机每天只能发送三次，验证码放到redis中，设置过期时间120
    public static void verifyCode(String phone) &#123;
        //连接redis
        Jedis jedis = new Jedis(&quot;192.168.31.51&quot;, 6379);

        //拼接key
        //手机发送次数key
        String countKey = &quot;VerifyCode&quot; + phone + &quot;:count&quot;;
        //验证码key
        String codeKey = &quot;VerifyCode&quot; + phone + &quot;:code&quot;;

        //每个手机每天只能发送三次
        String count = jedis.get(countKey);
        if (count == null) &#123;
            //没有发送次数，第一次发送
            //设置发送次数是1
            jedis.setex(countKey, 24 * 60 * 60, &quot;1&quot;);
        &#125; else if (Integer.parseInt(count) &lt;= 2) &#123;
            //发送次数+1
            jedis.incr(countKey);
        &#125; else if (Integer.parseInt(count) &gt; 2) &#123;
            //发送三次，不能再发送
            System.out.println(&quot;今天发送次数已经超过三次&quot;);
            jedis.close();
            return;//超过三次之后就会自动退出不会再发送了，不添加这一行，即使显示发送次数，但还会有验证码接收到
        &#125;

        //发送验证码放到redis里面
        String vcode = getCode();//调用生成的验证码
        jedis.setex(codeKey, 120, vcode);//设置生成的验证码只有120秒的时间
        System.out.println(&quot;发送成功&quot;);
        jedis.close();
    &#125;

    //1 生成6位数字验证码，code是验证码
    public static String getCode() &#123;
        Random random = new Random();
        String code = &quot;&quot;;
        for (int i = 0; i &lt; 6; i++) &#123;
            int rand = random.nextInt(10);
            code += rand;
        &#125;
        return code;
    &#125;
&#125;
</code></pre>
<h2 id="8-SprintBoot整合Redis"><a href="#8-SprintBoot整合Redis" class="headerlink" title="8.SprintBoot整合Redis"></a>8.SprintBoot整合Redis</h2><h4 id="创建SpringBoot工程"><a href="#创建SpringBoot工程" class="headerlink" title="创建SpringBoot工程"></a>创建SpringBoot工程</h4><p>New Project  &gt;  Spring Initializr  &gt;   配置项目 &gt; Next</p>
<h4 id="1-依赖文件"><a href="#1-依赖文件" class="headerlink" title="1.依赖文件"></a>1.依赖文件</h4><pre><code>&lt;!-- redis --&gt;
&lt;dependency&gt;
&lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
&lt;artifactId&gt;spring-boot-starter-data-redis&lt;/artifactId&gt;
&lt;/dependency&gt;

&lt;!-- spring2.X集成redis所需common-pool2--&gt;
&lt;dependency&gt;
&lt;groupId&gt;org.apache.commons&lt;/groupId&gt;
&lt;artifactId&gt;commons-pool2&lt;/artifactId&gt;
&lt;version&gt;2.6.0&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<h4 id="2-redis配置"><a href="#2-redis配置" class="headerlink" title="2.redis配置"></a>2.redis配置</h4><p>src/main/resources/application.properties</p>
<pre><code>#Redis服务器地址
spring.redis.host=192.168.31.51
#Redis服务器连接端口
spring.redis.port=6379
#Redis数据库索引（默认为0）
spring.redis.database= 0
#连接超时时间（毫秒）
spring.redis.timeout=1800000
#连接池最大连接数（使用负值表示没有限制）
spring.redis.lettuce.pool.max-active=20
#最大阻塞等待时间(负数表示没限制)
spring.redis.lettuce.pool.max-wait=-1
#连接池中的最大空闲连接
spring.redis.lettuce.pool.max-idle=5
#连接池中的最小空闲连接
spring.redis.lettuce.pool.min-idle=0
</code></pre>
<h4 id="3-redis配置类"><a href="#3-redis配置类" class="headerlink" title="3.redis配置类"></a>3.<strong>redis配置类</strong></h4><p>src/main/java/cn/itxiaoma/redis_springboot/config/RedisConfig.java</p>
<pre><code>package cn.itxiaoma.redis_springboot.config;

import com.fasterxml.jackson.annotation.JsonAutoDetect;
import com.fasterxml.jackson.annotation.PropertyAccessor;
import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.cache.CacheManager;
import org.springframework.cache.annotation.CachingConfigurerSupport;
import org.springframework.cache.annotation.EnableCaching;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.redis.cache.RedisCacheConfiguration;
import org.springframework.data.redis.cache.RedisCacheManager;
import org.springframework.data.redis.connection.RedisConnectionFactory;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.data.redis.serializer.Jackson2JsonRedisSerializer;
import org.springframework.data.redis.serializer.RedisSerializationContext;
import org.springframework.data.redis.serializer.RedisSerializer;
import org.springframework.data.redis.serializer.StringRedisSerializer;

import java.time.Duration;


@EnableCaching
@Configuration
public class RedisConfig extends CachingConfigurerSupport &#123;

    @Bean
    public RedisTemplate&lt;String, Object&gt; redisTemplate(RedisConnectionFactory factory) &#123;
        RedisTemplate&lt;String, Object&gt; template = new RedisTemplate&lt;&gt;();
        RedisSerializer&lt;String&gt; redisSerializer = new StringRedisSerializer();
        Jackson2JsonRedisSerializer jackson2JsonRedisSerializer = new Jackson2JsonRedisSerializer(Object.class);
        ObjectMapper om = new ObjectMapper();
        om.setVisibility(PropertyAccessor.ALL, JsonAutoDetect.Visibility.ANY);
        om.enableDefaultTyping(ObjectMapper.DefaultTyping.NON_FINAL);
        jackson2JsonRedisSerializer.setObjectMapper(om);
        template.setConnectionFactory(factory);
        //key序列化方式
        template.setKeySerializer(redisSerializer);
        //value序列化
        template.setValueSerializer(jackson2JsonRedisSerializer);
        //value hashmap序列化
        template.setHashValueSerializer(jackson2JsonRedisSerializer);
        return template;
    &#125;

    @Bean
    public CacheManager cacheManager(RedisConnectionFactory factory) &#123;
        RedisSerializer&lt;String&gt; redisSerializer = new StringRedisSerializer();
        Jackson2JsonRedisSerializer jackson2JsonRedisSerializer = new Jackson2JsonRedisSerializer(Object.class);
        //解决查询缓存转换异常的问题
        ObjectMapper om = new ObjectMapper();
        om.setVisibility(PropertyAccessor.ALL, JsonAutoDetect.Visibility.ANY);
        om.enableDefaultTyping(ObjectMapper.DefaultTyping.NON_FINAL);
        jackson2JsonRedisSerializer.setObjectMapper(om);
        // 配置序列化（解决乱码的问题）,过期时间600秒
        RedisCacheConfiguration config = RedisCacheConfiguration.defaultCacheConfig()
                .entryTtl(Duration.ofSeconds(600))
                .serializeKeysWith(RedisSerializationContext.SerializationPair.fromSerializer(redisSerializer))
                .serializeValuesWith(RedisSerializationContext.SerializationPair.fromSerializer(jackson2JsonRedisSerializer))
                .disableCachingNullValues();
        RedisCacheManager cacheManager = RedisCacheManager.builder(factory)
                .cacheDefaults(config)
                .build();
        return cacheManager;
    &#125;
&#125;
</code></pre>
<p><code>@EnableCaching</code>开启缓存类<br><code>@Configuration</code>配置类</p>
<h4 id="4-调试"><a href="#4-调试" class="headerlink" title="4.调试"></a>4.调试</h4><p>src/main/java/cn/itxiaoma/redis_springboot/controller/RedisTestController.java</p>
<pre><code>package cn.itxiaoma.redis_springboot.controller;


import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping(&quot;/redisTest&quot;)
public class RedisTestController &#123;
    @Autowired
    private RedisTemplate redisTemplate;

    @GetMapping
    public String testRedis() &#123;
        //设置值到redis
        redisTemplate.opsForValue().set(&quot;name&quot;, &quot;ITXiaoMa&quot;);
        //从redis获取值
        String name = (String) redisTemplate.opsForValue().get(&quot;name&quot;);
        return name;
    &#125;
&#125;
</code></pre>
<p>访问：<a href="http://localhost:8080/redisTest">http://localhost:8080/redisTest</a></p>
<p>返回：ITXiaoMa</p>
<h2 id="参考-Reference"><a href="#参考-Reference" class="headerlink" title="参考 Reference"></a>参考 Reference</h2><p>参考资料：《Redis开发与运维》</p>
<p>视频教程：<a href="https://www.bilibili.com/video/BV1Rv41177Af">https://www.bilibili.com/video/BV1Rv41177Af</a></p>
<p>参考笔记：<a href="https://zhangc233.github.io/2021/05/02/Redis/">https://zhangc233.github.io/2021/05/02/Redis/</a></p>
<p>参考笔记：<a href="https://blog.csdn.net/weixin_47872288/article/details/118410080">https://blog.csdn.net/weixin_47872288/article/details/118410080</a></p>
]]></content>
      <categories>
        <category>REDIS</category>
      </categories>
      <tags>
        <tag>REDIS</tag>
        <tag>后端</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis06-事务、锁、秒杀</title>
    <url>/2022/02/06/Redis06-%E4%BA%8B%E5%8A%A1%E3%80%81%E9%94%81%E3%80%81%E7%A7%92%E6%9D%80/</url>
    <content><![CDATA[<h2 id="9-事务"><a href="#9-事务" class="headerlink" title="9.事务"></a>9.事务</h2><p>Redis 事务是一个单独的隔离操作：事务中的所有命令都会序列化、按顺序地执行。事务在执行的过程中，不会被其他客户端发送来的命令请求所打断。</p>
<p>Redis 事务的主要作用就是<strong>串联多个命令</strong>防止别的命令插队。</p>
<h3 id="multi、exec、discard"><a href="#multi、exec、discard" class="headerlink" title="multi、exec、discard"></a>multi、exec、discard</h3><p>从输入multi开始，输入的命令会依次进入命令队列中，但不会执行；直到输入exec后，会将之前命令队列中的命令依次执行。</p>
<p>组队的过程中可以通过discard来放弃组队。</p>
<p><img src="https://tva1.sinaimg.cn/large/e6c9d24ely1h0s1gd6snij20n607laal.jpg" alt="image-20220330173052359"></p>
<pre><code>127.0.0.1:6379&gt; multi
OK
127.0.0.1:6379(TX)&gt; set k1 v1
QUEUED
127.0.0.1:6379(TX)&gt; set k2 v2
QUEUED
127.0.0.1:6379(TX)&gt; exec
1) OK
2) OK
127.0.0.1:6379&gt; multi
OK
127.0.0.1:6379(TX)&gt; set k3 v3
QUEUED
127.0.0.1:6379(TX)&gt; discard
OK
</code></pre>
<h3 id="错误处理"><a href="#错误处理" class="headerlink" title="错误处理"></a>错误处理</h3><p><img src="https://tva1.sinaimg.cn/large/e6c9d24ely1h0s24xp2bgj20mp09i0tc.jpg" alt="image-20220330175429278"></p>
<ul>
<li>组队中某个命令出现了错误报告，执行时整个队列中的命令都会被取消</li>
<li>执行阶段某个命令报错，则只有报错的命令不会被执行，其他的命令都会执行，不会回滚。</li>
</ul>
<h2 id="10-锁"><a href="#10-锁" class="headerlink" title="10.锁"></a>10.锁</h2><h3 id="悲观锁-Pessimistic-Lock"><a href="#悲观锁-Pessimistic-Lock" class="headerlink" title="悲观锁 (Pessimistic Lock)"></a>悲观锁 (Pessimistic Lock)</h3><p>每次在拿数据的时候都会上锁，这样别人想拿这个数据就会 block 直到它拿到锁。</p>
<p>传统的关系型数据库里边就用到了很多这种锁机制<strong>，比如</strong>行锁<strong>，</strong>表锁<strong>等，</strong>读锁<strong>，</strong>写锁**等，都是在做操作之前先上锁。</p>
<h3 id="乐观锁-Optimistic-Lock"><a href="#乐观锁-Optimistic-Lock" class="headerlink" title="乐观锁 (Optimistic Lock)"></a>乐观锁 (Optimistic Lock)</h3><p>每次去拿数据的时候都认为别人不会修改，所以不会上锁，但是在更新的时候会判断一下在此期间别人有没有去更新这个数据，可以使用版本号等机制。</p>
<p>乐观锁适用于多读的应用类型，这样可以提高吞吐量。Redis 就是利用这种 <strong>check-and-set 机制</strong>实现事务的。</p>
<h2 id="11-秒杀案例"><a href="#11-秒杀案例" class="headerlink" title="11.秒杀案例"></a>11.秒杀案例</h2><p><img src="https://tva1.sinaimg.cn/large/e6c9d24ely1h0s6ga8wkej20d903aq2z.jpg" alt="image-20220330202345429"></p>
<h3 id="流程"><a href="#流程" class="headerlink" title="流程"></a>流程</h3><ol>
<li>先判断这两个有一个为空，则返回false</li>
<li>通过jedis来连接服务器的redis</li>
<li>用户库存的名称和数量为后台服务器redis给出，通过set 用户名称 数量</li>
<li>判断库存的数量是否为0，为0代表还没开始，结束其jedis连接</li>
<li>在判断用户是不是有所重复，通过java代码模块的sismember，判断现在订单的id与服务器id是否有重复的，</li>
<li>有重复就结束它的jedis连接</li>
<li>判断商品数量是否为0，为0就结束jedis的连接</li>
<li>核心代码也就是库存数减1个，用户数加1个，多个用户来秒杀库存</li>
</ol>
<h3 id="代码示例"><a href="#代码示例" class="headerlink" title="代码示例"></a>代码示例</h3><p>Java配置参考：</p>
<p><a href="https://blog.itxiaoma.cn/2021/11/21/JavaWeb01-Tomcat%EF%BC%8CMaven%E9%85%8D%E7%BD%AE/">JavaWeb01-Tomcat，Maven配置</a></p>
<p><a href="https://blog.itxiaoma.cn/2021/11/22/JavaWeb02%20-%20Servlet%EF%BC%8CCookie%EF%BC%8CSession/">JavaWeb02 - Servlet，Cookie，Session</a></p>
<p>pom.xml引入依赖</p>
<pre><code>&lt;dependency&gt;
  &lt;groupId&gt;javax.servlet&lt;/groupId&gt;
  &lt;artifactId&gt;javax.servlet-api&lt;/artifactId&gt;
  &lt;version&gt;4.0.1&lt;/version&gt;
  &lt;scope&gt;compile&lt;/scope&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
  &lt;groupId&gt;javax.servlet.jsp&lt;/groupId&gt;
  &lt;artifactId&gt;javax.servlet.jsp-api&lt;/artifactId&gt;
  &lt;version&gt;2.3.3&lt;/version&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
  &lt;groupId&gt;redis.clients&lt;/groupId&gt;
  &lt;artifactId&gt;jedis&lt;/artifactId&gt;
  &lt;version&gt;3.6.1&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p>src/main/webapp/index.jsp</p>
<pre><code>&lt;%@ page language=&quot;java&quot; contentType=&quot;text/html; charset=UTF-8&quot;
         pageEncoding=&quot;UTF-8&quot;%&gt;
&lt;!DOCTYPE html PUBLIC &quot;-//W3C//DTD HTML 4.01 Transitional//EN&quot; &quot;http://www.w3.org/TR/html4/loose.dtd&quot;&gt;
&lt;html&gt;
&lt;head&gt;
    &lt;meta http-equiv=&quot;Content-Type&quot; content=&quot;text/html; charset=UTF-8&quot;&gt;
    &lt;title&gt;Insert title here&lt;/title&gt;
    &lt;script src=&quot;http://ajax.aspnetcdn.com/ajax/jQuery/jquery-1.8.0.js&quot;&gt;&lt;/script&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;h1&gt;iPhone 13 Pro !!!  1元秒杀！！！
&lt;/h1&gt;


&lt;form id=&quot;msform&quot; action=&quot;&lt;%=request.getContextPath()%&gt;/doseckill&quot; enctype=&quot;application/x-www-form-urlencoded&quot;&gt;
    &lt;input type=&quot;hidden&quot; id=&quot;prodid&quot; name=&quot;prodid&quot; value=&quot;0101&quot;&gt;
    &lt;input type=&quot;button&quot;  id=&quot;miaosha_btn&quot; name=&quot;seckill_btn&quot; value=&quot;秒杀点我&quot;/&gt;
&lt;/form&gt;

&lt;/body&gt;
&lt;script  type=&quot;text/javascript&quot;&gt;
    $(function()&#123;
        $(&quot;#miaosha_btn&quot;).click(function()&#123;
            var url=$(&quot;#msform&quot;).attr(&quot;action&quot;);
            $.post(url,$(&quot;#msform&quot;).serialize(),function(data)&#123;
                if(data==&quot;false&quot;)&#123;
                    alert(&quot;抢光了&quot; );
                    $(&quot;#miaosha_btn&quot;).attr(&quot;disabled&quot;,true);
                &#125;
            &#125; );
        &#125;)
    &#125;)
&lt;/script&gt;
&lt;/html&gt;
</code></pre>
<p>src/main/webapp/WEB-INF/web.xml</p>
<pre><code>&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;
&lt;web-app xmlns=&quot;http://xmlns.jcp.org/xml/ns/javaee&quot;
         xmlns:xsi=&quot;http://www.w3.org/2001/XMLSchema-instance&quot;
         xsi:schemaLocation=&quot;http://xmlns.jcp.org/xml/ns/javaee
                      http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd&quot;
         version=&quot;4.0&quot;
         metadata-complete=&quot;true&quot;&gt;
    &lt;servlet&gt;
        &lt;description&gt;&lt;/description&gt;
        &lt;display-name&gt;doseckill&lt;/display-name&gt;
        &lt;servlet-name&gt;doseckill&lt;/servlet-name&gt;
        &lt;servlet-class&gt;cn.itxiaoma.servlet.SecKillServlet&lt;/servlet-class&gt;
    &lt;/servlet&gt;
    &lt;servlet-mapping&gt;
        &lt;servlet-name&gt;doseckill&lt;/servlet-name&gt;
        &lt;url-pattern&gt;/doseckill&lt;/url-pattern&gt;
    &lt;/servlet-mapping&gt;
&lt;/web-app&gt;
</code></pre>
<p>cn/itxiaoma/servlet/SecKillServlet.java</p>
<pre><code>package cn.itxiaoma.servlet;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.io.IOException;
import java.util.Random;

/**
 * 秒杀案例
 */
public class SecKillServlet extends HttpServlet &#123;
    private static final long serialVersionUID = 1L;

    public SecKillServlet() &#123;
        super();
    &#125;

    protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException, IOException &#123;

        String userid = new Random().nextInt(50000) +&quot;&quot; ;
        String prodid =request.getParameter(&quot;prodid&quot;);

        boolean isSuccess=SecKill_redis.doSecKill(userid,prodid);
        //boolean isSuccess= SecKill_redisByScript.doSecKill(userid,prodid);
        response.getWriter().print(isSuccess);
    &#125;
&#125;
</code></pre>
<p>cn/itxiaoma/servlet/SecKill_redis.java</p>
<pre><code>package cn.itxiaoma.servlet;

import redis.clients.jedis.Jedis;

import java.io.IOException;

public class SecKill_redis &#123;

    public static void main(String[] args) &#123;
        Jedis jedis =new Jedis(&quot;192.168.31.51&quot;,6379);
        System.out.println(jedis.ping());
        jedis.close();
    &#125;

    //秒杀过程
    public static boolean doSecKill(String uid,String prodid) throws IOException &#123;
        //1 uid和prodid非空判断
        if(uid == null || prodid == null) &#123;
            return false;
        &#125;

        //2 连接redis
        Jedis jedis = new Jedis(&quot;192.168.31.51&quot;,6379);

        //3 拼接key
        // 3.1 库存key
        String kcKey = &quot;sk:&quot;+prodid+&quot;:qt&quot;;
        // 3.2 秒杀成功用户key
        String userKey = &quot;sk:&quot;+prodid+&quot;:user&quot;;

        //4 获取库存，如果库存null，秒杀还没有开始
        String kc = jedis.get(kcKey);
        if(kc == null) &#123;
            System.out.println(&quot;秒杀还没有开始，请等待&quot;);
            jedis.close();
            return false;
        &#125;

        // 5 判断用户是否重复秒杀操作
        if(jedis.sismember(userKey, uid)) &#123;
            System.out.println(&quot;已经秒杀成功了，不能重复秒杀&quot;);
            jedis.close();
            return false;
        &#125;


        //因为kc为字符串，所以先转换城integer类型的
        //6 判断如果商品数量，库存数量小于1，秒杀结束
        if(Integer.parseInt(kc)&lt;=0) &#123;
            System.out.println(&quot;秒杀已经结束了&quot;);
            jedis.close();
            return false;
        &#125;


        //7.1 库存-1
        jedis.decr(kcKey);
        //7.2 把秒杀成功用户添加清单里面
        jedis.sadd(userKey,uid);

        System.out.println(&quot;秒杀成功了..&quot;);
        jedis.close();
        return true;
    &#125;
&#125;
</code></pre>
<p>测试：</p>
<ol>
<li><p>手动加入商品库存</p>
<pre><code>set sk:0101:qt 10
</code></pre>
</li>
<li><p>点击秒杀，控制台提示秒杀成功</p>
</li>
<li><p>秒杀结束，查看秒杀成功的用户</p>
<pre><code>smembers sk:0101:user
</code></pre>
</li>
</ol>
<p>高并发问题：</p>
<ol>
<li>超卖问题，数量会显示负值</li>
<li>连接超时问题</li>
</ol>
<h3 id="ab并发测试"><a href="#ab并发测试" class="headerlink" title="ab并发测试"></a>ab并发测试</h3><h4 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h4><p>可以使用命令<code>apt install apache2-utils</code>或者是<code>apt install httpd-tools</code>，如果apt-get不能安装，换成yum（系统版本问题）</p>
<h4 id="常用参数"><a href="#常用参数" class="headerlink" title="常用参数"></a>常用参数</h4><ul>
<li>-n 请求次数</li>
<li>-c 当前请求次数的并发请求</li>
<li>-k 增加请求头 Connection: Keep-Alive（开启了 HTTP 长连接，在一个连接里面进行多次通讯，浏览器支持 http/1.1都是用keeplive的）</li>
<li>-T 设计的类型，可以是post，get</li>
<li>-p 提交的参数（文件路径， 模拟表单提交参数，以&amp;符号结尾，如<code>prodid=0101&amp;</code>）</li>
</ul>
<p>示例：</p>
<p>vim postfile</p>
<pre><code>prodid=0101&amp;
</code></pre>
<pre><code>ab -n 2000 -c 20 -p ./postfile -T application/x-www-form-urlencoded http://192.168.31.51:8080/web_war/doseckill
</code></pre>
<p>结果</p>
<pre><code>127.0.0.1:6379&gt; get sk:0101:qt
&quot;-9&quot;
</code></pre>
<h3 id="问题解决"><a href="#问题解决" class="headerlink" title="问题解决"></a>问题解决</h3><h4 id="连接超时问题：增加连接池"><a href="#连接超时问题：增加连接池" class="headerlink" title="连接超时问题：增加连接池"></a>连接超时问题：增加连接池</h4><p>节省每次连接redis服务带来的消耗，把连接好的实例反复利用</p>
<ul>
<li>MaxTotal：控制一个pool可分配多少个jedis实例，通过pool.getResource()来获取；如果赋值为-1，则表示不限制；如果pool已经分配了MaxTotal个jedis实例，则此时pool的状态为exhausted。</li>
<li>maxIdle：控制一个pool最多有多少个状态为idle(空闲)的jedis实例；</li>
<li>MaxWaitMillis：表示当borrow一个jedis实例时，最大的等待毫秒数，如果超过等待时间，则直接抛JedisConnectionException；</li>
<li>testOnBorrow：获得一个jedis实例的时候是否检查连接可用性（ping()）；如果为true，则得到的jedis实例均是可用的；</li>
</ul>
<p>cn/itxiaoma/servlet/JedisPoolUtil.java</p>
<pre><code>package cn.itxiaoma.servlet;

import redis.clients.jedis.Jedis;
import redis.clients.jedis.JedisPool;
import redis.clients.jedis.JedisPoolConfig;

public class JedisPoolUtil &#123;
    private static volatile JedisPool jedisPool = null;

    private JedisPoolUtil() &#123;
    &#125;

    public static JedisPool getJedisPoolInstance() &#123;
        if (null == jedisPool) &#123;
            synchronized (JedisPoolUtil.class) &#123;
                if (null == jedisPool) &#123;
                    JedisPoolConfig poolConfig = new JedisPoolConfig();
                    poolConfig.setMaxTotal(200);
                    poolConfig.setMaxIdle(32);
                    poolConfig.setMaxWaitMillis(100*1000);
                    poolConfig.setBlockWhenExhausted(true);
                    poolConfig.setTestOnBorrow(true);  // ping  PONG，判断是否还存在

                    jedisPool = new JedisPool(poolConfig, &quot;192.168.31.51&quot;, 6379, 60000 );
                &#125;
            &#125;
        &#125;
        return jedisPool;
    &#125;

    public static void release(JedisPool jedisPool, Jedis jedis) &#123;
        if (null != jedis) &#123;
            jedisPool.returnResource(jedis);
        &#125;
    &#125;
&#125;
</code></pre>
<p>cn/itxiaoma/servlet/SecKill_redis.java</p>
<pre><code>//2 连接redis
//Jedis jedis = new Jedis(&quot;192.168.31.51&quot;,6379);
//通过连接池得到jedis对象
JedisPool jedisPoolInstance = JedisPoolUtil.getJedisPoolInstance();
Jedis jedis = jedisPoolInstance.getResource();
</code></pre>
<h4 id="超卖问题：增加乐观锁"><a href="#超卖问题：增加乐观锁" class="headerlink" title="超卖问题：增加乐观锁"></a>超卖问题：增加乐观锁</h4><ul>
<li>通过设置一个乐观锁，监视并且使用三个常用命令</li>
<li>通过判断版本号，判断是否有更改kckey的混乱使用</li>
<li>具体秒杀的过程通过事务来解决</li>
</ul>
<p>Redis Watch 命令用于监视一个(或多个) key ，如果在事务执行之前这个(或这些) key 被其他命令所改动，那么事务将被打断。</p>
<p>当事务执行时,也就是服务器收到了 exec 指令要顺序执行缓存的事务队列时, <em>Redis</em> 会检查关键变量自 <em>Watch</em> 之后,是否被修改了。</p>
<p><img src="https://tva1.sinaimg.cn/large/e6c9d24ely1h0sbxcim5qj20f50ataak.jpg" alt="image-20220330233311281"></p>
<pre><code>//加入一个监视的watch
jedis.watch(kcKey);

//7 秒杀过程
//使用事务
Transaction multi = jedis.multi();

//组队操作
multi.decr(kcKey);
multi.sadd(userKey,uid);

//执行
List&lt;Object&gt; results = multi.exec();

if(results == null || results.size()==0) &#123;
    System.out.println(&quot;秒杀失败了....&quot;);
    jedis.close();
    return false;
&#125;

System.out.println(&quot;秒杀成功了..&quot;);
jedis.close();
return true;
</code></pre>
<p>问题：有时还是会出现负数？？？</p>
<h3 id="商品遗留问题"><a href="#商品遗留问题" class="headerlink" title="商品遗留问题"></a>商品遗留问题</h3><p>由于增加了乐观锁之后，假设一个人买了之后，版本改变了，下一个人都不能买了，所以出现了商品遗留的问题</p>
<h4 id="LUA脚本"><a href="#LUA脚本" class="headerlink" title="LUA脚本"></a>LUA脚本</h4><p>嵌入式脚本语言，很多应用程序、游戏使用LUA作为自己的嵌入式脚本语言，以此来实现可配置性、可扩展性</p>
<ul>
<li>将复杂的或者多步的redis操作，写为一个脚本，一次提交给redis执行，减少反复连接redis的次数，提升性能。</li>
<li>LUA脚本是类似redis事务，有一定的原子性，不会被其他命令插队，可以完成一些redis事务性的操作，但是注意redis的lua脚本功能，只有在Redis 2.6以上的版本才可以使用。</li>
<li>利用lua脚本淘汰用户，解决超卖问题。</li>
<li>redis 2.6版本以后，通过lua脚本解决争抢问题，实际上是redis 利用其单线程的特性，用任务队列的方式解决多任务并发问题。</li>
</ul>
<h4 id="LUA基础语法"><a href="#LUA基础语法" class="headerlink" title="LUA基础语法"></a>LUA基础语法</h4><h5 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h5><table>
<thead>
<tr>
<th align="left">数据类型</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">nil</td>
<td align="left">这个最简单，只有值nil属于该类，表示一个无效值（在条件表达式中相当于false）。</td>
</tr>
<tr>
<td align="left">boolean</td>
<td align="left">包含两个值：false和true。</td>
</tr>
<tr>
<td align="left">number</td>
<td align="left">表示双精度类型的实浮点数</td>
</tr>
<tr>
<td align="left">string</td>
<td align="left">字符串由一对双引号或单引号来表示</td>
</tr>
<tr>
<td align="left">function</td>
<td align="left">由 C 或 Lua 编写的函数</td>
</tr>
<tr>
<td align="left">userdata</td>
<td align="left">表示任意存储在变量中的C数据结构</td>
</tr>
<tr>
<td align="left">thread</td>
<td align="left">表示执行的独立线路，用于执行协同程序</td>
</tr>
<tr>
<td align="left">table</td>
<td align="left">Lua 中的表（table）其实是一个”关联数组”（associative arrays），数组的索引可以是数字或者是字符串。在 Lua 里，table 的创建是通过”构造表达式”来完成，最简单构造表达式是{}，用来创建一个空表。</td>
</tr>
</tbody></table>
<h5 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h5><ul>
<li><p>变量在使用前，必须在代码中进行声明，即创建该变量。</p>
</li>
<li><p>Lua 变量有三种类型：全局变量、局部变量、表中的域。</p>
</li>
<li><p>Lua 中的变量全是全局变量，那怕是语句块或是函数里，除非用 local 显式声明为局部变量。</p>
</li>
<li><p>局部变量的作用域为从声明位置开始到所在语句块结束。</p>
</li>
<li><p>变量的默认值均为 nil。</p>
</li>
</ul>
<pre><code class="lua">-- test.lua 文件脚本
a = 5               -- 全局变量
local b = 5         -- 局部变量

function joke()
    c = 5           -- 全局变量
    local d = 6     -- 局部变量
end

joke()
print(c,d)          --&gt; 5 nil

do 
    local a = 6     -- 局部变量
    b = 6           -- 全局变量
    print(a,b);     --&gt; 6 6
end

print(a,b)      --&gt; 5 6
</code></pre>
<p>应该尽可能的使用局部变量，有两个好处：</p>
<ol>
<li>避免命名冲突。</li>
<li>访问局部变量的速度比全局变量更快。</li>
</ol>
<h5 id="赋值语句"><a href="#赋值语句" class="headerlink" title="赋值语句"></a>赋值语句</h5><pre><code>a = &quot;hello&quot; .. &quot;world&quot;
t.n = t.n + 1
x, y = y, x                     -- swap &#39;x&#39; for &#39;y&#39;
a[i], a[j] = a[j], a[i]         -- swap &#39;a[i]&#39; for &#39;a[j]&#39;
a, b = f()                                            -- 多值赋值经常用来交换变量，或将函数调用返回给变量：
</code></pre>
<p>变量个数 &gt; 值的个数             按变量个数补足nil</p>
<p>变量个数 &lt; 值的个数             多余的值会被忽略</p>
<h5 id="索引"><a href="#索引" class="headerlink" title="索引"></a>索引</h5><pre><code>t[i]
t.i                 -- 当索引为字符串类型时的一种简化写法
gettable_event(t,i) -- 采用索引访问本质上是一个类似这样的函数调用
</code></pre>
<h5 id="循环"><a href="#循环" class="headerlink" title="循环"></a>循环</h5><table>
<thead>
<tr>
<th align="left">循环类型</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">while 循环</td>
<td align="left">在条件为 true 时，让程序重复地执行某些语句。执行语句前会先检查条件是否为 true。</td>
</tr>
<tr>
<td align="left"><a href="https://edu.aliyun.com/lua/lua-for-loop.html">for 循环</a></td>
<td align="left">重复执行指定语句，重复次数可在 for 语句中控制。</td>
</tr>
<tr>
<td align="left">repeat…until</td>
<td align="left">重复执行循环，直到 指定的条件为真时为止</td>
</tr>
<tr>
<td align="left">循环嵌套</td>
<td align="left">可以在循环内嵌套一个或多个循环语句（while、for、do..while）</td>
</tr>
<tr>
<td align="left">无限循环</td>
<td align="left"><code>while( true )do 换行 print(&quot;循环将永远执行下去&quot;)end</code></td>
</tr>
</tbody></table>
<h5 id="流程控制"><a href="#流程控制" class="headerlink" title="流程控制"></a>流程控制</h5><table>
<thead>
<tr>
<th align="left">语句</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">if 语句</td>
<td align="left"><strong>if 语句</strong> 由一个布尔表达式作为条件判断，其后紧跟其他语句组成。</td>
</tr>
<tr>
<td align="left">if…else 语句</td>
<td align="left"><strong>if 语句</strong> 可以与 <strong>else 语句</strong>搭配使用, 在 if 条件表达式为 false 时执行 else 语句代码。</td>
</tr>
<tr>
<td align="left">if 嵌套语句</td>
<td align="left">你可以在<strong>if</strong> 或 <strong>else if</strong>中使用一个或多个 <strong>if</strong> 或 <strong>else if</strong> 语句 。</td>
</tr>
</tbody></table>
<pre><code>if a&gt;b then
    print(&quot;0 为 true&quot;)end
</code></pre>
<h5 id="常用函数"><a href="#常用函数" class="headerlink" title="常用函数"></a>常用函数</h5><p>库函数</p>
<pre><code>assert(value) - 检查一个值是否为非nil, 若不是则(如果在wow.exe打开调试命令)显示对话框以及输出错误调试信息

date(format, time) - 返回当前用户机器上的时间.

error(&quot;error message&quot;,level) - 发生错误时,输出一条定义的错误信息.使用pcall() (见下面)捕捉错误.

loadstring(&quot;Lua code&quot;) - 分析字符串中的lua代码块并且将结果作为一个函数返回

time(table) - 返回从一个unix时间值

type(var) - 判断当前变量的类型, &quot;number&quot;, &quot;string&quot;, &quot;table&quot;, &quot;function&quot; 或者 &quot;userdata&quot;.
</code></pre>
<p>数学函数</p>
<pre><code>abs(value) - 返回当前数值的绝对值

ceil(value) - 返回一个整数, 不管小数点后面是多少, 整数部分都进1

floor(value) - 返回此值的整数值

max(value[, values...]) - 在一堆值中查找最大值

min(value[,values...]) - 在一堆值中查找最小值

mod(value,modulus) - 返回此值的余数

random([ [lower,] upper]) - 返回一个随机数字 (可选界限为整数值)
</code></pre>
<p>字符串函数</p>
<pre><code>format(formatstring[, value[, ...]]) - 格式化字符串

gsub(string,pattern,replacement[, limitCount]) - 全局替换

strbyte(string[, index]) - 转换字符串为整数值(可以指定某个字符).

strchar(asciiCode[, ...]) - 转换整数为相对应的字符

strfind(string, pattern[, initpos[, plain]]) - 在一个指定的目标字符串中搜索指定的内容(第三个参数为索引),返回其具体位置.

strlen(string) - 返回当前字符串的字符数

strlower(string) - 将字符串的字母转换为小写格式

strmatch(string, pattern[, initpos]) - 与strfind不同的是,macth返回的是具体值,而find返回的是此值的位置

strrep(seed,count) - 返回一个字符串种子副本的个数

strsub(string, index[, endIndex]) - 返回字符串指定位置的值.

strupper(string) - 将字符串的字母转为大写格式

tonumber(arg[, base]) - 若参数能转为数字则返回一个数值.可以指定转换的类型.默认为十进制整数

tostring(arg) - 转换参数为字符串
</code></pre>
<h4 id="代码示例-1"><a href="#代码示例-1" class="headerlink" title="代码示例"></a>代码示例</h4><p>lua脚本：</p>
<pre><code class="lua">local userid=KEYS[1]; 
local prodid=KEYS[2];
local qtkey=&quot;sk:&quot;..prodid..&quot;:qt&quot;;
local usersKey=&quot;sk:&quot;..prodid.&quot;:usr&#39;; 
local userExists=redis.call(&quot;sismember&quot;,usersKey,userid);
if tonumber(userExists)==1 then 
  return 2;
end
local num= redis.call(&quot;get&quot; ,qtkey);
if tonumber(num)&lt;=0 then 
  return 0; 
else 
  redis.call(&quot;decr&quot;,qtkey);
  redis.call(&quot;sadd&quot;,usersKey,userid);
end
return 1;
</code></pre>
<p>java代码：</p>
<p>src/main/java/cn/itxiaoma/servlet/SecKill_redisByScript.java</p>
<pre><code class="java">package cn.itxiaoma.servlet;

import redis.clients.jedis.HostAndPort;
import redis.clients.jedis.Jedis;
import redis.clients.jedis.JedisPool;

import java.io.IOException;
import java.util.HashSet;
import java.util.Set;

public class SecKill_redisByScript &#123;

    public static void main(String[] args) &#123;
        JedisPool jedispool = JedisPoolUtil.getJedisPoolInstance();

        Jedis jedis = jedispool.getResource();
        System.out.println(jedis.ping());

        Set&lt;HostAndPort&gt; set = new HashSet&lt;HostAndPort&gt;();

        //    doSecKill(&quot;201&quot;,&quot;sk:0101&quot;);
    &#125;

    static String secKillScript = &quot;local userid=KEYS[1];\r\n&quot; +
            &quot;local prodid=KEYS[2];\r\n&quot; +
            &quot;local qtkey=&#39;sk:&#39;..prodid..\&quot;:qt\&quot;;\r\n&quot; +
            &quot;local usersKey=&#39;sk:&#39;..prodid..\&quot;:usr\&quot;;\r\n&quot; +
            &quot;local userExists=redis.call(\&quot;sismember\&quot;,usersKey,userid);\r\n&quot; +
            &quot;if tonumber(userExists)==1 then \r\n&quot; +
            &quot;   return 2;\r\n&quot; +
            &quot;end\r\n&quot; +
            &quot;local num= redis.call(\&quot;get\&quot; ,qtkey);\r\n&quot; +
            &quot;if tonumber(num)&lt;=0 then \r\n&quot; +
            &quot;   return 0;\r\n&quot; +
            &quot;else \r\n&quot; +
            &quot;   redis.call(\&quot;decr\&quot;,qtkey);\r\n&quot; +
            &quot;   redis.call(\&quot;sadd\&quot;,usersKey,userid);\r\n&quot; +
            &quot;end\r\n&quot; +
            &quot;return 1&quot;;

    public static boolean doSecKill(String uid, String prodid) throws IOException &#123;

        JedisPool jedispool = JedisPoolUtil.getJedisPoolInstance();
        Jedis jedis = jedispool.getResource();

        String sha1 = jedis.scriptLoad(secKillScript);
        Object result = jedis.evalsha(sha1, 2, uid, prodid);

        String reString = String.valueOf(result);
        if (&quot;0&quot;.equals(reString)) &#123;
            System.err.println(&quot;已抢空！！&quot;);
        &#125; else if (&quot;1&quot;.equals(reString)) &#123;
            System.out.println(&quot;抢购成功！！！！&quot;);
        &#125; else if (&quot;2&quot;.equals(reString)) &#123;
            System.err.println(&quot;该用户已抢过！！&quot;);
        &#125; else &#123;
            System.err.println(&quot;抢购异常！！&quot;);
        &#125;
        jedis.close();
        return true;
    &#125;
&#125;
</code></pre>
<p>方法调用</p>
<p>src/main/java/cn/itxiaoma/servlet/SecKillServlet.java</p>
<pre><code>boolean isSuccess= SecKill_redisByScript.doSecKill(userid,prodid);
</code></pre>
<h2 id="参考-Reference"><a href="#参考-Reference" class="headerlink" title="参考 Reference"></a>参考 Reference</h2><p>参考资料：《Redis开发与运维》</p>
<p>视频教程：<a href="https://www.bilibili.com/video/BV1Rv41177Af">https://www.bilibili.com/video/BV1Rv41177Af</a></p>
<p>参考笔记：<a href="https://zhangc233.github.io/2021/05/02/Redis/">https://zhangc233.github.io/2021/05/02/Redis/</a></p>
<p>参考笔记：<a href="https://blog.csdn.net/weixin_47872288/article/details/118410080">https://blog.csdn.net/weixin_47872288/article/details/118410080</a></p>
<p>Redis watch命令——监控事务：<a href="http://c.biancheng.net/view/4544.html">http://c.biancheng.net/view/4544.html</a></p>
<p>Lua完全自学手册（图文教程）：<a href="https://edu.aliyun.com/course/506?spm=5176.10731542.0.0.5dd72a1d8kr9YJ">https://edu.aliyun.com/course/506?spm=5176.10731542.0.0.5dd72a1d8kr9YJ</a></p>
]]></content>
      <categories>
        <category>REDIS</category>
      </categories>
      <tags>
        <tag>REDIS</tag>
        <tag>后端</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis08-主从复制、集群</title>
    <url>/2022/02/08/Redis08-%E4%B8%BB%E4%BB%8E%E5%A4%8D%E5%88%B6%E3%80%81%E9%9B%86%E7%BE%A4/</url>
    <content><![CDATA[<h2 id="参考-Reference"><a href="#参考-Reference" class="headerlink" title="参考 Reference"></a>参考 Reference</h2><p>参考资料：《Redis开发与运维》</p>
<p>视频教程：<a href="https://www.bilibili.com/video/BV1Rv41177Af">https://www.bilibili.com/video/BV1Rv41177Af</a></p>
<p>参考笔记：<a href="https://zhangc233.github.io/2021/05/02/Redis/">https://zhangc233.github.io/2021/05/02/Redis/</a></p>
<p>参考笔记：<a href="https://blog.csdn.net/weixin_47872288/article/details/118410080">https://blog.csdn.net/weixin_47872288/article/details/118410080</a></p>
]]></content>
      <categories>
        <category>REDIS</category>
      </categories>
      <tags>
        <tag>REDIS</tag>
        <tag>后端</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis10-新功能介绍</title>
    <url>/2022/02/10/Redis10-%E6%96%B0%E5%8A%9F%E8%83%BD%E4%BB%8B%E7%BB%8D/</url>
    <content><![CDATA[<h2 id="参考-Reference"><a href="#参考-Reference" class="headerlink" title="参考 Reference"></a>参考 Reference</h2><p>参考资料：《Redis开发与运维》</p>
<p>视频教程：<a href="https://www.bilibili.com/video/BV1Rv41177Af">https://www.bilibili.com/video/BV1Rv41177Af</a></p>
<p>参考笔记：<a href="https://zhangc233.github.io/2021/05/02/Redis/">https://zhangc233.github.io/2021/05/02/Redis/</a></p>
<p>参考笔记：<a href="https://blog.csdn.net/weixin_47872288/article/details/118410080">https://blog.csdn.net/weixin_47872288/article/details/118410080</a></p>
]]></content>
      <categories>
        <category>REDIS</category>
      </categories>
      <tags>
        <tag>REDIS</tag>
        <tag>后端</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis09-缓存穿透/击穿/雪崩、分布式锁</title>
    <url>/2022/02/09/Redis09-%E7%BC%93%E5%AD%98%E7%A9%BF%E9%80%8F%C2%B7%E5%87%BB%E7%A9%BF%C2%B7%E9%9B%AA%E5%B4%A9%E3%80%81%E5%88%86%E5%B8%83%E5%BC%8F%E9%94%81/</url>
    <content><![CDATA[<h2 id="参考-Reference"><a href="#参考-Reference" class="headerlink" title="参考 Reference"></a>参考 Reference</h2><p>参考资料：《Redis开发与运维》</p>
<p>视频教程：<a href="https://www.bilibili.com/video/BV1Rv41177Af">https://www.bilibili.com/video/BV1Rv41177Af</a></p>
<p>参考笔记：<a href="https://zhangc233.github.io/2021/05/02/Redis/">https://zhangc233.github.io/2021/05/02/Redis/</a></p>
<p>参考笔记：<a href="https://blog.csdn.net/weixin_47872288/article/details/118410080">https://blog.csdn.net/weixin_47872288/article/details/118410080</a></p>
]]></content>
      <categories>
        <category>REDIS</category>
      </categories>
      <tags>
        <tag>REDIS</tag>
        <tag>后端</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaWeb01-Tomcat、Maven配置</title>
    <url>/2021/11/21/JavaWeb01-Tomcat%E3%80%81Maven%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[<h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><h3 id="web应用程序"><a href="#web应用程序" class="headerlink" title="web应用程序"></a>web应用程序</h3><h4 id="web组成"><a href="#web组成" class="headerlink" title="web组成"></a>web组成</h4><p>html, css, js</p>
<p>Jsp, servlet</p>
<p>java程序</p>
<p>jar包</p>
<p>配置文件（Properties）</p>
<h4 id="JSP-Servlet"><a href="#JSP-Servlet" class="headerlink" title="JSP/Servlet"></a>JSP/Servlet</h4><blockquote>
<p>sun公司主推的B/S架构，基于Java语言，可以承载三高（高并发，高可用，高性能）</p>
</blockquote>
<p>B/S：浏览器和服务器</p>
<p>C/S：客户端和服务器</p>
<h4 id="Web服务器"><a href="#Web服务器" class="headerlink" title="Web服务器"></a>Web服务器</h4><p>IIS：微软，ASP，WIndows自带</p>
<p>Tomcat：Apache基金会的核心项目，是开源免费的轻量级服务器</p>
<h4 id="Tomcat"><a href="#Tomcat" class="headerlink" title="Tomcat"></a>Tomcat</h4><table>
<thead>
<tr>
<th>文件夹</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>bin</td>
<td>启动，关闭的脚本文件</td>
</tr>
<tr>
<td>conf</td>
<td>配置</td>
</tr>
<tr>
<td>lib</td>
<td>依赖的jar包</td>
</tr>
<tr>
<td>logs</td>
<td>日志</td>
</tr>
<tr>
<td>webapps</td>
<td>存放网站内容</td>
</tr>
</tbody></table>
<h5 id="Docker安装"><a href="#Docker安装" class="headerlink" title="Docker安装"></a>Docker安装</h5><blockquote>
<p>vim docker-compose.yml</p>
</blockquote>
<pre><code>version: &#39;3.5&#39;
services:
  tomcat:
    restart: always
    image: tomcat
    container_name: tomcat
    ports:
      - 8080:8080
    volumes:
      - ./courage/ROOT/:/usr/local/tomcat/webapps/ROOT
    environment:
      TZ: Asia/Shanghai
</code></pre>
<p>启动：<code>docker-compose up -d</code></p>
<h5 id="本地安装"><a href="#本地安装" class="headerlink" title="本地安装"></a>本地安装</h5><p>下载地址：<a href="https://dlcdn.apache.org/tomcat/tomcat-9/v9.0.55/bin/apache-tomcat-9.0.55.zip">https://dlcdn.apache.org/tomcat/tomcat-9/v9.0.55/bin/apache-tomcat-9.0.55.zip</a></p>
<p>解压并复制到<code>/usr/local/ApacheTomcat/ </code></p>
<pre><code>cd /usr/local/ApacheTomcat/bin 
chmod u+x *.sh
./startup.sh
</code></pre>
<p>安装完成</p>
<pre><code>Using CATALINA_BASE:   /usr/local/ApacheTomcat
Using CATALINA_HOME:   /usr/local/ApacheTomcat
Using CATALINA_TMPDIR: /usr/local/ApacheTomcat/temp
Using JRE_HOME:        /Library/Java/JavaVirtualMachines/jdk1.8.0_181.jdk/Contents/Home
Using CLASSPATH:       /usr/local/ApacheTomcat/bin/bootstrap.jar:/usr/local/ApacheTomcat/bin/tomcat-juli.jar
Using CATALINA_OPTS:   
Tomcat started.
</code></pre>
<p>访问：<a href="http://localhost:8080/">http://localhost:8080</a></p>
<p>tomcat默认端口号：8080</p>
<p>mysql默认端口号：3306</p>
<p>http默认端口号：80</p>
<p>https默认端口号：443</p>
<pre><code>&lt;Connector port=&quot;8080&quot; protocol=&quot;HTTP/1.1&quot;
               connectionTimeout=&quot;20000&quot;
               redirectPort=&quot;8443&quot; /&gt;       
</code></pre>
<p>主机名：localhost</p>
<p>默认网站存放位置：webapps/ROOT/</p>
<pre><code>&lt;Host name=&quot;localhost&quot;  appBase=&quot;webapps&quot;
            unpackWARs=&quot;true&quot; autoDeploy=&quot;true&quot;&gt;        
</code></pre>
<p>网站结构</p>
<pre><code>- webapps        Tomacat服务器web目录
        - ROOT
            - project        项目目录
                - index.html    项目首页
                - WEB-INF
                    - classes        java程序
                    -lib                web应用依赖的jar包
                    -web.xml        网站配置文件
                - static            静态资源
            - css
            -js
            -img
        
</code></pre>
<h4 id="Http（超文本传输协议）"><a href="#Http（超文本传输协议）" class="headerlink" title="Http（超文本传输协议）"></a>Http（超文本传输协议）</h4><ol>
<li><p>请求行：GET，POST…</p>
</li>
<li><p>消息头</p>
<pre><code>Accept: text/html 数据类型
Accept-Encoding: gzip 编码
Accept-Language: 语言
Cache-Control: 缓存
Connection: Keep-Alive 请求完成后断开/保持连接
Host: 主机
</code></pre>
</li>
<li><p>响应体</p>
<pre><code>Refrush: 刷新频率
Locaton: 让网页重新定位
</code></pre>
</li>
<li><p>响应状态码</p>
<pre><code>200        请求成功
300        重定向
400        找不到资源
500        服务器错误
</code></pre>
</li>
</ol>
<h4 id="Maven-项目架构管理工具"><a href="#Maven-项目架构管理工具" class="headerlink" title="Maven 项目架构管理工具"></a>Maven 项目架构管理工具</h4><blockquote>
<p>作用：自动导入和配置jar包</p>
</blockquote>
<p>核心思想：约定大于配置（不要违反约束）</p>
<h5 id="下载安装"><a href="#下载安装" class="headerlink" title="下载安装"></a>下载安装</h5><p>下载地址：<a href="https://dlcdn.apache.org/maven/maven-3/3.8.3/binaries/apache-maven-3.8.3-bin.zip">https://dlcdn.apache.org/maven/maven-3/3.8.3/binaries/apache-maven-3.8.3-bin.zip</a></p>
<pre><code>sudo unzip apache-maven-3.8.3-bin.zip -d /usr/local
</code></pre>
<p>vim ~/.bash_profile</p>
<pre><code>export MAVEN_HOME=/usr/local/apache-maven-3.6.3
export PATH=$PATH:$MAVEN_HOME/bin
</code></pre>
<pre><code class="bash">source ~/.bash_profile 
</code></pre>
<p>验证是否安装成功</p>
<pre><code>mvn -v
</code></pre>
<h5 id="配置环境变量"><a href="#配置环境变量" class="headerlink" title="配置环境变量"></a>配置环境变量</h5><h5 id="配置阿里云镜像"><a href="#配置阿里云镜像" class="headerlink" title="配置阿里云镜像"></a>配置阿里云镜像</h5><blockquote>
<p>settings.xml</p>
</blockquote>
<pre><code>&lt;mirror&gt;
    &lt;id&gt;nexus-aliyun&lt;/id&gt;
    &lt;mirrorOf&gt;*,!jeecg,!jeecg-snapshots&lt;/mirrorOf&gt;
    &lt;name&gt;Nexus aliyun&lt;/name&gt;
    &lt;url&gt;http://maven.aliyun.com/nexus/content/groups/public&lt;/url&gt; 
&lt;/mirror&gt;
</code></pre>
<h5 id="配置本地仓库-localRepository"><a href="#配置本地仓库-localRepository" class="headerlink" title="配置本地仓库 localRepository"></a>配置本地仓库 localRepository</h5><p>建立仓库：</p>
<pre><code>&lt;localRepository&gt;\xxx\maven-repo&lt;/localRepository&gt;
</code></pre>
<h5 id="在IDEA项目中使用maven"><a href="#在IDEA项目中使用maven" class="headerlink" title="在IDEA项目中使用maven"></a>在IDEA项目中使用maven</h5><blockquote>
<p>New Project &gt; Maven &gt; create from archetype &gt; maven-archetype-webapp </p>
</blockquote>
<p>项目名</p>
<pre><code>GroupId:组id cn.itxiaoma
ArtifactId：项目id javaweb-01-maven
</code></pre>
<p>maven路径</p>
<pre><code>Maven home path: /usr/local/apache-maven-3.8.3
User settings file: /usr/local/apache-maven-3.8.3/conf/settings.xml
Local respository: /usr/local/apache-maven-3.8.3/maven-repo
</code></pre>
<p>等待项目初始化完成</p>
<pre><code>BUILD SUCCESS
</code></pre>
<p>IEDA中的maven配置</p>
<blockquote>
<p>Preference &gt; Build &gt; Build Tools &gt; Maven</p>
</blockquote>
<p>标记文件夹: 新建java, resources文件夹</p>
<pre><code>File &gt; Project Structure &gt; Modules =&gt;指定Sources和Resources
</code></pre>
<h5 id="在IDEA中配置Tomcat"><a href="#在IDEA中配置Tomcat" class="headerlink" title="在IDEA中配置Tomcat"></a>在IDEA中配置Tomcat</h5><blockquote>
<p>Run &gt; Edit Configurations &gt; + &gt; Tomcat Server - Local &gt; Application server:选择Tomcat目录</p>
<p>Deployment &gt; + &gt; javaweb-01-maven.war（指定访问目录）</p>
</blockquote>
<h5 id="Maven核心配置pom-xml"><a href="#Maven核心配置pom-xml" class="headerlink" title="Maven核心配置pom.xml"></a>Maven核心配置pom.xml</h5><blockquote>
<p>Maven的高级之处在于，他会帮你导入JAR包依赖的其他jar</p>
</blockquote>
<p>打包方式：jar java应用 war java web应用</p>
<pre><code>&lt;dependencies&gt;
  &lt;dependency&gt;
    &lt;groupId&gt;junit&lt;/groupId&gt;
    &lt;artifactId&gt;junit&lt;/artifactId&gt;
    &lt;version&gt;4.11&lt;/version&gt;
    &lt;scope&gt;test&lt;/scope&gt;
  &lt;/dependency&gt;
&lt;/dependencies&gt;
</code></pre>
<p>注：Maven由于约定大于配置，可能会无法导出资源</p>
<p>解决方案：在build中配置resources，防止资源导出失败</p>
<pre><code>&lt;!--在build中配置resources,来防止我们资源导出失败的问题--&gt;
&lt;build&gt;
    &lt;resources&gt;
        &lt;resource&gt;
            &lt;directory&gt;src/main/resources&lt;/directory&gt;
            &lt;excludes&gt;
                &lt;exclude&gt;**/*.properties&lt;/exclude&gt;
                &lt;exclude&gt;**/*.xml&lt;/exclude&gt;
            &lt;/excludes&gt;
            &lt;filtering&gt;false&lt;/filtering&gt;
        &lt;/resource&gt;
        &lt;resource&gt;
            &lt;directory&gt;src/main/java&lt;/directory&gt;
            &lt;includes&gt;
                &lt;include&gt;**/*.properties&lt;/include&gt;
                &lt;include&gt;**/*.xml&lt;/include&gt;
            &lt;/includes&gt;
            &lt;filtering&gt;false&lt;/filtering&gt;
        &lt;/resource&gt;
    &lt;/resources&gt;
&lt;/build&gt;
</code></pre>
<p>maven默认web项目中的web.xml替换版本（参照Tomact的/webapps/ROOT/WEB-INF/web.xml）</p>
<pre><code>&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;
&lt;web-app xmlns=&quot;http://xmlns.jcp.org/xml/ns/javaee&quot;
         xmlns:xsi=&quot;http://www.w3.org/2001/XMLSchema-instance&quot;
         xsi:schemaLocation=&quot;http://xmlns.jcp.org/xml/ns/javaee
                      http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd&quot;
         version=&quot;4.0&quot;
         metadata-complete=&quot;true&quot;&gt;
&lt;/web-app&gt;
</code></pre>
]]></content>
      <categories>
        <category>JAVA</category>
        <category>JavaWeb</category>
      </categories>
      <tags>
        <tag>后端</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaWeb02-Servlet、Cookie、Session</title>
    <url>/2021/11/22/JavaWeb02-Servlet%E3%80%81Cookie%E3%80%81Session/</url>
    <content><![CDATA[<h2 id="Servlet"><a href="#Servlet" class="headerlink" title="Servlet"></a>Servlet</h2><blockquote>
<p>Servlet是sun公司开发的处理动态web的技术</p>
<p>Servlet有两个默认的实现类：HttpServlet、GenericServlet</p>
</blockquote>
<p>开发Servlet程序的步骤：</p>
<ol>
<li>编写一个类，实现Servlet接口</li>
<li>把开发好的Java类部署到web服务器中</li>
</ol>
<p>实现了Servlet接口的java程序称为Servlet</p>
<h4 id="创建HelloServlet项目"><a href="#创建HelloServlet项目" class="headerlink" title="创建HelloServlet项目"></a>创建HelloServlet项目</h4><ol>
<li><p>构建一个普通的maven项目，删除src文件夹，这个空的工程作为maven主工程</p>
</li>
<li><p>引入servlet jsp依赖到pom.xml</p>
<pre><code>&lt;dependencies&gt;
  &lt;!--添加Servlet和JSP依赖--&gt;
  &lt;dependency&gt;
    &lt;groupId&gt;javax.servlet&lt;/groupId&gt;
    &lt;artifactId&gt;javax.servlet-api&lt;/artifactId&gt;
    &lt;version&gt;4.0.1&lt;/version&gt;
  &lt;/dependency
  &lt;!-- https://mvnrepository.com/artifact/javax.servlet.jsp/javax.servlet.jsp-api --&gt;
  &lt;dependency&gt;
    &lt;groupId&gt;javax.servlet.jsp&lt;/groupId&gt;
    &lt;artifactId&gt;javax.servlet.jsp-api&lt;/artifactId&gt;
    &lt;version&gt;2.3.3&lt;/version&gt;
  &lt;/dependency&gt;
&lt;/dependencies&gt;
</code></pre>
<p>或手动下载jar包到文件夹中</p>
<p>依赖下载地址:<a href="https://mvnrepository.com/">https://mvnrepository.com/</a></p>
</li>
<li><p>新建Module子模块servlet01（maven-archetype-webapp）</p>
<p>父项目中的java子项目可以直接使用，反之不可以</p>
<p> 问题1:子项目pom中没有parent标签</p>
<p>解决：在子项目的pom.xml中手动添加parent标签并刷新maven</p>
</li>
<li><pre><code>&lt;parent&gt;
  &lt;groupId&gt;cn.itxiaoma&lt;/groupId&gt;
  &lt;artifactId&gt;HelloMaven&lt;/artifactId&gt;
  &lt;version&gt;1.0-SNAPSHOT&lt;/version&gt;
&lt;/parent&gt;
</code></pre>
<p>问题2：子项目pom文件有一条横线并变灰</p>
<p>解决：file –&gt;setting–&gt;maven–&gt;Ignored Files 将清单中对应项目的pom.xml文件取消选中</p>
</li>
<li><p>将servlet01中的web.xml改为最新，在main目录下新建java和resources</p>
<pre><code>&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;
&lt;web-app xmlns=&quot;http://xmlns.jcp.org/xml/ns/javaee&quot;
         xmlns:xsi=&quot;http://www.w3.org/2001/XMLSchema-instance&quot;
         xsi:schemaLocation=&quot;http://xmlns.jcp.org/xml/ns/javaee
                      http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd&quot;
         version=&quot;4.0&quot;
         metadata-complete=&quot;true&quot;&gt;
&lt;/web-app&gt;
</code></pre>
</li>
<li><p>在java目录下新建package包cn.itxiaoma.servlet</p>
<p>问题：右键New没有package选项</p>
<p>解决：右键 Mark Directory as &gt; Sources Root</p>
</li>
<li><p>在包下新建java类HelloServlet，并实现Servlet接口(继承HttpServlet)</p>
<pre><code>package cn.itxiaoma.servlet;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.io.IOException;
import java.io.PrintWriter;

public class HelloServlet extends HttpServlet &#123;
    //get和post只是请求实现的不同方式，业务逻辑一样，因此可以互相调用
    @Override
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;
        PrintWriter printWriter = resp.getWriter();
        printWriter.print(&quot;Hello Servlet&quot;);
    &#125;
    @Override
    protected void doPost(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;
        doGet(req, resp);
    &#125;
&#125;
</code></pre>
</li>
<li><p>编写Servlet的映射</p>
<p>浏览器连接的是web服务，因此需要在web服务中注册我们写的Servlet，并配置浏览器能够访问的路径</p>
<pre><code>&lt;!--  注册Servlet--&gt;
&lt;servlet&gt;
  &lt;servlet-name&gt;hello&lt;/servlet-name&gt;
  &lt;servlet-class&gt;cn.itxiaoma.servlet.HelloServlet&lt;/servlet-class&gt;
&lt;/servlet&gt;
&lt;!--  Servlet的请求路径--&gt;
&lt;servlet-mapping&gt;
  &lt;servlet-name&gt;hello&lt;/servlet-name&gt;
  &lt;url-pattern&gt;/hello&lt;/url-pattern&gt;
&lt;/servlet-mapping&gt;
</code></pre>
</li>
<li><p>配置Tomcat</p>
<p>Run &gt; Edit Configurations &gt; + &gt; Tomcat Server - Local &gt; Application server:选择Tomcat目录</p>
<p>Deployment &gt; + &gt; servlet01:war exploed</p>
<p>Application Context: /servlet01_war_exploded</p>
</li>
<li><p>启动测试</p>
</li>
</ol>
<p>   <a href="http://localhost:8081/servlet01_war_exploded/hello">http://localhost:8081/servlet01_war_exploded/hello</a></p>
<h4 id="Servlet原理"><a href="#Servlet原理" class="headerlink" title="Servlet原理"></a>Servlet原理</h4><ol>
<li>浏览器发送http请求(service)到web容器(Tomcat)</li>
<li>首次访问的请求会加载Servlet类到内存</li>
<li>web容器生成请求(Request)和响应(Response)两个对象，并调用Servlet的service方法</li>
<li>Request会从service拿到请求，并执行我们重写的处理方法，得到响应信息传给Response</li>
<li>web容器读取响应信息传回浏览器</li>
</ol>
<h4 id="Servlet-Mapping问题"><a href="#Servlet-Mapping问题" class="headerlink" title="Servlet Mapping问题"></a>Servlet Mapping问题</h4><p>指定映射路径</p>
<pre><code>&lt;servlet-mapping&gt;
  &lt;servlet-name&gt;hello&lt;/servlet-name&gt;
  &lt;url-pattern&gt;/hello&lt;/url-pattern&gt;
&lt;/servlet-mapping&gt;
</code></pre>
<p>多映射路径</p>
<pre><code>&lt;servlet-mapping&gt;
  &lt;servlet-name&gt;hello&lt;/servlet-name&gt;
  &lt;url-pattern&gt;/hello/*&lt;/url-pattern&gt;
&lt;/servlet-mapping&gt;
</code></pre>
<p>默认请求路径(会覆盖首页)</p>
<pre><code>&lt;servlet-mapping&gt;
  &lt;servlet-name&gt;hello&lt;/servlet-name&gt;
  &lt;url-pattern&gt;/*&lt;/url-pattern&gt;
&lt;/servlet-mapping&gt;
</code></pre>
<p>指定后缀/前缀</p>
<pre><code>&lt;servlet-mapping&gt;
  &lt;servlet-name&gt;hello&lt;/servlet-name&gt;
  &lt;url-pattern&gt;*.test&lt;/url-pattern&gt;
&lt;/servlet-mapping&gt;
</code></pre>
<p>注：自定义后缀/前缀 * 前不能有映射路径</p>
<h4 id="自定义异常处理ErrorServlet"><a href="#自定义异常处理ErrorServlet" class="headerlink" title="自定义异常处理ErrorServlet"></a>自定义异常处理ErrorServlet</h4><blockquote>
<p>cn/itxiaoma/servlet/ErrorServlet.java</p>
</blockquote>
<pre><code>package cn.itxiaoma.servlet;
import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.io.IOException;
import java.io.PrintWriter;
public class ErrorServlet extends HttpServlet &#123;
    @Override
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;
        resp.setContentType(&quot;text/html&quot;);
        resp.setCharacterEncoding(&quot;utf-8&quot;);
        PrintWriter pw = resp.getWriter();
        pw.print(&quot;&lt;h1&gt;404&lt;/h1&gt;&quot;);
    &#125;
    @Override
    protected void doPost(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;
        doGet(req, resp);
    &#125;
&#125;
</code></pre>
<blockquote>
<p>src/main/webapp/WEB-INF/web.xml</p>
</blockquote>
<pre><code>&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;
&lt;web-app xmlns=&quot;http://xmlns.jcp.org/xml/ns/javaee&quot;
         xmlns:xsi=&quot;http://www.w3.org/2001/XMLSchema-instance&quot;
         xsi:schemaLocation=&quot;http://xmlns.jcp.org/xml/ns/javaee
                      http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd&quot;
         version=&quot;4.0&quot;
         metadata-complete=&quot;true&quot;&gt;
  &lt;servlet&gt;
    &lt;servlet-name&gt;hello&lt;/servlet-name&gt;
    &lt;servlet-class&gt;cn.itxiaoma.servlet.HelloServlet&lt;/servlet-class&gt;
  &lt;/servlet&gt;
  &lt;servlet-mapping&gt;
    &lt;servlet-name&gt;hello&lt;/servlet-name&gt;
    &lt;url-pattern&gt;/hello&lt;/url-pattern&gt;
  &lt;/servlet-mapping&gt;

  &lt;servlet&gt;
    &lt;servlet-name&gt;error&lt;/servlet-name&gt;
    &lt;servlet-class&gt;cn.itxiaoma.servlet.ErrorServlet&lt;/servlet-class&gt;
  &lt;/servlet&gt;
  &lt;servlet-mapping&gt;
    &lt;servlet-name&gt;error&lt;/servlet-name&gt;
    &lt;url-pattern&gt;/*&lt;/url-pattern&gt;
  &lt;/servlet-mapping&gt;
&lt;/web-app&gt;
</code></pre>
<p>优先级问题：制定了固有的映射路径优先级最高，找不到再走默认的映射</p>
<h4 id="ServletContext对象"><a href="#ServletContext对象" class="headerlink" title="ServletContext对象"></a>ServletContext对象</h4><blockquote>
<p> web容器在启动的时候，它会为每个web程序创建一个ServletContext对象，代表当前的web应用</p>
</blockquote>
<h5 id="1-共享数据"><a href="#1-共享数据" class="headerlink" title="1. 共享数据"></a>1. 共享数据</h5><blockquote>
<p>在一个Servlet中保存的数据，可以在其他Servlet中获取</p>
</blockquote>
<p>web.xml</p>
<pre><code>&lt;servlet&gt;
  &lt;servlet-name&gt;get&lt;/servlet-name&gt;
  &lt;servlet-class&gt;cn.itxiaoma.servlet.GetServlet&lt;/servlet-class&gt;
&lt;/servlet&gt;
&lt;servlet-mapping&gt;
  &lt;servlet-name&gt;get&lt;/servlet-name&gt;
  &lt;url-pattern&gt;/get&lt;/url-pattern&gt;
&lt;/servlet-mapping&gt;
</code></pre>
<p>HelloServlet.java</p>
<pre><code>package cn.itxiaoma.servlet;
import javax.servlet.ServletContext;
import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.io.IOException;
public class HelloServlet extends HttpServlet &#123;
    //get和post只是请求实现的不同方式，业务逻辑一样，因此可以互相调用
    @Override
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;
        ServletContext context= this.getServletContext();
        String username = &quot;Apple&quot;;
        context.setAttribute(&quot;username&quot;, username);
        System.out.println(username);
    &#125;
    @Override
    protected void doPost(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;
        doGet(req, resp);
    &#125;
&#125;
</code></pre>
<p>GetServlet.java</p>
<pre><code>package cn.itxiaoma.servlet;
import javax.servlet.ServletContext;
import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.io.IOException;
import java.io.PrintWriter;
public class GetServlet extends HttpServlet &#123;
    //get和post只是请求实现的不同方式，业务逻辑一样，因此可以互相调用
    @Override
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;
        ServletContext context = this.getServletContext();
        PrintWriter printWriter = resp.getWriter();
        resp.setContentType(&quot;text/html&quot;);
        resp.setCharacterEncoding(&quot;utf-8&quot;);
        printWriter.print(&quot;Username:&quot; + context.getAttribute(&quot;username&quot;));
    &#125;
    @Override
    protected void doPost(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;
        doGet(req, resp);
    &#125;
&#125;
</code></pre>
<h5 id="2-获取初始化参数"><a href="#2-获取初始化参数" class="headerlink" title="2. 获取初始化参数"></a>2. 获取初始化参数</h5><p>web.xml</p>
<pre><code>&lt;context-param&gt;
  &lt;param-name&gt;url&lt;/param-name&gt;
  &lt;param-value&gt;jdbc:mysql://localhost:3306/testdb&lt;/param-value&gt;
&lt;/context-param&gt;
</code></pre>
<p>HelloServlet.java</p>
<pre><code>package cn.itxiaoma.servlet;
import javax.servlet.ServletContext;
import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.io.IOException;
import java.io.PrintWriter;
public class HelloServlet extends HttpServlet &#123;
    //get和post只是请求实现的不同方式，业务逻辑一样，因此可以互相调用
    @Override
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;
        ServletContext context= this.getServletContext();
        String url = context.getInitParameter(&quot;url&quot;);
        resp.getWriter().print(url);
    &#125;
    @Override
    protected void doPost(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;
        doGet(req, resp);
    &#125;
&#125;
</code></pre>
<h5 id="3-请求转发"><a href="#3-请求转发" class="headerlink" title="3. 请求转发"></a>3. 请求转发</h5><pre><code>ServletContext context= this.getServletContext();
RequestDispatcher requestDispatcher = context.getRequestDispatcher(&quot;/gp&quot;);//转发的请求路径
requestDispatcher.forward(req, resp);
</code></pre>
<h5 id="4-读取资源文件"><a href="#4-读取资源文件" class="headerlink" title="4. 读取资源文件"></a>4. 读取资源文件</h5><p>resources文件夹下新建db.properties（会打包到classes路径下，称为classpath）</p>
<pre><code>username=root
password=123456
</code></pre>
<p>读取资源文件</p>
<pre><code>ServletContext context= this.getServletContext();
InputStream inputStream = context.getResourceAsStream(&quot;/WEB-INF/classes/db.properties&quot;);
Properties properties = new Properties();
properties.load(inputStream);
String username = properties.getProperty(&quot;username&quot;);
String password = properties.getProperty(&quot;password&quot;);
resp.getWriter().print(username + password);
</code></pre>
<h4 id="HttpServletResponse"><a href="#HttpServletResponse" class="headerlink" title="HttpServletResponse"></a>HttpServletResponse</h4><blockquote>
<p>web服务器接收到客户端的http请求，针对这个请求，分表创建一个代表请求的HttpServletRequest对象，和一个代表响应的HttpServletResponse对象</p>
</blockquote>
<p>javax/servlet/ServletResponse.java</p>
<h6 id="负责向浏览器发送数据的方法"><a href="#负责向浏览器发送数据的方法" class="headerlink" title="负责向浏览器发送数据的方法"></a>负责向浏览器发送数据的方法</h6><pre><code>public ServletOutputStream getOutputStream() throws IOException;
public PrintWriter getWriter() throws IOException;
</code></pre>
<h6 id="负责向浏览器发送响应头的方法"><a href="#负责向浏览器发送响应头的方法" class="headerlink" title="负责向浏览器发送响应头的方法"></a>负责向浏览器发送响应头的方法</h6><pre><code>public void setCharacterEncoding(String charset);
public void setContentLength(int len);
public void setContentLengthLong(long len);
public void setContentType(String type);
</code></pre>
<h5 id="常见应用"><a href="#常见应用" class="headerlink" title="常见应用"></a>常见应用</h5><ol>
<li><p>向浏览器输出消息</p>
</li>
<li><p>下载文件</p>
<ol>
<li>获取下载文件路径，文件名</li>
<li>获取下载文件的输入流</li>
<li>创建缓冲区</li>
<li>获取OutputStream对象</li>
<li>将FileOutputStream流写入Buffer缓冲区</li>
<li>使用OutputStream将缓冲区中的数据输出到客户端 </li>
</ol>
<pre><code>String realPath = &quot;/Users/4wheels/Downloads/test.jpg&quot;;
String filename = realPath.substring(realPath.lastIndexOf(&quot;/&quot;) + 1);
resp.setHeader(&quot;Content-Disposition&quot;, &quot;attachment;filename=&quot; + URLEncoder.encode(filename, &quot;UTF-8&quot;));
//获取文件输入流
FileInputStream inputStream = new FileInputStream(realPath);
//缓冲区
int len = 0;
byte[] buffer = new byte[1024];
ServletOutputStream servletOutputStream = resp.getOutputStream();
while ((len = inputStream.read(buffer)) &gt; 0) &#123;
    servletOutputStream.write(buffer, 0, len);
&#125;
inputStream.close();
servletOutputStream.close();
</code></pre>
</li>
<li><p>验证码功能</p>
<pre><code>protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;
    //浏览器3秒自动刷新
    resp.setHeader(&quot;refresh&quot;, &quot;3&quot;);
    //在内存中创建图片
    BufferedImage image = new BufferedImage(80, 20, BufferedImage.TYPE_INT_RGB);
    //绘制图片
    Graphics2D g = (Graphics2D) image.getGraphics();
    g.setColor(Color.white);
    g.fillRect(0, 0, 80, 20);//绘制填色矩形
    g.setColor(Color.BLACK);
    g.setFont(new Font(null, Font.BOLD, 20));
    g.drawString(makeNum(), 2, 20);
    resp.setContentType(&quot;image/jpeg&quot;);
    resp.setDateHeader(&quot;expires&quot;, -1);//浏览器不缓存
    resp.setHeader(&quot;Cache-COntrol&quot;, &quot;no-cache&quot;);
    resp.setHeader(&quot;Pragma&quot;, &quot;no-cache&quot;);
    ImageIO.write(image, &quot;jpg&quot;, resp.getOutputStream());
&#125;

private String makeNum() &#123;
    Random random = new Random();
    String num = random.nextInt(9999999) + &quot;&quot;;
    StringBuffer sb = new StringBuffer();
    for (int i = 0; i &lt; 7 - num.length(); i++) &#123;
        sb.append(&quot;0&quot;);
    &#125;
    num = sb.toString() + num;
    return num;
&#125;
</code></pre>
</li>
<li><p>重定向</p>
<pre><code>resp.sendRedirect(&quot;/test&quot;);
</code></pre>
</li>
</ol>
<h4 id="HttpServletRequest"><a href="#HttpServletRequest" class="headerlink" title="HttpServletRequest"></a>HttpServletRequest</h4><h5 id="处理登录请求"><a href="#处理登录请求" class="headerlink" title="处理登录请求"></a>处理登录请求</h5><pre><code>&lt;html&gt;
&lt;body&gt;
&lt;form action=&quot;$&#123;pageContext.request.contextPath&#125;/login&quot; method=&quot;post&quot;&gt;
    Username&lt;input type=&quot;text&quot; name=&quot;username&quot;/&gt;&lt;br&gt;
    Password&lt;input type=&quot;password&quot; name=&quot;password&quot;/&gt;&lt;br&gt;
    Hobby&lt;input type=&quot;checkbox&quot; name=&quot;hobby&quot; value=&quot;1&quot;/&gt;
    &lt;input type=&quot;checkbox&quot; name=&quot;hobby&quot; value=&quot;2&quot;/&gt;
    &lt;input type=&quot;checkbox&quot; name=&quot;hobby&quot; value=&quot;3&quot;/&gt;
    &lt;input type=&quot;submit&quot;/&gt;
&lt;/form&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>web.xml</p>
<pre><code>&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;
&lt;web-app xmlns=&quot;http://xmlns.jcp.org/xml/ns/javaee&quot;
         xmlns:xsi=&quot;http://www.w3.org/2001/XMLSchema-instance&quot;
         xsi:schemaLocation=&quot;http://xmlns.jcp.org/xml/ns/javaee
                      http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd&quot;
         version=&quot;4.0&quot;
         metadata-complete=&quot;true&quot;&gt;
  &lt;servlet&gt;
    &lt;servlet-name&gt;login&lt;/servlet-name&gt;
    &lt;servlet-class&gt;cn.itxiaoma.servlet.LoginServlet&lt;/servlet-class&gt;
  &lt;/servlet&gt;
  &lt;servlet-mapping&gt;
    &lt;servlet-name&gt;login&lt;/servlet-name&gt;
    &lt;url-pattern&gt;/login&lt;/url-pattern&gt;
  &lt;/servlet-mapping&gt;
&lt;/web-app&gt;
</code></pre>
<p>LoginServlet.java</p>
<pre><code>package cn.itxiaoma.servlet;
import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.io.IOException;
import java.util.Arrays;
public class LoginServlet extends HttpServlet &#123;
    //get和post只是请求实现的不同方式，业务逻辑一样，因此可以互相调用
    @Override
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;
        req.setCharacterEncoding(&quot;utf-8&quot;);
        resp.setCharacterEncoding(&quot;utf-8&quot;);
        String username = req.getParameter(&quot;username&quot;);
        String password = req.getParameter(&quot;password&quot;);
        String[] hobby = req.getParameterValues(&quot;hobby&quot;);
        System.out.println(username);
        System.out.println(password);
        System.out.println(Arrays.toString(hobby));
//        req.getRequestDispatcher(&quot;/success.jsp&quot;).forward(req, resp);
    &#125;
    @Override
    protected void doPost(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;
        doGet(req, resp);
    &#125;
&#125;
</code></pre>
<h2 id="Cookie"><a href="#Cookie" class="headerlink" title="Cookie"></a>Cookie</h2><ol>
<li>cookie只能保存字符串，不能保存对象</li>
<li>一个cookie只能保存一个信息，最大4kb</li>
<li>一个web站点可以给浏览器发送多个cookie，一个站点最多20个</li>
<li>浏览器最多存300个左右cookie</li>
<li>cookie不设置有效期或设为0，关闭浏览器自动失效   </li>
</ol>
<pre><code>req.setCharacterEncoding(&quot;utf-8&quot;);
resp.setCharacterEncoding(&quot;utf-8&quot;);
resp.setContentType(&quot;text/html;charset=utf-8&quot;);
PrintWriter printWriter = resp.getWriter();
Cookie[] cookies = req.getCookies();
if (cookies != null) &#123;
    printWriter.print(&quot;Last Time:&quot;);
    for (int i = 0; i &lt; cookies.length; i++) &#123;
        Cookie cookie = cookies[i];
        if (cookie.getName().equals(&quot;lastLoginTime&quot;)) &#123;
            long lastLoginTime = Long.parseLong(cookie.getValue());
            Date date = new Date(lastLoginTime);
            printWriter.write(date.toLocaleString());
        &#125;
    &#125;
&#125; else &#123;
    printWriter.print(&quot;First Visit.&quot;);
&#125;
Cookie cookie = new Cookie(&quot;lastLoginTime&quot;, System.currentTimeMillis()+&quot;&quot;);
resp.addCookie(cookie);
</code></pre>
<h2 id="Session"><a href="#Session" class="headerlink" title="Session"></a>Session</h2><blockquote>
<p>服务器会给每个用户（浏览器）创建一个Session，只有浏览器没有关闭，session就存在</p>
</blockquote>
<p>Session和Cookie的区别：Cookie由客户端创建在浏览器保存，Session在服务器端创建和保存</p>
<p>Session和ServletContext区别：ServletContext作用域是全局（Web应用），Session作用域是会话（浏览器）</p>
<pre><code>&lt;servlet&gt;
  &lt;servlet-name&gt;setSession&lt;/servlet-name&gt;
  &lt;servlet-class&gt;cn.itxiaoma.servlet.SetSession&lt;/servlet-class&gt;
&lt;/servlet&gt;
&lt;servlet-mapping&gt;
  &lt;servlet-name&gt;setSession&lt;/servlet-name&gt;
  &lt;url-pattern&gt;/set&lt;/url-pattern&gt;
&lt;/servlet-mapping&gt;
&lt;servlet&gt;
  &lt;servlet-name&gt;getSession&lt;/servlet-name&gt;
  &lt;servlet-class&gt;cn.itxiaoma.servlet.GetSession&lt;/servlet-class&gt;
&lt;/servlet&gt;
&lt;servlet-mapping&gt;
  &lt;servlet-name&gt;getSession&lt;/servlet-name&gt;
  &lt;url-pattern&gt;/get&lt;/url-pattern&gt;
&lt;/servlet-mapping&gt;
&lt;servlet&gt;
  &lt;servlet-name&gt;delSession&lt;/servlet-name&gt;
  &lt;servlet-class&gt;cn.itxiaoma.servlet.DelSession&lt;/servlet-class&gt;
&lt;/servlet&gt;
&lt;servlet-mapping&gt;
  &lt;servlet-name&gt;delSession&lt;/servlet-name&gt;
  &lt;url-pattern&gt;/del&lt;/url-pattern&gt;
&lt;/servlet-mapping&gt;
&lt;!--  设置session默认失效时间--&gt;
&lt;session-config&gt;
  &lt;!-- 15分钟后自动失效--&gt;
  &lt;session-timeout&gt;15&lt;/session-timeout&gt;
&lt;/session-config&gt;
</code></pre>
<p>SetSession.java</p>
<pre><code>req.setCharacterEncoding(&quot;utf-8&quot;);
resp.setCharacterEncoding(&quot;utf-8&quot;);
resp.setContentType(&quot;text/html;charset=utf-8&quot;);
PrintWriter printWriter = resp.getWriter();
HttpSession session = req.getSession();
session.setAttribute(&quot;name&quot;, &quot;test&quot;);
String sessionId = session.getId();
if (session.isNew()) &#123;
    printWriter.write(&quot;Session创建成功，Id：&quot; + sessionId);
&#125; else &#123;
    printWriter.write(&quot;Session已存在，Id：&quot; + sessionId);
&#125;
</code></pre>
<p>GetSession.java</p>
<pre><code>req.setCharacterEncoding(&quot;utf-8&quot;);
resp.setCharacterEncoding(&quot;utf-8&quot;);
resp.setContentType(&quot;text/html;charset=utf-8&quot;);
PrintWriter printWriter = resp.getWriter();
HttpSession session = req.getSession();
String sessionId = session.getId();
String sessionName = (String) session.getAttribute(&quot;name&quot;);
printWriter.write(&quot;Session Id:&quot; + sessionId);
printWriter.write(&quot;Session Name:&quot; + sessionName);
</code></pre>
<p>DelSession.java</p>
<pre><code>HttpSession session = req.getSession();
//删除指定session
session.removeAttribute(&quot;name&quot;);
//手动注销session
session.invalidate();
</code></pre>
]]></content>
      <categories>
        <category>JAVA</category>
        <category>JavaWeb</category>
      </categories>
      <tags>
        <tag>后端</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis07-持久化</title>
    <url>/2022/02/07/Redis07-%E6%8C%81%E4%B9%85%E5%8C%96/</url>
    <content><![CDATA[<h2 id="12-持久化"><a href="#12-持久化" class="headerlink" title="12.持久化"></a>12.持久化</h2><ul>
<li>RDB（Redis DataBase）</li>
<li>AOF（Append Of File）</li>
</ul>
<h3 id="RDB"><a href="#RDB" class="headerlink" title="RDB"></a>RDB</h3><blockquote>
<p>在指定的<strong>时间间隔</strong>内将内存中的<strong>数据集快照</strong>（Snapshot）写入磁盘，恢复时将快照文件直接读到内存中</p>
</blockquote>
<h4 id="持久化流程"><a href="#持久化流程" class="headerlink" title="持久化流程"></a>持久化流程</h4><ul>
<li>Redis会单独创建（fork）一个子进程来进行持久化</li>
<li>Redis会先将数据写入到一个临时文件中，等持久化过程结束了，在用这个临时文件替换上次持久化好的文件。</li>
<li>整个过程不需要主进程IO操作，确保了极高的性能</li>
<li>如果需要大规模数据恢复，且对于数据完整性不敏感，RDB比AOF方式高效</li>
<li>缺点是最后一次持久化的数据可能丢失</li>
</ul>
<h4 id="fork进程"><a href="#fork进程" class="headerlink" title="fork进程"></a>fork进程</h4><ul>
<li>fork的作用是复制一个与当前进程一样的进程。新进程的所有数据（变量、环境变量、程序计数器等）数值都和原进程一致，但是一个全新的进程，并作为原进程的子进程</li>
<li>在Linux程序中，fork()会产生一个和父进程完全相同的子进程，但子进程在此后会exec系统调用，出于效率考虑，引入了“写时复制技术”</li>
<li><strong>一般情况父进程和子进程共用同一段物理内存</strong>，只有进程空间的各段内容要发生变化时，才会将父进程的内容复制一份给子进程</li>
</ul>
<p><img src="https://tva1.sinaimg.cn/large/e6c9d24ely1h0tys53ifrj20cy08n74h.jpg" alt="image-20220401092922689"></p>
<h4 id="配置文件-SNAPSHOTTING"><a href="#配置文件-SNAPSHOTTING" class="headerlink" title="配置文件 SNAPSHOTTING"></a>配置文件 SNAPSHOTTING</h4><p>在redis.conf中配置文件名称，默认为dump.db</p>
<pre><code>dbfilename dump.rdb                                    #持久化文件     
dir ./                                                            #在启动目录中生成rdb文件    
stop-writes-on-bgsave-error yes     #当Redis无法写入磁盘时，关闭写操作（推荐yes）
rdbcompression yes                                     #对于存储到磁盘中的快照，是否进行压缩（推荐yes）
rdbchecksum yes                                         #检查数据完整性（+10%性能消耗），希望最大性能时可关闭此功能
</code></pre>
<h5 id="Save"><a href="#Save" class="headerlink" title="Save"></a>Save</h5><p>格式：save 秒数 写操作次数</p>
<p>解释：在x秒内最低写入y次，则执行持久化操作</p>
<p>禁用：不设置save或传入空字符串</p>
<pre><code>save 20 3 #20秒内至少写入3次
</code></pre>
<ul>
<li>save ：save 时只管保存，其它不管，全部阻塞。手动保存，不建议。</li>
<li><strong>bgsave：Redis 会在后台异步进行快照操作，</strong> <strong>快照同时还可以响应客户端请求。</strong></li>
<li>可以通过 lastsave 命令获取最后一次成功执行快照的时间。</li>
</ul>
<h5 id="flushall"><a href="#flushall" class="headerlink" title="flushall"></a>flushall</h5><p>flushall命令也会产生dump.rdb文件，但里面是空的，无意义</p>
<h5 id="动态停止RDB"><a href="#动态停止RDB" class="headerlink" title="动态停止RDB"></a>动态停止RDB</h5><p>save 后给空值，表示禁用保存策略</p>
<pre><code>redis-cli config set save &quot;&quot;
</code></pre>
<h4 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h4><p>优点：</p>
<ul>
<li>适合大规模的数据恢复</li>
<li>对数据完整性和一致性要求不高更适合使用</li>
<li>节省磁盘空间</li>
<li>恢复速度快</li>
</ul>
<p>缺点：</p>
<ul>
<li>Fork的时候，内存中的数据被克隆了一份，大致2倍的膨胀性需要考虑</li>
<li>数据庞大时比较消耗性能</li>
<li>因为定时备份，Redis意外down掉的话，会丢失最后一次快照后的所有修改。</li>
</ul>
<h4 id="备份恢复"><a href="#备份恢复" class="headerlink" title="备份恢复"></a>备份恢复</h4><p>备份</p>
<ul>
<li>因为是临时文件，如果redis关闭之后，rdb的东西就会不见</li>
<li>需要通过mv更改其名字手动备份，如<code>mv dump.rdb d.rdb</code></li>
</ul>
<p>恢复：</p>
<ul>
<li>在Redis启动之前，将备份文件改回dump.rdb，启动Redis时， 数据会直接加载</li>
</ul>
<h3 id="AOF"><a href="#AOF" class="headerlink" title="AOF"></a>AOF</h3><ul>
<li>以日志的形式来记录每个写操作（增量保存），将redis执行过的所有写指令记录下来，只许追加文件但不可以改写文件</li>
<li>redis启动时会读取该文件重新构建数据（根据文件内容将写指令从前到后执行一次，以完成数据恢复）</li>
<li>AOF默认不开启，开启时保存路径和RDB相同</li>
<li>如果AOF和RDB同时存在的时候,Redis会优先使用从AOF文件来还原数据库状态</li>
</ul>
<h4 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h4><pre><code>appendonly yes
appendfilename &quot;appendonly.aof&quot;
</code></pre>
<ul>
<li>AOF默认不开启，开启时保存路径和RDB相同</li>
<li>如果AOF和RDB同时存在的时候，Redis会优先使用从AOF文件来还原数据库状态</li>
</ul>
<p>如果直接在日志添加一些无法识别的数据，启动redis会启动不了<br>可以通过<code>redis-check-aof--fix appendonly.aof</code>进行恢复</p>
<h5 id="同步频率"><a href="#同步频率" class="headerlink" title="同步频率"></a>同步频率</h5><p>appendfsync always 始终同步，每次Redis的写入都会立刻记入日志；性能较差但数据完整性比较好<br>appendfsync everysec 每秒同步，每秒记入日志一次，如果宕机，本秒的数据可能丢失。<br>appendfsync no redis不主动进行同步，把同步时机交给操作系统</p>
<h4 id="备份恢复-1"><a href="#备份恢复-1" class="headerlink" title="备份恢复"></a>备份恢复</h4><p>AOF 的备份机制和性能虽然和 RDB 不同，但是备份和恢复的操作同 RDB 一样，都是拷贝备份文件，需要恢复时再拷贝到 Redis 工作目录下，启动系统即加载。</p>
<h5 id="正常恢复"><a href="#正常恢复" class="headerlink" title="正常恢复"></a>正常恢复</h5><ul>
<li>修改默认的 appendonly no，改为 yes。</li>
<li>将有数据的 aof 文件复制一份保存到对应目录 (查看目录：config get dir)。</li>
<li>恢复：重启 redis 然后重新加载。</li>
</ul>
<h5 id="异常恢复"><a href="#异常恢复" class="headerlink" title="异常恢复"></a>异常恢复</h5><ul>
<li>修改默认的 appendonly no，改为 yes。</li>
<li>如遇到 <strong>AOF 文件损坏</strong>，通过 /usr/local/bin/ <strong>redis-check-aof–fix appendonly.aof</strong> 进行恢复。</li>
<li>备份被写坏的 AOF 文件。</li>
<li>恢复：重启 redis，然后重新加载。</li>
</ul>
<h4 id="重写-Rewrite"><a href="#重写-Rewrite" class="headerlink" title="重写 Rewrite"></a>重写 <strong>Rewrite</strong></h4><p>AOF 采用文件追加方式，文件会越来越大为避免出现此种情况，新增了重写机制，当 AOF 文件的大小超过所设定的阈值时，Redis 就会启动 AOF 文件的内容压缩，只保留可以恢复数据的最小指令集，可以使用命令 bgrewriteaof。</p>
<p>AOF 文件持续增长而过大时，会 fork 出一条新进程来将文件重写 (也是先写临时文件最后再 rename)，redis4.0 版本后的重写，是指把 rdb 的快照，以二进制的形式附在新的 aof 头部，作为已有的历史数据，替换掉原来的流水账操作。</p>
<h5 id="no-appendfsync-on-rewrite"><a href="#no-appendfsync-on-rewrite" class="headerlink" title="no-appendfsync-on-rewrite"></a>no-appendfsync-on-rewrite</h5><ul>
<li>如果 no-appendfsync-on-rewrite=yes ，不写入 aof 文件只写入缓存，用户请求不会阻塞，但是在这段时间如果宕机会丢失这段时间的缓存数据。（降低数据安全性，提高性能）</li>
<li>如果 no-appendfsync-on-rewrite=no，还是会把数据往磁盘里刷，但是遇到重写操作，可能会发生阻塞。（数据安全，但是性能降低）</li>
</ul>
<h5 id="触发机制"><a href="#触发机制" class="headerlink" title="触发机制"></a>触发机制</h5><p>Redis 会记录上次重写时的 AOF 大小，默认配置是当 AOF 文件大小是上次 rewrite 后大小的一倍且文件大于 64M 时触发。</p>
<p>重写虽然可以节约大量磁盘空间，减少恢复时间。但是每次重写还是有一定的负担的，因此设定 Redis 要满足一定条件才会进行重写。</p>
<ul>
<li>auto-aof-rewrite-percentage：设置重写的基准值，文件达到 100% 时开始重写（文件是原来重写后文件的 2 倍时触发）。</li>
<li>auto-aof-rewrite-min-size：设置重写的基准值，最小文件 64MB。达到这个值开始重写。</li>
<li>系统载入时或者上次重写完毕时，Redis 会记录此时 AOF 大小，设为 base_size,</li>
<li>如果 Redis 的 AOF 当前大小 &gt;= base_size +base_size*100% (默认) 且当前大小 &gt;=64mb (默认) 的情况下，Redis 会对 AOF 进行重写。</li>
<li>例如：文件达到 70MB 开始重写，降到 50MB，下次什么时候开始重写？100MB</li>
</ul>
<h5 id="重写流程"><a href="#重写流程" class="headerlink" title="重写流程"></a>重写流程</h5><p><img src="https://tva1.sinaimg.cn/large/e6c9d24ely1h0u9wy5ubtj20gb0ewmy0.jpg" alt="image-20220401155437546"></p>
<ol>
<li>bgrewriteaof 触发重写，判断是否当前有 bgsave 或 bgrewriteaof 在运行，如果有，则等待该命令结束后再继续执行；</li>
<li>主进程 fork 出子进程执行重写操作，保证主进程不会阻塞；</li>
<li>子进程遍历 redis 内存中数据到临时文件，客户端的写请求同时写入 aof_buf 缓冲区和 aof_rewrite_buf 重写缓冲区，保证原 AOF 文件完整以及新 AOF 文件生成期间的新的数据修改动作不会丢失；</li>
<li>子进程写完新的 AOF 文件后，向主进程发信号，父进程更新统计信息。主进程把 aof_rewrite_buf 中的数据写入到新的 AOF 文件；</li>
<li>使用新的 AOF 文件覆盖旧的 AOF 文件，完成 AOF 重写。</li>
</ol>
<h4 id="优缺点-1"><a href="#优缺点-1" class="headerlink" title="优缺点"></a>优缺点</h4><p>优点：</p>
<ul>
<li>备份机制更稳健，丢失数据概率更低。</li>
<li>可读的日志文本，通过操作 AOF 稳健，可以处理误操作。</li>
</ul>
<p>缺点：</p>
<ul>
<li>比起 RDB 占用更多的磁盘空间。</li>
<li>恢复备份速度要慢。</li>
<li>每次读写都同步的话，有一定的性能压力。</li>
<li>存在个别 Bug，造成恢复不能。</li>
</ul>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul>
<li>官方推荐两个都启用：</li>
</ul>
<ul>
<li>如果对数据不敏感，可以选单独用 RDB。</li>
<li>不建议单独用 AOF，因为可能会出现 Bug。</li>
<li>如果只是做纯内存缓存，可以都不用。</li>
</ul>
<h4 id="官网建议"><a href="#官网建议" class="headerlink" title="官网建议"></a>官网建议</h4><ul>
<li>RDB 持久化方式能够在指定的时间间隔能对你的数据进行快照存储。</li>
<li>AOF 持久化方式记录每次对服务器写的操作，当服务器重启的时候会重新执行这些命令来恢复原始的数据，AOF 命令以 redis 协议追加保存每次写的操作到文件末尾。</li>
<li>Redis 还能对 AOF 文件进行后台重写，使得 AOF 文件的体积不至于过大。</li>
<li>只做缓存：如果你只希望你的数据在服务器运行的时候存在，你也可以不使用任何持久化方式。</li>
<li>同时开启两种持久化方式：在这种情况下，当 redis 重启的时候会优先载入 AOF 文件来恢复原始的数据，因为在通常情况下 AOF 文件保存的数据集要比 RDB 文件保存的数据集要完整。</li>
<li>RDB 的数据不实时，同时使用两者时服务器重启也只会找 AOF 文件。那要不要只使用 AOF 呢？</li>
<li>建议不要，因为 RDB 更适合用于备份数据库 (AOF 在不断变化不好备份)，快速重启，而且不会有 AOF 可能潜在的 bug，留着作为一个万一的手段。</li>
<li>性能建议：<ul>
<li>因为 RDB 文件只用作后备用途，建议只在 Slave 上持久化 RDB 文件，而且只要 15 分钟备份一次就够了，只保留 save 9001 这条规则。</li>
<li>如果使用 AOF，好处是在最恶劣情况下也只会丢失不超过两秒数据，启动脚本较简单，只 load 自己的 AOF 文件就可以了。</li>
<li>aof 代价：一是带来了持续的 IO，二是 AOF rewrite 的最后，将 rewrite 过程中产生的新数据写到新文件造成的阻塞几乎是不可避免的。</li>
<li>只要硬盘许可，应该尽量减少 AOF rewrite 的频率，AOF 重写的基础大小默认值 64M 太小了，可以设到 5G 以上。默认超过原大小 100% 大小时重写可以改到适当的数值。</li>
</ul>
</li>
</ul>
<h2 id="参考-Reference"><a href="#参考-Reference" class="headerlink" title="参考 Reference"></a>参考 Reference</h2><p>参考资料：《Redis开发与运维》</p>
<p>视频教程：<a href="https://www.bilibili.com/video/BV1Rv41177Af">https://www.bilibili.com/video/BV1Rv41177Af</a></p>
<p>参考笔记：<a href="https://zhangc233.github.io/2021/05/02/Redis/">https://zhangc233.github.io/2021/05/02/Redis/</a></p>
<p>参考笔记：<a href="https://blog.csdn.net/weixin_47872288/article/details/118410080">https://blog.csdn.net/weixin_47872288/article/details/118410080</a></p>
<p><a href="https://www.cnblogs.com/biyeymyhjob/archive/2012/07/20/2601655.html">Linux写时拷贝技术(copy-on-write)</a></p>
]]></content>
      <categories>
        <category>REDIS</category>
      </categories>
      <tags>
        <tag>REDIS</tag>
        <tag>后端</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker01-安装和卸载</title>
    <url>/2021/08/01/Docker01-%E5%AE%89%E8%A3%85%E5%92%8C%E5%8D%B8%E8%BD%BD/</url>
    <content><![CDATA[<h2 id="安装Docker"><a href="#安装Docker" class="headerlink" title="安装Docker"></a>安装Docker</h2><h3 id="学习环境-Centos-7"><a href="#学习环境-Centos-7" class="headerlink" title="学习环境(Centos 7)"></a>学习环境(Centos 7)</h3><pre><code>cat /etc/os-release

NAME=&quot;CentOS Linux&quot;
VERSION=&quot;7 (Core)&quot;
ID=&quot;centos&quot;
ID_LIKE=&quot;rhel fedora&quot;
VERSION_ID=&quot;7&quot;
PRETTY_NAME=&quot;CentOS Linux 7 (Core)&quot;
ANSI_COLOR=&quot;0;31&quot;
CPE_NAME=&quot;cpe:/o:centos:centos:7&quot;
HOME_URL=&quot;https://www.centos.org/&quot;
BUG_REPORT_URL=&quot;https://bugs.centos.org/&quot;

CENTOS_MANTISBT_PROJECT=&quot;CentOS-7&quot;
CENTOS_MANTISBT_PROJECT_VERSION=&quot;7&quot;
REDHAT_SUPPORT_PRODUCT=&quot;centos&quot;
REDHAT_SUPPORT_PRODUCT_VERSION=&quot;7&quot;
</code></pre>
<p>帮助文档：<a href="https://docs.docker.com/engine/install/centos/">https://docs.docker.com/engine/install/centos/</a></p>
<h4 id="1-删除旧版本"><a href="#1-删除旧版本" class="headerlink" title="1.删除旧版本"></a>1.删除旧版本</h4><pre><code>sudo yum remove docker \
                  docker-client \
                  docker-client-latest \
                  docker-common \
                  docker-latest \
                  docker-latest-logrotate \
                  docker-logrotate \
                  docker-engine
</code></pre>
<h4 id="2-需要的安装包"><a href="#2-需要的安装包" class="headerlink" title="2.需要的安装包"></a>2.需要的安装包</h4><pre><code>sudo yum install -y yum-utils
</code></pre>
<h4 id="3-设置镜像仓库（阿里云）"><a href="#3-设置镜像仓库（阿里云）" class="headerlink" title="3.设置镜像仓库（阿里云）"></a>3.设置镜像仓库（阿里云）</h4><pre><code>yum-config-manager \
    --add-repo \
    http://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo
</code></pre>
<p>阿里云镜像加速<br><a href="https://help.aliyun.com/document_detail/60750.html">https://help.aliyun.com/document_detail/60750.html</a></p>
<pre><code>sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json &lt;&lt;-&#39;EOF&#39;
&#123;
  &quot;registry-mirrors&quot;: [&quot;https://***.mirror.aliyuncs.com&quot;]
&#125;
EOF
sudo systemctl daemon-reload
sudo systemctl restart docker
</code></pre>
<h4 id="4-更新yum索引"><a href="#4-更新yum索引" class="headerlink" title="4.更新yum索引"></a>4.更新yum索引</h4><pre><code>yum makecache fast
</code></pre>
<h4 id="5-安装Docker-（docker-ce-社区-docker-ee企业）"><a href="#5-安装Docker-（docker-ce-社区-docker-ee企业）" class="headerlink" title="5.安装Docker （docker-ce 社区 docker-ee企业）"></a>5.安装Docker （docker-ce 社区 docker-ee企业）</h4><pre><code>sudo yum install docker-ce docker-ce-cli containerd.io
</code></pre>
<h4 id="6-启动docker"><a href="#6-启动docker" class="headerlink" title="6.启动docker"></a>6.启动docker</h4><pre><code>systemctl start docker 
</code></pre>
<h4 id="7-查看版本"><a href="#7-查看版本" class="headerlink" title="7.查看版本"></a>7.查看版本</h4><pre><code>docker version
</code></pre>
<h4 id="8-运行hello-world"><a href="#8-运行hello-world" class="headerlink" title="8.运行hello world"></a>8.运行hello world</h4><pre><code>sudo docker run hello-world
docker images

REPOSITORY    TAG       IMAGE ID       CREATED        SIZE
hello-world   latest    d1165f221234   4 months ago   13.3kB
</code></pre>
<h2 id="卸载Docker"><a href="#卸载Docker" class="headerlink" title="卸载Docker"></a>卸载Docker</h2><h4 id="1-卸载依赖"><a href="#1-卸载依赖" class="headerlink" title="1.卸载依赖"></a>1.卸载依赖</h4><pre><code>sudo yum remove docker-ce docker-ce-cli containerd.io
</code></pre>
<h4 id="2-删除资源"><a href="#2-删除资源" class="headerlink" title="2.删除资源"></a>2.删除资源</h4><pre><code>sudo rm -rf /var/lib/docker
sudo rm -rf /var/lib/containerd
</code></pre>
<h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><p><a href="https://www.bilibili.com/video/BV1og4y1q7M4">【狂神说Java】Docker最新超详细版教程通俗易懂</a></p>
]]></content>
      <categories>
        <category>DOCKER</category>
      </categories>
      <tags>
        <tag>DOCKER</tag>
        <tag>容器</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker02-概念和命令</title>
    <url>/2021/08/02/Docker02-%E6%A6%82%E5%BF%B5%E5%92%8C%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<h2 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h2><h3 id="Docker是怎么工作的？"><a href="#Docker是怎么工作的？" class="headerlink" title="Docker是怎么工作的？"></a>Docker是怎么工作的？</h3><p>Docker是一个Client - Server 结构的系统，Docker的守护进程运行在宿主机上，通过Socket从客户端访问。Docker-Server收到Docker-Client的指令，就会执行命令。</p>
<h3 id="Docker为什么比VM快？"><a href="#Docker为什么比VM快？" class="headerlink" title="Docker为什么比VM快？"></a>Docker为什么比VM快？</h3><p>1.Docker有更少的抽象层<br>2.Docker利用的是宿主机的内核，VM需要Guest OS(操作系统内核)</p>
<h3 id="帮助命令"><a href="#帮助命令" class="headerlink" title="帮助命令"></a>帮助命令</h3><pre><code class="applescript">docker version
docker info        #系统信息，包括镜像和容器的数量
docker xxx --help
</code></pre>
<p>官方文档：<a href="https://link.segmentfault.com/?enc=t5Vf/z9an/EXV0jMYwGlng==.vaIGlj6kdfz3fJUOrnryodA0cl87C9RT2QmwB7y5mKnObNDo1t+Lu2KYA9YTS6TF">https://docs.docker.com/refer…</a></p>
<h3 id="镜像命令"><a href="#镜像命令" class="headerlink" title="镜像命令"></a>镜像命令</h3><pre><code class="ada">docker images --help
</code></pre>
<h4 id="docker-images-查看所有本地镜像"><a href="#docker-images-查看所有本地镜像" class="headerlink" title="docker images 查看所有本地镜像"></a>docker images 查看所有本地镜像</h4><pre><code class="crmsh">REPOSITORY    TAG       IMAGE ID       CREATED        SIZE
hello-world   latest    d1165f221234   4 months ago   13.3kB

REPOSITORY  #镜像仓库源  
TAG         #镜像标签
IMAGE ID    #镜像ID   
CREATED     #镜像创建时间   
SIZE        #镜像大小

#可选项
-a --all      #列出所有镜像
-q --quiet    #只显示镜像ID   
</code></pre>
<h4 id="docker-search-搜索镜像"><a href="#docker-search-搜索镜像" class="headerlink" title="docker search 搜索镜像"></a>docker search 搜索镜像</h4><pre><code class="dsconfig">docker search mysql

#可选项
-f --filter #过滤条件

--fitler=STARS=3000 #收藏3000以上的镜像
</code></pre>
<h4 id="docker-pull-下载镜像"><a href="#docker-pull-下载镜像" class="headerlink" title="docker pull 下载镜像"></a>docker pull 下载镜像</h4><p>Dockerhub地址：<a href="https://link.segmentfault.com/?enc=1fQLqXUpTCiNuvMtyLIwVg==.F/4e7SDZroG+zG1fNwMTwurpXIv8D8e6LPYoGvnEQKw=">https://hub.docker.com</a></p>
<pre><code class="elixir">docker pull [OPTIONS] NAME[:TAG|@DIGEST]
OPTIONS #可选项
NAME    #镜像名
TAG     #镜像标签

#指定版本下载
docker pull mysql:5.7
</code></pre>
<h6 id="docker-rmi-删除镜像"><a href="#docker-rmi-删除镜像" class="headerlink" title="docker rmi 删除镜像"></a>docker rmi 删除镜像</h6><pre><code class="powershell">docker rmi -f 容器ID1 ... 容器IDN
#删除全部镜像
docker rmi -f $(docker images -aq)
</code></pre>
<h3 id="容器命令"><a href="#容器命令" class="headerlink" title="容器命令"></a>容器命令</h3><h4 id="docker-run-新建并启动容器"><a href="#docker-run-新建并启动容器" class="headerlink" title="docker run 新建并启动容器"></a>docker run 新建并启动容器</h4><pre><code class="awk">docker run [OPTIONS] IMAGE [COMMAND] [ARG...]
 
#参数说明
--name #容器名
-d     #后台方式运行  
-it    #交互方式运行，进入容器查看  
-p     #指定容器端口
-P     #随机指定端口

#测试：启动并进入容器
docker pull centos
docker run -it centos /bin/bash      
#退出容器
exit #容器停止并退出
CTRL + P + Q #不停止退出
</code></pre>
<h4 id="docker-ps-列出容器"><a href="#docker-ps-列出容器" class="headerlink" title="docker ps 列出容器"></a>docker ps 列出容器</h4><pre><code class="ldif">docker ps #列出当前正在运行的容器

-a   #列出当前正在运行的容器+历史运行过的容器
-n=? #最近创建的容器
-q   #只显示容器编号
</code></pre>
<h4 id="docker-rm-删除容器"><a href="#docker-rm-删除容器" class="headerlink" title="docker rm 删除容器"></a>docker rm 删除容器</h4><pre><code class="powershell">docker rm 容器ID
docker rm $(docker ps -aq) #删除所有容器

-f #强制删除（包括运行中的容器）
</code></pre>
<h4 id="docker-start-启动容器"><a href="#docker-start-启动容器" class="headerlink" title="docker start 启动容器"></a>docker start 启动容器</h4><pre><code class="livecodeserver">docker start 容器ID
docker restart 容器ID
docker stop 容器ID
docker kill 容器ID
</code></pre>
<h4 id="docker-logs-查看日志"><a href="#docker-logs-查看日志" class="headerlink" title="docker logs 查看日志"></a>docker logs 查看日志</h4><pre><code class="bash">docker logs -tf --tail 10 容器ID #显示指定行数
docker logs -t -f 容器ID         #显示日志
</code></pre>
<h4 id="docker-top-查看进程"><a href="#docker-top-查看进程" class="headerlink" title="docker top 查看进程"></a>docker top 查看进程</h4><pre><code class="coq">docker top 容器ID
</code></pre>
<h4 id="docker-inspect-查看元数据"><a href="#docker-inspect-查看元数据" class="headerlink" title="docker inspect 查看元数据"></a>docker inspect 查看元数据</h4><pre><code class="gradle">docker inspect 容器ID
</code></pre>
<h4 id="docker-exec-attach-进入当前正在运行的容器"><a href="#docker-exec-attach-进入当前正在运行的容器" class="headerlink" title="docker exec\attach 进入当前正在运行的容器"></a>docker exec\attach 进入当前正在运行的容器</h4><pre><code class="awk">docker exec -it 容器ID /bin/bash  
#进入容器后开启新的终端，可以操作

docker attach 容器ID
#进入容器正在执行的终端，不会启动新的进程
</code></pre>
<h4 id="docker-cp-拷贝文件"><a href="#docker-cp-拷贝文件" class="headerlink" title="docker cp 拷贝文件"></a>docker cp 拷贝文件</h4><pre><code class="armasm">docker cp 容器ID:容器内路径 宿主机路径
</code></pre>
<h4 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h4><pre><code class="routeros">docker run -d 容器ID
docker ps 发现容器停止了

原因：docker容器在后台运行，必须有一个前台进程，如果docker发现没有应用，就会自动停止
</code></pre>
<h5 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h5><p><img src="https://tva1.sinaimg.cn/large/e6c9d24ely1h0ub73h9icj20kb0f8q4j.jpg"></p>
]]></content>
      <categories>
        <category>DOCKER</category>
      </categories>
      <tags>
        <tag>DOCKER</tag>
        <tag>容器</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker03-安装Nginx,Elasticsearch,Portainer可视化</title>
    <url>/2021/08/03/Docker03-%E5%AE%89%E8%A3%85Nginx,Elasticsearch,Portainer%E5%8F%AF%E8%A7%86%E5%8C%96/</url>
    <content><![CDATA[<h2 id="安装Nginx"><a href="#安装Nginx" class="headerlink" title="安装Nginx"></a>安装Nginx</h2><p>Dockerhub官网: <a href="https://link.segmentfault.com/?enc=qeH1+L9RocjBLJmZX8+Y3Q==.rTmChDtYteqttg/ORAA8LKKTnYaAw/GieoyoO6Xopx4=">https://hub.docker.com/_/nginx</a></p>
<h4 id="拉取镜像"><a href="#拉取镜像" class="headerlink" title="拉取镜像"></a>拉取镜像</h4><pre><code class="ldif">docker pull nginx
docker run -d --name nginx01 -p 3344:80 nginx

-d                      #后台运行
--name                  #容器命名
-p 宿主机端口:容器内部端口  #配置端口 
</code></pre>
<h4 id="查看容器"><a href="#查看容器" class="headerlink" title="查看容器"></a>查看容器</h4><pre><code class="apache">docker ps

CONTAINER ID   IMAGE     COMMAND                  CREATED         STATUS         PORTS                                   NAMES
e7c5fb92ea7f   nginx     &quot;/docker-entrypoint.…&quot;   9 seconds ago   Up 8 seconds   0.0.0.0:3344-&gt;80/tcp, :::3344-&gt;80/tcp   nginx01
</code></pre>
<h4 id="测试访问"><a href="#测试访问" class="headerlink" title="测试访问"></a>测试访问</h4><pre><code class="xml">curl localhost:3344

&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;Welcome to nginx!&lt;/title&gt;
...
</code></pre>
<h4 id="进入容器"><a href="#进入容器" class="headerlink" title="进入容器"></a>进入容器</h4><pre><code class="gradle">docker exec -it nginx01 /bin/bash
whereis nginx
cat /etc/nginx/conf.d/default.conf
</code></pre>
<h4 id="停止容器"><a href="#停止容器" class="headerlink" title="停止容器"></a>停止容器</h4><pre><code class="arduino">docker stop e7c5fb92ea7f
</code></pre>
<h2 id="安装Elasticsearch"><a href="#安装Elasticsearch" class="headerlink" title="安装Elasticsearch"></a>安装Elasticsearch</h2><pre><code class="apache">docker run -d --name es01 -p 9200:9200 -p 9300:9300 -e &quot;discovery.type=single-node&quot; -e ES_JAVA_OPTS=&quot;-Xms64m -Xmx512m&quot; elasticsearch:7.13.4
</code></pre>
<h4 id="启动测试"><a href="#启动测试" class="headerlink" title="启动测试"></a>启动测试</h4><pre><code class="actionscript">curl localhost:9200

&#123;
  &quot;name&quot; : &quot;3fd55120f117&quot;,
  &quot;cluster_name&quot; : &quot;docker-cluster&quot;,
  &quot;cluster_uuid&quot; : &quot;S6dFIP9US120-499yDVUGA&quot;,
  &quot;version&quot; : &#123;
    &quot;number&quot; : &quot;7.13.4&quot;,
    &quot;build_flavor&quot; : &quot;default&quot;,
    &quot;build_type&quot; : &quot;docker&quot;,
    &quot;build_hash&quot; : &quot;c5f60e894ca0c61cdbae4f5a686d9f08bcefc942&quot;,
    &quot;build_date&quot; : &quot;2021-07-14T18:33:36.673943207Z&quot;,
    &quot;build_snapshot&quot; : false,
    &quot;lucene_version&quot; : &quot;8.8.2&quot;,
    &quot;minimum_wire_compatibility_version&quot; : &quot;6.8.0&quot;,
    &quot;minimum_index_compatibility_version&quot; : &quot;6.0.0-beta1&quot;
  &#125;,
  &quot;tagline&quot; : &quot;You Know, for Search&quot;
&#125;
</code></pre>
<h4 id="查看内容占用"><a href="#查看内容占用" class="headerlink" title="查看内容占用"></a>查看内容占用</h4><pre><code class="apache">docker stats

CONTAINER ID   NAME      CPU %     MEM USAGE / LIMIT     MEM %     NET I/O           BLOCK I/O         PIDS
3fd55120f117   es01      0.32%     413.4MiB / 1.795GiB   22.49%    1.74kB / 2.54kB   20.3MB / 1.26MB   47
</code></pre>
<h2 id="安装portainer"><a href="#安装portainer" class="headerlink" title="安装portainer"></a>安装portainer</h2><h4 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h4><pre><code class="gradle">docker run -d -p 8088:9000 \
--restart=always \
-v /var/run/docker.sock:/var/run/docker.sock \
--privileged=true \
portainer/portainer
</code></pre>
<h4 id="测试访问-1"><a href="#测试访问-1" class="headerlink" title="测试访问"></a>测试访问</h4><pre><code class="awk">http://xxx.com:8088
</code></pre>
<h4 id="新建admin用户"><a href="#新建admin用户" class="headerlink" title="新建admin用户"></a>新建admin用户</h4><h4 id="选择Local"><a href="#选择Local" class="headerlink" title="选择Local"></a>选择Local</h4>]]></content>
      <categories>
        <category>DOCKER</category>
      </categories>
      <tags>
        <tag>DOCKER</tag>
        <tag>容器</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker04-镜像的原理和提交</title>
    <url>/2021/08/04/Docker04-%E9%95%9C%E5%83%8F%E7%9A%84%E5%8E%9F%E7%90%86%E5%92%8C%E6%8F%90%E4%BA%A4/</url>
    <content><![CDATA[<h2 id="经销"><a href="#经销" class="headerlink" title="经销"></a>经销</h2><p>镜像是轻量级，可执行的独立软件包，用来打包软件运行环境和基于运行环境开发的软件。它包含运行某个软件所需要的所有内容（代码，运行时库、环境变量、配置文件）。</p>
<h3 id="加载原理"><a href="#加载原理" class="headerlink" title="加载原理"></a>加载原理</h3><p>Docker的镜像实际由一层层的文件系统(UnionFS)组成。</p>
<blockquote>
<p>UnionFS 联合文件系统：分层，轻量级，高性能的文件系统，支持对文件系统的修改作为一次提交来层层叠加。</p>
</blockquote>
<p><img src="https://segmentfault.com/img/bVcTPep" alt="image.png"></p>
<h4 id="bootfs"><a href="#bootfs" class="headerlink" title="bootfs"></a>bootfs</h4><p>Docker镜像的最底层是bootfs(boot file system),与Linux相同，包含boot加载器(bootloader)和内核(kernel)。当boot加载完成整个内核就在内存中了，内存使用权由bootfs转交给内核，同时系统卸载bootfs。</p>
<h4 id="rootfs"><a href="#rootfs" class="headerlink" title="rootfs"></a>rootfs</h4><p>bootfs之上是rootfs(root file system)，就是操作系统发行版(Ubuntu,Centos…)，包含/dev,/proc,/bin,/etc等标准目录和文件。</p>
<h4 id="Docker为什么小？"><a href="#Docker为什么小？" class="headerlink" title="Docker为什么小？"></a>Docker为什么小？</h4><ol>
<li>对于不同的Linux发行版，rootfs有区别，但bootfs基本是一致的，可以共用bootfs。</li>
<li>一个精简的OS的rootfs只需要最基本的命令，工具和程序库，可以很小。</li>
<li>Docker镜像底层可以直接使用宿主机的内核，自己只需要提供rootfs。</li>
</ol>
<h3 id="分层原理：layer"><a href="#分层原理：layer" class="headerlink" title="分层原理：layer"></a>分层原理：layer</h3><p>Docker镜像都是只读的，当容器启动时，会把一个新的可写层被加载到镜像顶部。<br>这一层称为容器层，容器层之下都是镜像层。</p>
<h3 id="镜像提交"><a href="#镜像提交" class="headerlink" title="镜像提交"></a>镜像提交</h3><pre><code class="routeros">docker commit -m=&quot;描述&quot; -a=&quot;作者&quot; 容器ID 目标镜像:[TAG]
</code></pre>
<h4 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h4><pre><code class="routeros">#安装并启动nginx
docker run -d --name nginx01 -p 3344:80 nginx

#进入容器
docker exec -it nginx01 /bin/bash

#安装vim
apt-get update
apt-get install vim
#修改nginx主页
vim /usr/share/nginx/html/index.html
#退出容器
CTRL+P+Q

#提交版本
docker ps
docker commit -a=&quot;itxiaoma&quot; -m=&quot;add vim&quot; c1ab7a56364f nginx-vim:1.0
docker stop c1ab7a56364f

#运行新的版本
docker run -d  -p 3344:80 nginx-vim:1.0
</code></pre>
<h4 id="访问"><a href="#访问" class="headerlink" title="访问"></a>访问</h4><p><img src="https://segmentfault.com/img/bVcTPeV" alt="image.png"></p>
]]></content>
      <categories>
        <category>DOCKER</category>
      </categories>
      <tags>
        <tag>DOCKER</tag>
        <tag>容器</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker05-数据挂载</title>
    <url>/2021/08/05/Docker05-%E6%95%B0%E6%8D%AE%E6%8C%82%E8%BD%BD/</url>
    <content><![CDATA[<h2 id="容器数据卷"><a href="#容器数据卷" class="headerlink" title="容器数据卷"></a>容器数据卷</h2><h3 id="指定路径挂载"><a href="#指定路径挂载" class="headerlink" title="指定路径挂载"></a>指定路径挂载</h3><blockquote>
<p>-v 宿主机目录:容器目录</p>
</blockquote>
<pre><code class="applescript">docker run -it -v 宿主机目录:容器目录 centos /bin/bash

#查看挂载信息
docker inspect 容器ID

&quot;Mounts&quot;: [
    &#123;
        &quot;Type&quot;: &quot;bind&quot;,
        &quot;Source&quot;: &quot;宿主机目录&quot;,
        &quot;Destination&quot;: &quot;容器目录&quot;,
        ...
    &#125;
]
</code></pre>
<h3 id="匿名挂载"><a href="#匿名挂载" class="headerlink" title="匿名挂载"></a>匿名挂载</h3><blockquote>
<p>-v 容器目录</p>
</blockquote>
<pre><code class="dockerfile">docker run -d -P --name test1 -v /test  nginx

-P 随机开放端口

#查看所有volume信息
docker volume ls

DRIVER   VOLUME NAME
local    ccae06dda8374c37d25321b266e1d77eb786594f1aa9dfb861eb6097114d78da

#查看具体挂载
docker volume inspect ccae06dda8374c37d25321b266e1d77eb786594f1aa9dfb861eb6097114d78da

[
    &#123;
        &quot;CreatedAt&quot;: &quot;2021-08-01T20:09:40+08:00&quot;,
        &quot;Driver&quot;: &quot;local&quot;,
        &quot;Labels&quot;: null,
        &quot;Mountpoint&quot;: &quot;/var/lib/docker/volumes/ccae06dda8374c37d25321b266e1d77eb786594f1aa9dfb861eb6097114d78da/_data&quot;,
        &quot;Name&quot;: &quot;ccae06dda8374c37d25321b266e1d77eb786594f1aa9dfb861eb6097114d78da&quot;,
        &quot;Options&quot;: null,
        &quot;Scope&quot;: &quot;local&quot;
    &#125;
]
</code></pre>
<h3 id="具名挂载"><a href="#具名挂载" class="headerlink" title="具名挂载"></a>具名挂载</h3><p>具名挂载可以方便的找到我们的卷，使用较多</p>
<blockquote>
<p>-v 卷名:容器目录</p>
</blockquote>
<pre><code class="dockerfile">docker run -d -P --name test2 -v test-name:/test  nginx
#查看所有volume信息
docker volume ls

DRIVER    VOLUME NAME
local     test-name

#查看具体挂载
docker volume inspect test-name

[
    &#123;
        &quot;CreatedAt&quot;: &quot;2021-08-01T20:12:29+08:00&quot;,
        &quot;Driver&quot;: &quot;local&quot;,
        &quot;Labels&quot;: null,
        &quot;Mountpoint&quot;: &quot;/var/lib/docker/volumes/test-name/_data&quot;,
        &quot;Name&quot;: &quot;test-name&quot;,
        &quot;Options&quot;: null,
        &quot;Scope&quot;: &quot;local&quot;
    &#125;
]
docker默认挂载目录:/var/lib/docker/volumes/...
</code></pre>
<h3 id="拓展"><a href="#拓展" class="headerlink" title="拓展"></a>拓展</h3><p>-v 通过容器内路径:ro/rw 改变读写权限</p>
<pre><code class="axapta">ro readonly  #只读（容器内无法修改，只能通过宿主机操作）
rw readwrite #可读可写（默认）
</code></pre>
<h2 id="Mysql数据挂载"><a href="#Mysql数据挂载" class="headerlink" title="Mysql数据挂载"></a>Mysql数据挂载</h2><p>官方文档：<a href="https://link.segmentfault.com/?enc=6DljGlWfrZXy8pyrWRWZkA==.uuOe+8fH3XIRxsreDuvlLXnCP6XOIaHelTL4U5/rDNk=">https://hub.docker.com/_/mysql</a></p>
<pre><code class="awk">#获取镜像
docker pull mysql:5.7

#数据挂载
docker run -d -p 3310:3306 \
-v /home/mysql/conf:/etc/mysql/conf.d \
-v /home/mysql/data:/var/lib/mysql \
-e MYSQL_ROOT_PASSWORD=密码 \
--name 容器名称 \
mysql:5.7
</code></pre>
<p>测试连接<br><img src="https://segmentfault.com/img/bVcTPO3" alt="image.png"></p>
]]></content>
      <categories>
        <category>DOCKER</category>
      </categories>
      <tags>
        <tag>DOCKER</tag>
        <tag>容器</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker06-数据卷容器和Dockerfile</title>
    <url>/2021/08/06/Docker06-%E6%95%B0%E6%8D%AE%E5%8D%B7%E5%AE%B9%E5%99%A8%E5%92%8CDockerfile/</url>
    <content><![CDATA[<h2 id="数据卷容器"><a href="#数据卷容器" class="headerlink" title="数据卷容器"></a>数据卷容器</h2><h3 id="使用Dockerfile创建镜像"><a href="#使用Dockerfile创建镜像" class="headerlink" title="使用Dockerfile创建镜像"></a>使用Dockerfile创建镜像</h3><pre><code class="apache">docker build -f dockerfile1 -t test/centos:1.0 .
注意最后有一个.(表示生成在当前目录下)
vim dockerfile1

FROM centos

VOLUME [&quot;volume01&quot;,&quot;volume02&quot;]

CMD echo &quot;---end---&quot;

CMD /bin/bash
</code></pre>
<p>查看挂载信息</p>
<pre><code class="bash">docker run -it test/centos:1.0 /bin/bash

ls -l 

drwxr-xr-x  2 root root 4096 Aug  1 13:42 volume01
drwxr-xr-x  2 root root 4096 Aug  1 13:42 volume02

touch volume01/test1

#查看容器信息
docker inspect 容器ID

&quot;Mounts&quot;: [
            &#123;
                &quot;Type&quot;: &quot;volume&quot;,
                &quot;Name&quot;: &quot;f031e6bb7308ae91279cf1ba149aa04fb657bfdb612df79ad8e1e14d9ddd1b3a&quot;,
                &quot;Source&quot;: &quot;/var/lib/docker/volumes/f031e6bb7308ae91279cf1ba149aa04fb657bfdb612df79ad8e1e14d9ddd1b3a/_data&quot;,
                &quot;Destination&quot;: &quot;volume01&quot;,
                &quot;Driver&quot;: &quot;local&quot;,
                &quot;Mode&quot;: &quot;&quot;,
                &quot;RW&quot;: true,
                &quot;Propagation&quot;: &quot;&quot;
            &#125;,
            &#123;
                &quot;Type&quot;: &quot;volume&quot;,
                &quot;Name&quot;: &quot;e34e2bae85f36f00364ec5c59a3fa85e92f946010a50099d97a92bd41c9d1c96&quot;,
                &quot;Source&quot;: &quot;/var/lib/docker/volumes/e34e2bae85f36f00364ec5c59a3fa85e92f946010a50099d97a92bd41c9d1c96/_data&quot;,
                &quot;Destination&quot;: &quot;volume02&quot;,
                &quot;Driver&quot;: &quot;local&quot;,
                &quot;Mode&quot;: &quot;&quot;,
                &quot;RW&quot;: true,
                &quot;Propagation&quot;: &quot;&quot;
            &#125;
        ]
</code></pre>
<h3 id="容器间数据共享"><a href="#容器间数据共享" class="headerlink" title="容器间数据共享"></a>容器间数据共享</h3><blockquote>
<p>–volumes-from 数据卷容器</p>
</blockquote>
<pre><code class="dockerfile">docker run -itd --name docker01 test/centos:1.0

docker run -itd --name docker02 --volumes-from docker01  test/centos:1.0
docker run -itd --name docker03 --volumes-from docker01  test/centos:1.0
#docker01就是数据卷容器
#docker01,02,03共享volume01,volume02目录
注：删除docker01后，02和03仍可以访问共享文件
</code></pre>
<h3 id="多个Mysql实现数据共享"><a href="#多个Mysql实现数据共享" class="headerlink" title="多个Mysql实现数据共享"></a>多个Mysql实现数据共享</h3><pre><code class="apache">docker run -d -p 3310:3306 -v /etc/mysql/conf.d -v /var/lib/mysql -e MYSQL_ROOT_PASSWORD=密码 --name mysql01 mysql:5.7

docker run -d -p 3310:3306 -e MYSQL_ROOT_PASSWORD=密码 --name mysql02 --volumes-from mysql01 mysql:5.7
</code></pre>
<h3 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h3><p>容器间配置信息的传递，数据卷容器的生命周期一直持续到没有容器使用为止。</p>
<h2 id="Dockerfile"><a href="#Dockerfile" class="headerlink" title="Dockerfile"></a>Dockerfile</h2><h3 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h3><p>用来构建docker镜像的文件</p>
<blockquote>
<p>1.编写dockerfile文件<br>2.docker build 构建镜像<br>3.docker run 运行镜像<br>4.docker push 发布镜像</p>
</blockquote>
<h3 id="构建过程"><a href="#构建过程" class="headerlink" title="构建过程"></a>构建过程</h3><p>基础知识：</p>
<blockquote>
<p>1.每个保留关键字（指令）都是大写字母<br>2.从上到下执行<br>3.#表示注释<br>4.每个指令都会创建提交一个新的镜像层</p>
</blockquote>
<h3 id="基础指令"><a href="#基础指令" class="headerlink" title="基础指令"></a>基础指令</h3><pre><code class="dockerfile">FROM         #基础镜像
MAINTAINER   #镜像维护者：姓名+邮箱
RUN          #镜像构建时需要运行的命令
ADD          #添加内容
WORKDIR      #工作目录
VOLUME       #挂载目录
EXPOSE       #指定暴露端口
CMD          #容器启动时运行（仅最后一个生效，可被替代）
ENTRYPOINT   #容器启动时运行，可追加命令
ONBUILD      #构建被继承的dockerfile时运行
COPY         #将文件拷贝到镜像中
ENV          #构建时设置环境变量
</code></pre>
<h3 id="构建centos"><a href="#构建centos" class="headerlink" title="构建centos"></a>构建centos</h3><p>1.编写文件</p>
<blockquote>
<p>vim dockerfile-centos</p>
</blockquote>
<pre><code class="dockerfile">FROM centos
MAINTAINER itxiaoma&lt;xxx@qq.com&gt;

ENV MYPATH /usr/local
WORKDIR $MYPATH

RUN yum -y install vim
RUN yum -y install net-tools

EXPOSE 80

CMD echo $MYPATH
CMD echo &quot;--- BUILD END ---&quot;
CMD /bin/bash
</code></pre>
<p>2.执行构建命令</p>
<pre><code class="apache">docker build dockerfile-centos -t mycentos:0.1 .
</code></pre>
<p>3.测试运行</p>
<pre><code class="applescript">docker run -it mycentos:0.1

vim test
ifconfig

docker history 镜像ID #查看镜像构建过程
</code></pre>
<h3 id="CMD和ENTRYPOINT的区别"><a href="#CMD和ENTRYPOINT的区别" class="headerlink" title="CMD和ENTRYPOINT的区别"></a>CMD和ENTRYPOINT的区别</h3><p>测试CMD</p>
<pre><code class="dockerfile">vim dockerfile-cmd

FROM centos
CMD [&quot;ls&quot;,&quot;-a&quot;]

docker build -f dockerfile-cmd -t cmdtest .

docker run cmdtest

docker run entrytest ls -al #不可追加命令
</code></pre>
<p>测试ENTRYPOINT</p>
<pre><code class="dockerfile">vim dockerfile-entry

FROM centos
ENTRYPOINT [&quot;ls&quot;,&quot;-a&quot;]

docker build -f dockerfile-entry -t entrytest .

docker run entrytest

docker run entrytest -l #可追加命令
追加命令是直接拼接在ENTRYPOINT命令后的
</code></pre>
<h3 id="发布镜像"><a href="#发布镜像" class="headerlink" title="发布镜像"></a>发布镜像</h3><p>1.注册登录Docker Hub</p>
<pre><code class="pgsql">docker login -u itxiaoma

Login Succeeded
</code></pre>
<p>2.提交镜像</p>
<pre><code class="apache">docker tag entrytest itxiaoma/entrytest:1.0

docker push itxiaoma/entrytest:1.0
</code></pre>
<h3 id="发布到阿里云镜像"><a href="#发布到阿里云镜像" class="headerlink" title="发布到阿里云镜像"></a>发布到阿里云镜像</h3><p>1.在容器镜像服务创建命名空间，创建本地仓库<br>2.根据引导推送镜像</p>
]]></content>
      <categories>
        <category>DOCKER</category>
      </categories>
      <tags>
        <tag>DOCKER</tag>
        <tag>容器</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker07-网络</title>
    <url>/2021/08/07/Docker07-%E7%BD%91%E7%BB%9C/</url>
    <content><![CDATA[<h2 id="docker0"><a href="#docker0" class="headerlink" title="docker0"></a>docker0</h2><blockquote>
<p>安装docker的主机会有一个docker0网卡（桥接模式）,使用的是evth-pair技术<br><code>evth-pair是虚拟设备接口，成对出现</code></p>
</blockquote>
<pre><code class="pf">ip addr

1: lo: &lt;LOOPBACK,UP,LOWER_UP&gt; mtu 65536 qdisc noqueue state UNKNOWN group default qlen 1000
    inet 127.0.0.1/8 scope host lo...
2: eth0: &lt;BROADCAST,MULTICAST,UP,LOWER_UP&gt; mtu 1500 qdisc pfifo_fast state UP group default qlen 1000
    inet 172.17.0.16/20 brd 172.17.15.255 scope global eth0...
3: docker0: &lt;NO-CARRIER,BROADCAST,MULTICAST,UP&gt; mtu 1500 qdisc noqueue state DOWN group default
    inet 172.18.0.1/16 brd 172.18.255.255 scope global docker0...
/16是网络标识位，子网掩码是255.255.0.0，/24为255.255.255.0
</code></pre>
<blockquote>
<p>容器启动的时候docker会分配给容器一个eth@ifxxx IP地址，可以在外部ping通</p>
</blockquote>
<pre><code class="pf">docker run -it -P --name tomcat01 tomcat
docker exec -it b4da99b29aef /bin/bash
ip addr

1: lo: &lt;LOOPBACK,UP,LOWER_UP&gt; mtu 65536 qdisc noqueue state UNKNOWN group default qlen 1000
    inet 127.0.0.1/8 scope host lo
       valid_lft forever preferred_lft forever
62: eth0@if63: &lt;BROADCAST,MULTICAST,UP,LOWER_UP&gt; mtu 1500 qdisc noqueue state UP group default 
    inet 172.18.0.2/16 brd 172.18.255.255 scope global eth0
       valid_lft forever preferred_lft forever
</code></pre>
<blockquote>
<p>每启动一个容器，宿主机也会多一个网卡</p>
</blockquote>
<pre><code class="pf">63: vethb4f3058@if62: &lt;BROADCAST,MULTICAST,UP,LOWER_UP&gt; mtu 1500 qdisc noqueue master docker0 state UP group default 
    link/ether f6:e4:da:9e:1c:bc brd ff:ff:ff:ff:ff:ff link-netnsid 0
    inet6 fe80::f4e4:daff:fe9e:1cbc/64 scope link 
       valid_lft forever preferred_lft forever
</code></pre>
<blockquote>
<p>结论：容器间可以相互ping通</p>
</blockquote>
<pre><code class="applescript">docker run -d -P --name tomcat02 tomcat
docker exec -it tomcat02 /bin/bash
ping ping 172.18.0.2
</code></pre>
<p><img src="https://segmentfault.com/img/bVcTWuj" alt="image.png"></p>
<h2 id="–link"><a href="#–link" class="headerlink" title="–link"></a>–link</h2><pre><code class="applescript">docker run -d -P --name tomcat03 --link tomcat02 tomcat

docker exec -it tomcat03 ping tomcat02
</code></pre>
<p>查看网络</p>
<pre><code class="routeros">docker network ls

docker network inspect 网卡ID
--link 就是在tomecat03容器的hosts中写入172.18.0.3 tomcat02 容器ID
</code></pre>
<blockquote>
<p>缺点：docker0不支持容器名访问！</p>
</blockquote>
<h2 id="自定义网络"><a href="#自定义网络" class="headerlink" title="自定义网络"></a>自定义网络</h2><p>创建自定义网络</p>
<pre><code class="jboss-cli">docker network create --driver bridge --subnet 192.168.0.0/16 --gateway 192.168.0.1 mynet
 
docker network ls

NETWORK ID     NAME      DRIVER    SCOPE
e7e2c109f027   mynet     bridge    local

--driver bridge 桥接
--subnet 子网
--gateway 网关
</code></pre>
<p><code>docker run 默认执行 --net bridge 参数</code><br>使用自定义网络</p>
<pre><code class="css">docker run -d -P --name tomcat01 --net mynet tomcat
docker run -d -P --name tomcat02 --net mynet tomcat

docker exec -it tomcat01 ping tomcat02
</code></pre>
<h2 id="网络联通"><a href="#网络联通" class="headerlink" title="网络联通"></a>网络联通</h2><pre><code class="arduino">docker network connect 网络 容器名
</code></pre>
<p>测试</p>
<pre><code class="routeros">docker run -d -P --name tomcat03 tomcat

#接入网络
docker network connect mynet tomcat03

#测试
docker exec -it tomcat01 ping tomcat03

#查看网络
docker inspect mynet

&quot;Containers&quot;: &#123;
            &quot;02a670496edfd99d1542ddc3f871d53b5e66295c1b9228f119f9262ee362bcc4&quot;: &#123;
                &quot;Name&quot;: &quot;tomcat03&quot;,
                &quot;EndpointID&quot;: &quot;ee626c9944c7732c10234feb78d7e6e59877d4ad3c531690cbf6af23103cb581&quot;,
                &quot;MacAddress&quot;: &quot;02:42:c0:a8:00:04&quot;,
                &quot;IPv4Address&quot;: &quot;192.168.0.4/16&quot;,
                &quot;IPv6Address&quot;: &quot;&quot;
            &#125;,
            &quot;b42c7b942d856b83af449e14696dc773f34d63acc5ca6d576ce2493763cc6d89&quot;: &#123;
                &quot;Name&quot;: &quot;tomcat01&quot;,
                &quot;EndpointID&quot;: &quot;392deab3c2848252c99b122f655ed903c2f7b33b136a585dfb845105cb24150a&quot;,
                &quot;MacAddress&quot;: &quot;02:42:c0:a8:00:02&quot;,
                &quot;IPv4Address&quot;: &quot;192.168.0.2/16&quot;,
                &quot;IPv6Address&quot;: &quot;&quot;
            &#125;,
            &quot;e84de7e8d89aa59ffd866ec4620a13c4b49fa510dac6989cdba9b6b870602571&quot;: &#123;
                &quot;Name&quot;: &quot;tomcat02&quot;,
                &quot;EndpointID&quot;: &quot;4461c14570c1a2fa87b43c15843b295af6f2a4551f9a7fcda5a27418cf6b8450&quot;,
                &quot;MacAddress&quot;: &quot;02:42:c0:a8:00:03&quot;,
                &quot;IPv4Address&quot;: &quot;192.168.0.3/16&quot;,
                &quot;IPv6Address&quot;: &quot;&quot;
            &#125;
        &#125;,
</code></pre>
<h2 id="Redis部署-分片-高可用-负载均衡"><a href="#Redis部署-分片-高可用-负载均衡" class="headerlink" title="Redis部署 分片+高可用+负载均衡"></a>Redis部署 分片+高可用+负载均衡</h2><p>1.创建网卡</p>
<pre><code class="apache">docker network create redis --subnet 172.38.0.0/16
</code></pre>
<p>2.通过shell脚本创建6个redis配置</p>
<pre><code class="stata">for port in $(seq 1 6); \
do \
mkdir -p /mydata/redis/node-$&#123;port&#125;/conf
touch /mydata/redis/node-$&#123;port&#125;/conf/redis.conf
cat &lt;&lt; EOF &gt;/mydata/redis/node-$&#123;port&#125;/conf/redis.conf
port 6379
bind 0.0.0.0
cluster-enabled yes
cluster-config-file nodes.conf
cluster-node-timeout 5000
cluster-announce-ip 172.38.0.1$&#123;port&#125;
cluster-announce-port 6379
cluster-announce-bus-port 16379
appendonly yes
EOF
done
</code></pre>
<p>3.启动docker容器1-6</p>
<pre><code class="awk">docker run -p 6371:6379 -p 16371:16379 --name redis-1 \
-v /mydata/redis/node-1/data:/data \
-v /mydata/redis/node-1/conf/redis.conf:/etc/redis/redis.conf \
-d --net redis --ip 172.38.0.11 redis:5.0.9-alpine3.11 redis-server /etc/redis/redis.conf

...2-6
</code></pre>
<p>4.进入容器，搭建集群</p>
<pre><code class="dns">docker exec -it redis-1 /bin/sh

redis-cli --cluster create 172.38.0.11:6379 172.38.0.12:6379 172.38.0.13:6379 172.38.0.14:6379 172.38.0.15:6379 172.38.0.16:6379 --cluster-replicas 1

Can I set the above configuration? (type &#39;yes&#39; to accept): yes
</code></pre>
<p>5.测试集群</p>
<pre><code class="accesslog"># redis-cli -c

#集群信息
127.0.0.1:6379&gt; cluster info
#集群节点
127.0.0.1:6379&gt; cluster nodes
#测试数据
127.0.0.1:6379&gt; set a b
-&gt; Redirected to slot [15495] located at 172.38.0.13:6379
#停掉redis-3
docker stop redis-3
#获取测试数据
127.0.0.1:6379&gt; get a
-&gt; Redirected to slot [15495] located at 172.38.0.14:6379
&quot;b&quot;
</code></pre>
<h2 id="打包Docker镜像"><a href="#打包Docker镜像" class="headerlink" title="打包Docker镜像"></a>打包Docker镜像</h2><p>1.构架项目<br>2.打包应用<br>3.编写Dockerfile<br>4.构建镜像docker build -t<br>5.发布运行docker push</p>
]]></content>
      <categories>
        <category>DOCKER</category>
      </categories>
      <tags>
        <tag>DOCKER</tag>
        <tag>容器</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker08-DockerCompose</title>
    <url>/2021/08/08/Docker08-DockerCompose/</url>
    <content><![CDATA[<h2 id="DockerCompose"><a href="#DockerCompose" class="headerlink" title="DockerCompose"></a>DockerCompose</h2><blockquote>
<p>官方介绍：定义、运行多个容器。YAML file配置文件。所有环境都可以使用。</p>
</blockquote>
<h3 id="使用步骤"><a href="#使用步骤" class="headerlink" title="使用步骤"></a>使用步骤</h3><p>1.编写Dockerfile<br>2.编写docker-compose.yml<br>3.运行docker-compose up</p>
<h3 id="安装compose"><a href="#安装compose" class="headerlink" title="安装compose"></a>安装compose</h3><p>官网地址：<a href="https://link.segmentfault.com/?enc=i3yp9g905YfztkwTduyX+g==.D2J+WlzIdJwgskd3A1gm6qlsv0ZFQWb4P37qWQXK5FVToj6B0cdYYrfgjSNmmS2x">https://docs.docker.com/compo…</a></p>
<pre><code class="awk">sudo curl -L &quot;https://get.daocloud.io/docker/compose/releases/download/1.29.2/docker-compose-$(uname -s)-$(uname -m)&quot; -o /usr/local/bin/docker-compose

sudo chmod +x /usr/local/bin/docker-compose

docker-compose version
</code></pre>
<h3 id="快速开始"><a href="#快速开始" class="headerlink" title="快速开始"></a>快速开始</h3><p>官方文档：<a href="https://link.segmentfault.com/?enc=8Kp8XD4VW+N3DtVLX1yqpg==.1tONnKdcArws1eccy6nQ5GNe95MiCUVyerx2WHETBNMiQkA9ibx4VS34UW09AunC">https://docs.docker.com/compo…</a></p>
<pre><code class="vim">mkdir composetest
cd composetest

vim app.py
vim requirements.txt
vim Dockerfile
vim docker-compose.yml

docker-compose up
</code></pre>
<p>测试</p>
<pre><code class="apache">curl localhost:5000
</code></pre>
<p>停止</p>
<pre><code class="ebnf">docker-compose down
</code></pre>
<p>扩展</p>
<pre><code class="mathematica">docker images
CTRL + C
默认服务名：文件名_服务名_num
docker network ls
项目中的内容都在同个网络下，通过域名访问(如localhost=redis)
</code></pre>
<h3 id="YAML配置编写规则"><a href="#YAML配置编写规则" class="headerlink" title="YAML配置编写规则"></a>YAML配置编写规则</h3><p>官方文档：<a href="https://link.segmentfault.com/?enc=NWKM3SSMqazoK8HO8jKS2A==.UnTNsgqD+MjYfeyNLjo3oWzErHxjl8CHma75AcXSsYLXo3KVyU4J7DlZ9Od8DZ7bwg35nuUyYEIKGm4SboMKcg==">https://docs.docker.com/compo…</a></p>
<pre><code class="nestedtext">#1.版本
version: &#39;&#39; 
#2.服务
services: 
  服务1: web
    #服务配置（build,ports,volumes...）
  服务2: redis,db...
#3.其他配置（网络，卷挂载，全局配置...）  
volumes:
network:
config:  
</code></pre>
<h2 id="Compose一键部署WordPress"><a href="#Compose一键部署WordPress" class="headerlink" title="Compose一键部署WordPress"></a>Compose一键部署WordPress</h2><p>官方文档：<a href="https://link.segmentfault.com/?enc=b8mdZQEB0OtaGGlwxeOwNQ==.phTbmq+9pTRCdoqQYs9buvkGBhLIBODoybrecSJ3F8y3A8WFKSFJisUKl4m2j3Rm">https://docs.docker.com/sampl…</a></p>
<p>docker-compose.yml</p>
<pre><code class="dts">version: &quot;3.9&quot;
    
services:
  db:
    image: mysql:5.7
    volumes:
      - db_data:/var/lib/mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: somewordpress
      MYSQL_DATABASE: wordpress
      MYSQL_USER: wordpress
      MYSQL_PASSWORD: wordpress
    
  wordpress:
    depends_on:
      - db
    image: wordpress:latest
    volumes:
      - wordpress_data:/var/www/html
    ports:
      - &quot;8000:80&quot;
    restart: always
    environment:
</code></pre>
]]></content>
      <categories>
        <category>DOCKER</category>
      </categories>
      <tags>
        <tag>DOCKER</tag>
        <tag>容器</tag>
      </tags>
  </entry>
</search>

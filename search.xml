<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>01-Go语言基础20问</title>
    <url>/2021/12/01/GO%E5%BE%AE%E6%9C%8D%E5%8A%A101-Go%E8%AF%AD%E8%A8%80%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<h3 id="1-Go语言的变量命名规范是什么？"><a href="#1-Go语言的变量命名规范是什么？" class="headerlink" title="1 Go语言的变量命名规范是什么？"></a>1 Go语言的变量命名规范是什么？</h3><p>以字母或下划线开头，后面可以跟任意数量的字母，数字，下划线。</p>
<p>变量名称一般遵循驼峰法，变量为私有则首字母小写，共有则首字母大写。</p>
<h3 id="2-如何获取变量的地址？如何取到地址中的值？"><a href="#2-如何获取变量的地址？如何取到地址中的值？" class="headerlink" title="2  如何获取变量的地址？如何取到地址中的值？"></a>2  如何获取变量的地址？如何取到地址中的值？</h3><p>一个指针的值是另一个变量的地址。</p>
<p>如var x int声明变量x，则&amp;x是变量x的内存地址；</p>
<p>p := &amp;x 表示指向该地址的指针，*p指向地址中的值。</p>
<h3 id="3-变量的生命周期是什么？作用域是什么？"><a href="#3-变量的生命周期是什么？作用域是什么？" class="headerlink" title="3  变量的生命周期是什么？作用域是什么？"></a>3  变量的生命周期是什么？作用域是什么？</h3><p>变量的生命周期指的是在程序运行期间变量有效存在的时间段。</p>
<p>包一级声明的变量来说，它们的生命周期和整个程序的运行周期是一致的。</p>
<p>局部变量的生命周期是：每次从创建一个新变量的声明语句开始，直到该变量不再被引用为止，然后变量的存储空间可能被回收。</p>
<p>函数的形式参数和返回值：都属于局部变量，在函数被调用的时候创建，函数调用结束后被销毁。</p>
<p>如果将局部变量赋值给全局变量，将会阻止 GC 对这个局部变量的回收，导致不必要的内存占用，从而影响程序的性能。</p>
<p>作用域是指源代码中可以有效使用函数或变量的范围。</p>
<p>作用域是一个编译时的属性，变量的生命周期是一个运行时的概念。</p>
<p>在函数体内声明的变量称之为局部变量，它们的作用域只在函数体内，函数的参数和返回值变量都属于局部变量。</p>
<p>在函数体外声明的变量称之为全局变量，全局变量只需要在一个源文件中定义，就可以在所有源文件中使用，当然，不包含这个全局变量的源文件需要使用“import”关键字引入全局变量所在的源文件之后才能使用这个全局变量。</p>
<h3 id="4-创建变量有哪几种方式"><a href="#4-创建变量有哪几种方式" class="headerlink" title="4  创建变量有哪几种方式"></a>4  创建变量有哪几种方式</h3><pre><code>var a int    
var b int = 1
var c = 2
d := 3
var e, f int = 4, 5
var g, h = 6, &quot;abc&quot;
</code></pre>
<h3 id="5-Go语言简单数据类型都有哪些？"><a href="#5-Go语言简单数据类型都有哪些？" class="headerlink" title="5  Go语言简单数据类型都有哪些？"></a>5  Go语言简单数据类型都有哪些？</h3><p>整型，浮点型，布尔型，字符串</p>
<h3 id="6-初始化数组的几种方式？"><a href="#6-初始化数组的几种方式？" class="headerlink" title="6  初始化数组的几种方式？"></a>6  初始化数组的几种方式？</h3><pre><code>var array [5]int
var array = [5]int&#123;1, 2, 3, 4, 5&#125;
var array = [...]int&#123;1, 2, 3, 4, 5&#125;
array :=[5]int&#123;1, 2, 3, 4, 5&#125;
array :=[...]int&#123;1, 2, 3, 4, 5&#125;
</code></pre>
<h3 id="7-遍历数组"><a href="#7-遍历数组" class="headerlink" title="7  遍历数组"></a>7  遍历数组</h3><pre><code>for i := 0; i &lt; len(arr1); i++ &#123;
        fmt.Println(arr1[i])
&#125;
for k, v := range arr1 &#123;
        fmt.Printf(&quot;key:%d value:%s\n&quot;, k, v)
&#125;
</code></pre>
<h3 id="8-初始化切片的几种方式？"><a href="#8-初始化切片的几种方式？" class="headerlink" title="8  初始化切片的几种方式？"></a>8  初始化切片的几种方式？</h3><pre><code>slice := make([]int, 3, 5)
slice := make([]int, 5)
slice := []int&#123;&#125;
slice := []int&#123;1, 2, 3, 4, 5&#125;
</code></pre>
<h3 id="9-遍历切片"><a href="#9-遍历切片" class="headerlink" title="9  遍历切片"></a>9  遍历切片</h3><pre><code>var arr [5]int = [...]int&#123;10,20,30,40,50&#125;
slice := arr[1:4]
// for循环方式
for i := 0; i &lt; len(slice); i++ &#123;
    fmt.Printf(&quot;slice[%v]=%v&quot;, i, slice[i])
&#125;
fmt.Println()
// for range方式
for i, v := range slice &#123;
    fmt.Printf(&quot;i=%v v=%v\n&quot;, i, v)
&#125;
</code></pre>
<h3 id="10-如何复制切片"><a href="#10-如何复制切片" class="headerlink" title="10 如何复制切片"></a>10 如何复制切片</h3><pre><code>s := []int&#123;1,2,3&#125;
s1 := s[0:2]
s2 := make([]int, 3)
copy(s2, s)//将s中的值，依次copy到s2
</code></pre>
<h3 id="11-实现切片的增删改查"><a href="#11-实现切片的增删改查" class="headerlink" title="11 实现切片的增删改查"></a>11 实现切片的增删改查</h3><pre><code>func main() &#123;
    arr := []int&#123;1, 2, 3&#125;
    slice := arr[:]
    //增
    s1 := append(slice, 4)
    fmt.Println(s1) //[1 2 3 4]
    //删
    n := 1
    s2 := append(slice[:n], slice[n+1:]...)
    fmt.Println(s2) //[1 3]
    //改
    slice[1] = 100
    fmt.Println(slice) //[1 100 3]
    //查
    fmt.Println(slice[1]) //100
&#125;
</code></pre>
<h3 id="12-下面代码是否有问题？并说出为什么？-如果有问题，如何修正？"><a href="#12-下面代码是否有问题？并说出为什么？-如果有问题，如何修正？" class="headerlink" title="12 下面代码是否有问题？并说出为什么？ 如果有问题，如何修正？"></a>12 下面代码是否有问题？并说出为什么？ 如果有问题，如何修正？</h3><pre><code>s := []string&#123;&quot;炭烤生蚝&quot;, &quot;麻辣小龙虾&quot;, &quot;干锅鸭&quot;&#125;
s2 := make([]*string, len(s))
for i, v := range s &#123;
    s2[i] = &amp;v
&#125;
</code></pre>
<p>切片本身就是引用传值，应该把星号和&amp;去掉</p>
<h3 id="11-分别写一个-if-和-switch、枚举-的例子"><a href="#11-分别写一个-if-和-switch、枚举-的例子" class="headerlink" title="11 分别写一个 if 和 switch、枚举 的例子"></a>11 分别写一个 if 和 switch、枚举 的例子</h3><pre><code>func main() &#123;
    a := 10
    if a == 10 &#123;
        fmt.Println(&quot;a=10&quot;)
    &#125;
    switch a &#123;
    case 10:
        fmt.Println(&quot;a=10&quot;)
    default:
        fmt.Println(&quot;a&lt;&gt;10&quot;)
    &#125;
&#125;
</code></pre>
<h3 id="12-map有什么特点？"><a href="#12-map有什么特点？" class="headerlink" title="12 map有什么特点？"></a>12 map有什么特点？</h3><pre><code>1.map是无序的key/value对的集合
2.map中所有的key都有相同的类型，所有的value也有着相同的类型，但是key和value之间可以是不同的数据类型。
3.key必须是支持==比较运算符的数据类型，map可以通过测试key是否相等来判断是否已经存在
4.value数据类型则没有任何的限制。
</code></pre>
<h3 id="13-什么样的类型可以做map的key"><a href="#13-什么样的类型可以做map的key" class="headerlink" title="13 什么样的类型可以做map的key"></a>13 什么样的类型可以做map的key</h3><p>必须是支持==比较运算符的数据类型，map可以通过测试key是否相等来判断是否已经存在</p>
<h3 id="14-写一个map的增删改查"><a href="#14-写一个map的增删改查" class="headerlink" title="14 写一个map的增删改查"></a>14 写一个map的增删改查</h3><pre><code>func main() &#123;
    s := make(map[int]string)
    //增
    s[0] = &quot;aaa&quot;
    s[1] = &quot;bbb&quot;
    fmt.Println(s) //map[0:aaa 1:bbb]
    //删
    delete(s, 1)
    fmt.Println(s) //map[0:aaa]
    //改
    s[0] = &quot;ccc&quot;
    fmt.Println(s) //map[0:ccc]
    //查
    if v, ok := s[0]; ok &#123;
        fmt.Println(v)//ccc
    &#125;
&#125;
</code></pre>
<h3 id="15-函数的定义"><a href="#15-函数的定义" class="headerlink" title="15 函数的定义"></a>15 函数的定义</h3><p>函数是基本的代码块，用于执行一个任务。</p>
<h3 id="16-函数传参，传值还是传引用？"><a href="#16-函数传参，传值还是传引用？" class="headerlink" title="16 函数传参，传值还是传引用？"></a>16 函数传参，传值还是传引用？</h3><p>Go语言中所有的传参都是值传递（传值），都是一个副本，一个拷贝。</p>
<p>非引用类型（int、string、struct等这些），这样就在函数中就无法修改原内容数据；引用类型（指针、map、slice、chan等这些），这样就可以修改原内容数据。</p>
<h3 id="17-定义函数的多返回值？"><a href="#17-定义函数的多返回值？" class="headerlink" title="17 定义函数的多返回值？"></a>17 定义函数的多返回值？</h3><pre><code>func test() (int, error) &#123;
    return 0, nil
&#125;
func main() &#123;
    fmt.Println(test())//0 &lt;nil&gt;
&#125;
</code></pre>
<h3 id="18-举例说明-函数变量、匿名函数、闭包、变长函数？"><a href="#18-举例说明-函数变量、匿名函数、闭包、变长函数？" class="headerlink" title="18 举例说明 函数变量、匿名函数、闭包、变长函数？"></a>18 举例说明 函数变量、匿名函数、闭包、变长函数？</h3><pre><code>package main

import &quot;fmt&quot;

func ShowBookInfoAndPrice(bookName, author string, price float64) (string, float64) &#123;
    return bookName + author, price
&#125;

//函数式编程
func PrintBookInfo(do func(string, string, float64) (bookInfo string, finalPrice float64),
    bookName, author string, price float64) &#123;
    bookInfo, finalPrice := do(bookName, author, price)
    fmt.Println(&quot;bookInfo:&quot;, bookInfo)
    fmt.Println(&quot;finalPrice:&quot;, finalPrice)
&#125;

//匿名函数
func PrintBookInfo2(bookName, author string, price float64) &#123;
    do := func(string, string, float64) (bookInfo string, finalPrice float64) &#123;
        return bookName + author, price
    &#125;
    fmt.Println(do(bookName, author, price))
&#125;

//闭包：返回值为匿名函数的函数
func PrintBookInfo3(bookName, author string, price float64) func(string, string, float64) (bookInfo string, finalPrice float64) &#123;
    return func(string, string, float64) (bookInfo string, finalPrice float64) &#123;
        return bookName + author, price
    &#125;
&#125;

//可变参数
func PrintBookNames(bookList ...string) &#123;
    r := &quot;&quot;
    for _, item := range bookList &#123;
        r += item
    &#125;
    fmt.Println(r)
&#125;
func main() &#123;
    PrintBookInfo(ShowBookInfoAndPrice, &quot;AAA&quot;, &quot;BBB&quot;, 99.99)
    PrintBookInfo2(&quot;AAA&quot;, &quot;BBB&quot;, 99.99)
    r := PrintBookInfo3(&quot;AAA&quot;, &quot;BBB&quot;, 99.99)
    fmt.Println(r(&quot;AAA&quot;, &quot;BBB&quot;, 99.99))
    PrintBookNames(&quot;AAA&quot;, &quot;BBB&quot;, &quot;CCC&quot;)
&#125;
</code></pre>
<h3 id="19-说一下面向对象设计的好处？"><a href="#19-说一下面向对象设计的好处？" class="headerlink" title="19 说一下面向对象设计的好处？"></a>19 说一下面向对象设计的好处？</h3><p>面向对象的三大特性：封装，继承，多态</p>
<p>使用面向对象思想进行开发有以下优点：</p>
<p>1、易维护<br>采用面向对象思想设计的结构，可读性高，由于继承的存在，即使改变需求，那么维护也只是在局部模块，所以维护起来是非常方便和较低成本的。<br>2、质量高<br>在设计时，可重用现有的，在以前的项目的领域中已被测试过的类使系统满足业务需求并具有较高的质量。<br>3、效率高<br>在软件开发时，根据设计的需要对现实世界的事物进行抽象，产生类。使用这样的方法解决问题，接近于日常生活和自然的思考方式，势必提高软件开发的效率和质量。<br>4、易扩展<br>由于继承、封装、多态的特性，自然设计出高内聚、低耦合的系统结构，使得系统更灵活、更容易扩展，而且成本较低。</p>
<h3 id="20-方法的定义"><a href="#20-方法的定义" class="headerlink" title="20 方法的定义"></a>20 方法的定义</h3><p>关键字 func 和函数名之间的参数被称作接收者，将函数与接收者的类型绑在一起。</p>
<p>如果一个函数有接收者，这个函数就被称为方法</p>
<h3 id="21-指针接收者和值接收者有何不同"><a href="#21-指针接收者和值接收者有何不同" class="headerlink" title="21 指针接收者和值接收者有何不同"></a>21 指针接收者和值接收者有何不同</h3><p>值接收者，是接收者的类型是一个值，是一个副本，方法内部无法对其真正的接收者做更改；</p>
<p>指针接收者，接收者的类型是一个指针，是接收者的引用，对这个引用的修改之间影响真正的接收者。</p>
]]></content>
      <categories>
        <category>GO微服务</category>
      </categories>
      <tags>
        <tag>GO微服务</tag>
        <tag>GO</tag>
      </tags>
  </entry>
  <entry>
    <title>01-基础及安装</title>
    <url>/2022/02/01/Redis01-%E5%9F%BA%E7%A1%80%E5%8F%8A%E5%AE%89%E8%A3%85/</url>
    <content><![CDATA[<h2 id="1-NoSQL数据库简介"><a href="#1-NoSQL数据库简介" class="headerlink" title="1.NoSQL数据库简介"></a>1.NoSQL数据库简介</h2><h3 id="1-1-技术分类"><a href="#1-1-技术分类" class="headerlink" title="1.1 技术分类"></a>1.1 技术分类</h3><ol>
<li>解决功能性问题</li>
<li>解决扩展性问题</li>
<li>解决性能问题</li>
</ol>
<h3 id="1-2-NoSQL数据库"><a href="#1-2-NoSQL数据库" class="headerlink" title="1.2 NoSQL数据库"></a>1.2 NoSQL数据库</h3><blockquote>
<p>NoSql = Not Only SQL 非关系型数据库，以key-value模式存储。</p>
</blockquote>
<h4 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h4><ul>
<li>不遵循SQL标准</li>
<li>不支持ACID</li>
<li>远超于SQL的性能</li>
</ul>
<h4 id="适用场景"><a href="#适用场景" class="headerlink" title="适用场景"></a>适用场景</h4><ul>
<li>数据高并发读写</li>
<li>海量数据读写</li>
<li>数据高可扩展性</li>
</ul>
<h4 id="不适用场景"><a href="#不适用场景" class="headerlink" title="不适用场景"></a>不适用场景</h4><ul>
<li>需要事务支持</li>
<li>结构化查询存储，处理复杂关系</li>
</ul>
<h4 id="常见NoSQL"><a href="#常见NoSQL" class="headerlink" title="常见NoSQL"></a>常见NoSQL</h4><ul>
<li>Memcache：数据在内存中，不支持持久化，一般作为缓存数据库。</li>
<li>Redis：数据在内存中，支持持久化，支持多种数据结构，一般作为缓存数据库。</li>
<li>MongoDB：<ul>
<li>文档型数据库</li>
<li>数据在内存中，内存不足时保存到硬盘</li>
<li>支持二进制数据和大型对象</li>
<li>配合/替代关系型数据库(RDBMS)存储特定数据</li>
</ul>
</li>
</ul>
<h2 id="2-Redis概述安装"><a href="#2-Redis概述安装" class="headerlink" title="2.Redis概述安装"></a>2.Redis概述安装</h2><p>官网：<a href="https://redis.io/">https://redis.io/</a></p>
<h4 id="Redis介绍"><a href="#Redis介绍" class="headerlink" title="Redis介绍"></a>Redis介绍</h4><ul>
<li>Redis 是一个开源的 key-value 存储系统。</li>
<li>和 Memcached 类似，它支持存储的 value 类型相对更多，包括 string (字符串)、list (链表)、set (集合)、zset (sorted set –有序集合) 和 hash（哈希类型）。</li>
<li>这些数据类型都支持 push/pop、add/remove 及取交集并集和差集及更丰富的操作，而且<strong>这些操作都是原子性的</strong>。</li>
<li>在此基础上，Redis 支持各种不同方式的排序。</li>
<li>与 memcached 一样，为了保证效率，数据都是缓存在内存中。</li>
<li>区别的是 Redis 会周期性的把更新的数据写入磁盘或者把修改操作写入追加的记录文件。</li>
<li>并且在此基础上实现了 master-slave (主从) 同步。</li>
</ul>
<h4 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h4><ul>
<li>高频次，热门访问的数据，降低数据库 IO。</li>
<li>分布式架构，做 session 共享。</li>
</ul>
<p><img src="https://cdn.jsdelivr.net/gh/zhangc233/pic@master/img/image-20210618160101040.png" alt="image-20210618160101040"></p>
<h4 id="Redis安装"><a href="#Redis安装" class="headerlink" title="Redis安装"></a>Redis安装</h4><p>官方教程：<a href="https://redis.io/docs/getting-started/installation/">https://redis.io/docs/getting-started/installation/</a></p>
<h5 id="docker安装redis"><a href="#docker安装redis" class="headerlink" title="docker安装redis"></a>docker安装redis</h5><p>docker-compose.yml</p>
<pre><code>version: &#39;3.5&#39;

services:
  redis:
    image: redis
    container_name: redis
    restart: always
    ports:
      - &quot;6379:6379&quot;
    volumes:
      - ./redis-data/:/data  
</code></pre>
<p>测试</p>
<pre><code>docker exec -it redis /bin/bash

redis-cli
</code></pre>
<h4 id="相关知识"><a href="#相关知识" class="headerlink" title="相关知识"></a>相关知识</h4><ol>
<li>默认有16个数据库，所有库密码相同，初始默认使用0号库</li>
<li>切换数据库：<code>select 数据库ID</code></li>
<li>查看当前数据key数量：<code>dbsize</code></li>
<li>清空当前库：<code>flushdb</code></li>
<li>清空全部库：<code>flushall</code></li>
</ol>
<h5 id="Redis是单线程-多路IO复用"><a href="#Redis是单线程-多路IO复用" class="headerlink" title="Redis是单线程+多路IO复用"></a>Redis是单线程+多路IO复用</h5><ul>
<li>Memcache是多线程 + 锁</li>
<li>Redis与 Memcache 三点不同：支持多数据类型，支持持久化，单线程 + 多路 IO 复用</li>
</ul>
<p>多路复用是指使用一个线程来检查多个文件描述符（Socket）的就绪状态，比如调用 select 和 poll 函数，传入多个文件描述符，如果有一个文件描述符就绪，则返回，否则阻塞直到超时。得到就绪状态后进行真正的操作可以在同一个线程里执行，也可以启动线程执行（比如使用线程池）。</p>
<h2 id="参考-Reference"><a href="#参考-Reference" class="headerlink" title="参考 Reference"></a>参考 Reference</h2><p>参考资料：《Redis开发与运维》</p>
<p>视频教程：<a href="https://www.bilibili.com/video/BV1Rv41177Af">https://www.bilibili.com/video/BV1Rv41177Af</a></p>
<p>参考笔记：<a href="https://zhangc233.github.io/2021/05/02/Redis/">https://zhangc233.github.io/2021/05/02/Redis/</a></p>
<p>参考笔记：<a href="https://blog.csdn.net/weixin_47872288/article/details/118410080">https://blog.csdn.net/weixin_47872288/article/details/118410080</a></p>
]]></content>
      <categories>
        <category>REDIS</category>
        <category>REDIS学习笔记</category>
      </categories>
      <tags>
        <tag>REDIS</tag>
        <tag>后端</tag>
      </tags>
  </entry>
  <entry>
    <title>02-常用五大数据类型</title>
    <url>/2022/02/02/Redis02-%E4%BA%94%E5%A4%A7%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B/</url>
    <content><![CDATA[<h2 id="3-常用五大数据类型"><a href="#3-常用五大数据类型" class="headerlink" title="3.常用五大数据类型"></a>3.常用五大数据类型</h2><p>Redis常见数据类型操作命令：<a href="http://redis.cn/commands.html">http://redis.cn/commands.html</a></p>
<h3 id="3-1-键-Key"><a href="#3-1-键-Key" class="headerlink" title="3.1 键 Key"></a>3.1 键 Key</h3><p>Key值操作：</p>
<pre><code>keys * 查看当前库所有key

set key value 设置key值与value

exists key 判断key是否存在

type key 查看key是什么类型

del key 删除指定的key数据

unlink key 根据value选择非阻塞删除
------仅将keys从keyspace元数据中删除，真正的删除会在后续异步操作。

expire key 10 10秒钟：为给定的key设置过期时间

ttl key 查看还有多少秒过期，-1表示永不过期，-2表示已过期
</code></pre>
<h3 id="3-2字符串-String"><a href="#3-2字符串-String" class="headerlink" title="3.2字符串 String"></a>3.2字符串 String</h3><blockquote>
<p>一个Redis字符串value最多可以是512M</p>
</blockquote>
<blockquote>
<p>原子操作，不会被打断，从开始到结束（单线程不会被打断）</p>
</blockquote>
<h4 id="基本操作"><a href="#基本操作" class="headerlink" title="基本操作"></a>基本操作</h4><pre><code>set key value 设置key值

get key 查询key值

append key value 将给定的value追加到原值末尾

strlen key 获取值的长度

setnx key value 只有在key不存在的时候，设置key值

incr key 将key值存储的数字增1，只对数字值操作，如果为空，结果为1
decr key 将key值存储的数字减1，只对数字值操作，如果为空，结果为-1

incrby/decrby key &lt;步长&gt; 将key值存储的数字增减如步长
</code></pre>
<h4 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h4><pre><code>mset key value key value..同时设置一个或者多个key-value
mget key key...同时获取一个或多个value
msetnx key value key value..同时设置一个或者多个key-value.当且仅当所有给定key都不存在
getrange key &lt;起始位置&gt; &lt;结束位置&gt; 获取key的起始位置和结束位置的值(起始0)
setrange key &lt;起始位置&gt; value 将value的值覆盖起始位置开始
setex key &lt;&gt; value 设置键值的同时,设置过期时间
getset key value 用新值换旧值
</code></pre>
<h4 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h4><p>String 的数据结构为简单动态字符串 (Simple Dynamic String, 缩写 SDS)，是可以修改的字符串，内部结构实现上类似于 Java 的 ArrayList，采用预分配冗余空间的方式来减少内存的频繁分配。</p>
<p>当前字符串实际分配的空间 capacity 一般要高于实际字符串长度 len。当字符串长度小于 1M 时，扩容都是加倍现有的空间，如果超过 1M，扩容时一次只会多扩 1M 的空间。需要注意的是字符串最大长度为 512M。</p>
<h3 id="3-3-列表-List"><a href="#3-3-列表-List" class="headerlink" title="3.3 列表 List"></a>3.3 列表 List</h3><h4 id="单键多值"><a href="#单键多值" class="headerlink" title="单键多值"></a>单键多值</h4><ul>
<li>Redis列表是简单的字符串列表，按照插入顺序排序，可以添加元素到列表的头(左)尾(右)。</li>
<li>Redis列表底层是双向链表，对两端的操作性能很高，通过索引下标操作中间节点性能较差。</li>
</ul>
<h4 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h4><pre><code>lpush/rpush key value value...从左或者右插入一个或者多个值(头插与尾插)

lpop/rpop key 从左或者右吐出一个或者多个值(值在键在,值都没,键都没)

rpoplpush key1 key2 从key1列表右边吐出一个值,插入到key2的左边

lrange key start stop 按照索引下标获取元素(从左到右)

lrange key 0 -1 获取所有值

lindex key index 按照索引下标获得元素

llen key 获取列表长度

linsert key before/after value newvalue 在value的前面插入一个新值

lrem key n value 从左边删除n个value值

lset key index value 在列表key中的下标index中修改值value
</code></pre>
<h4 id="数据结构-1"><a href="#数据结构-1" class="headerlink" title="数据结构"></a>数据结构</h4><p><img src="https://cdn.jsdelivr.net/gh/zhangc233/pic@master/img/image-20210618164459005.png" alt="image-20210618164459005"></p>
<ul>
<li>List 的数据结构为快速链表 quickList。</li>
<li>首先在列表元素较少的情况下会使用一块连续的内存存储，这个结构是 ziplist，也即是压缩列表。它将所有的元素紧挨着一起存储，分配的是一块连续的内存。</li>
<li>当数据量比较多的时候才会改成 quicklist。因为普通的链表需要的附加指针空间太大，会比较浪费空间。比如这个列表里存 的只是 int 类型的数据，结构上还需要两个额外的指针 prev 和 next。</li>
<li>Redis 将链表和 ziplist 结合起来组成了 quicklist。也就是将多个 ziplist 使用双向指针串起来使用。这样既满足了快速的插入删除性能，又不会出现太大的空间冗余。</li>
</ul>
<h3 id="3-4-集合-Set"><a href="#3-4-集合-Set" class="headerlink" title="3.4 集合 Set"></a>3.4 集合 Set</h3><ul>
<li>Set是自动排重的列表</li>
<li>Redis的Set是string类型的无序集合，底层是value为null的hash表，所以添加，删除，查找的复杂度都是O(1)</li>
<li><strong>O(1)</strong> : 常量级的时间复杂度,并不是说只执行了一条语句,只要执行语句的数量是常量,都可以用它来表示。</li>
</ul>
<h4 id="常用命令-1"><a href="#常用命令-1" class="headerlink" title="常用命令"></a>常用命令</h4><pre><code>sadd key value value... 将一个或者多个member元素加入集合key中,已经存在的member元素被忽略

smembers key 取出该集合的所有值

sismember key value 判断该集合key是否含有改值

scard key 返回该集合的元素个数

srem key value value 删除集合中的某个元素

spop key 随机从集合中取出一个元素

srandmember key n 随即从该集合中取出n个值，不会从集合中删除

smove &lt;一个集合a&gt;&lt;一个集合b&gt;value 将一个集合a的某个value移动到另一个集合b

sinter key1 key2 返回两个集合的交集元素

sunion key1 key2 返回两个集合的并集元素

sdiff key1 key2 返回两个集合的差集元素（key1有的，key2没有）
</code></pre>
<h4 id="数据结构-2"><a href="#数据结构-2" class="headerlink" title="数据结构"></a>数据结构</h4><ul>
<li>Set 数据结构是 dict 字典，字典是用哈希表实现的。</li>
<li>Java 中 HashSet 的内部实现使用的是 HashMap，只不过所有的 value 都指向同一个对象。Redis 的 set 结构也是一样，它的内部也使用 hash 结构，所有的 value 都指向同一个内部值。</li>
</ul>
<h3 id="3-5-哈希-Hash"><a href="#3-5-哈希-Hash" class="headerlink" title="3.5 哈希 Hash"></a>3.5 哈希 Hash</h3><ul>
<li><p>Redis hash 是一个键值对集合。</p>
</li>
<li><p>Redis hash 是一个 string 类型的 field 和 value 的映射表，hash 特别适合用于存储对象。</p>
</li>
<li><p>类似 Java 里面的 Map&lt;String,Object&gt;。</p>
</li>
<li><p>通过 key (用户 ID) + field (属性标签) 就可以操作对应属性数据了，既不需要重复存储数据，也不会带来序列化和并发修改控制的问题。</p>
<p><img src="https://cdn.jsdelivr.net/gh/zhangc233/pic@master/img/image-20210618203718979.png" alt="image-20210618203718979"></p>
</li>
</ul>
<h4 id="常用命令-2"><a href="#常用命令-2" class="headerlink" title="常用命令"></a>常用命令</h4><pre><code>hset key field value 给key集合中的filed键赋值value

hget key1 field 集合field取出value

hmset key1 field1 value1 field2 value2 批量设置hash的值

hexists key1 field 查看哈希表key中，给定域field是否存在

hkeys key 列出该hash集合的所有field

hvals key 列出该hash集合的所有value

hincrby key field increment 为哈希表key中的域field的值加上增量1 -1

hsetnx key field value 将哈希表key中的域field的值设置为value，当且仅当域field不存在
</code></pre>
<p>示例</p>
<pre><code>127.0.0.1:6379&gt; hset user:1001 id 1
(integer) 1
127.0.0.1:6379&gt; hset user:1002 id 2
(integer) 1
127.0.0.1:6379&gt; hset user:1001 name AAA
(integer) 1
127.0.0.1:6379&gt; hset user:1001 name AAA age 20
(integer) 1
127.0.0.1:6379&gt; hkeys user:1001
1) &quot;id&quot;
2) &quot;name&quot;
3) &quot;age&quot;
127.0.0.1:6379&gt; hkeys user:1002
1) &quot;id&quot;
127.0.0.1:6379&gt; hexists user:1001 id
(integer) 1
127.0.0.1:6379&gt; hvals user:1001
1) &quot;1&quot;
2) &quot;AAA&quot;
3) &quot;20&quot;
127.0.0.1:6379&gt; hincrby user:1001 age 2
(integer) 22
127.0.0.1:6379&gt; hsetnx user:1002 age 18
(integer) 1
127.0.0.1:6379&gt; hkeys user:1002
1) &quot;id&quot;
2) &quot;age&quot;
</code></pre>
<h4 id="数据结构-3"><a href="#数据结构-3" class="headerlink" title="数据结构"></a>数据结构</h4><p>Hash 类型对应的数据结构是两种：ziplist（压缩列表），hashtable（哈希表）。当 field-value 长度较短且个数较少时，使用 ziplist，否则使用 hashtable。</p>
<h3 id="3-6-有序集合-Zset-Sorted-Set"><a href="#3-6-有序集合-Zset-Sorted-Set" class="headerlink" title="3.6 有序集合 Zset (Sorted Set)"></a>3.6 有序集合 Zset (Sorted Set)</h3><ul>
<li>Redis 有序集合 zset 与普通集合 set 非常相似，是一个没有重复元素的字符串集合。</li>
<li>不同之处是有序集合的每个成员都关联了一个<strong>评分（</strong>score），这个评分（score）被用来按照从最低分到最高分的方式排序集合中的成员。集合的成员是唯一的，但是评分可以是重复了 。</li>
<li>因为元素是有序的，所以你也可以很快的根据评分（score）或者次序（position）来获取一个范围的元素。</li>
<li>访问有序集合的中间元素也是非常快的，因此你能够使用有序集合作为一个没有重复成员的智能列表。</li>
</ul>
<h4 id="常用命令-3"><a href="#常用命令-3" class="headerlink" title="常用命令"></a>常用命令</h4><pre><code>zadd key score1 value1 score2 value2 将一个或多个member元素及其score值加入到有序key中

zrange key start stop (withscores) 返回有序集key，下标在start与stop之间的元素，带withscores，可以让分数一起和值返回到结果集。

zrangebyscore key min max(withscores) 返回有序集key，所有score值介于min和max之间（包括等于min或max）的成员。有序集成员按score的值递增次序排列

zrevrangebyscore key max min （withscores）同上，改为从大到小排列

zincrby key increment value 为元素的score加上增量

zrem key value 删除该集合下，指定值的元素

zcount key min max 统计该集合，分数区间内的元素个数

zrank key value 返回该值在集合中的排名，从0开始
</code></pre>
<h4 id="数据结构-4"><a href="#数据结构-4" class="headerlink" title="数据结构"></a>数据结构</h4><p>zset 底层使用了两个数据结构：</p>
<ol>
<li>hash，hash 的作用就是关联元素 value 和权重 score，保障元素 value 的唯一性，可以通过元素 value 找到相应的 score 值。</li>
<li>跳跃表(skiplist)，跳跃表的目的在于给元素 value 排序，根据 score 的范围获取元素列表。</li>
</ol>
<h2 id="参考-Reference"><a href="#参考-Reference" class="headerlink" title="参考 Reference"></a>参考 Reference</h2><p>参考资料：《Redis开发与运维》</p>
<p>视频教程：<a href="https://www.bilibili.com/video/BV1Rv41177Af">https://www.bilibili.com/video/BV1Rv41177Af</a></p>
<p>参考笔记：<a href="https://zhangc233.github.io/2021/05/02/Redis/">https://zhangc233.github.io/2021/05/02/Redis/</a></p>
<p>参考笔记：<a href="https://blog.csdn.net/weixin_47872288/article/details/118410080">https://blog.csdn.net/weixin_47872288/article/details/118410080</a></p>
]]></content>
      <categories>
        <category>REDIS</category>
        <category>REDIS学习笔记</category>
      </categories>
      <tags>
        <tag>REDIS</tag>
        <tag>后端</tag>
      </tags>
  </entry>
  <entry>
    <title>03-配置文件、发布和订阅</title>
    <url>/2022/02/03/Redis03-%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6%E3%80%81%E5%8F%91%E5%B8%83%E5%92%8C%E8%AE%A2%E9%98%85/</url>
    <content><![CDATA[<h2 id="4-配置文件"><a href="#4-配置文件" class="headerlink" title="4.配置文件"></a>4.配置文件</h2><p>官网下载：<a href="http://download.redis.io/redis-stable/redis.conf">http://download.redis.io/redis-stable/redis.conf</a></p>
<h4 id="Units-单位"><a href="#Units-单位" class="headerlink" title="Units 单位"></a>Units 单位</h4><h5 id="配置大小单位，只支持bytes，不支持bit"><a href="#配置大小单位，只支持bytes，不支持bit" class="headerlink" title="配置大小单位，只支持bytes，不支持bit"></a>配置大小单位，只支持bytes，不支持bit</h5><p>大小写不敏感</p>
<h4 id="INCLUDES-包含"><a href="#INCLUDES-包含" class="headerlink" title="INCLUDES 包含"></a>INCLUDES 包含</h4><h5 id="提取公共配置文件"><a href="#提取公共配置文件" class="headerlink" title="提取公共配置文件"></a>提取公共配置文件</h5><p>####NETWORK 网络</p>
<h5 id="配置支持原创连接"><a href="#配置支持原创连接" class="headerlink" title="配置支持原创连接"></a>配置支持原创连接</h5><pre><code>#bind 127.0.0.1 -::1 //注释绑定本地连接
protected-mode no         //关闭保护模式
</code></pre>
<p>端口号：默认6379</p>
<pre><code>port 6379
</code></pre>
<h4 id="GENERAL-通用"><a href="#GENERAL-通用" class="headerlink" title="GENERAL 通用"></a>GENERAL 通用</h4><h5 id="开启后台启动"><a href="#开启后台启动" class="headerlink" title="开启后台启动"></a>开启后台启动</h5><pre><code>daemonize yes
</code></pre>
<h5 id="进程号的路径"><a href="#进程号的路径" class="headerlink" title="进程号的路径"></a>进程号的路径</h5><pre><code>pidfile /var/run/redis_6379.pid
</code></pre>
<h5 id="日志级别"><a href="#日志级别" class="headerlink" title="日志级别"></a>日志级别</h5><pre><code>loglevel notice
</code></pre>
<h5 id="日志输出路径"><a href="#日志输出路径" class="headerlink" title="日志输出路径"></a>日志输出路径</h5><pre><code>logfile &quot;&quot;
</code></pre>
<h5 id="数据库数数量"><a href="#数据库数数量" class="headerlink" title="数据库数数量"></a>数据库数数量</h5><pre><code>databases 16
</code></pre>
<h4 id="SECURITY-安全"><a href="#SECURITY-安全" class="headerlink" title="SECURITY 安全"></a>SECURITY 安全</h4><h5 id="开启访问密码"><a href="#开启访问密码" class="headerlink" title="开启访问密码"></a>开启访问密码</h5><pre><code>requirepass foobared
</code></pre>
<h4 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h4><h5 id="最大客户端连接数"><a href="#最大客户端连接数" class="headerlink" title="最大客户端连接数"></a>最大客户端连接数</h5><pre><code>maxclients 10000
</code></pre>
<h5 id="设置最大内存"><a href="#设置最大内存" class="headerlink" title="设置最大内存"></a>设置最大内存</h5><pre><code>maxmemory &lt;bytes&gt;
</code></pre>
<h5 id="内存超出上限：按规则移除"><a href="#内存超出上限：按规则移除" class="headerlink" title="内存超出上限：按规则移除"></a>内存超出上限：按规则移除</h5><pre><code>maxmemory-policy noeviction
</code></pre>
<p>注：如果redis无法按规则移除，或者设置了“不允许移除”，则：</p>
<ul>
<li><p>需要申请内存的指令(SET/LPUSH等)会返回错误信息</p>
</li>
<li><p>无内存申请的指令(GET等)仍会正常响应</p>
</li>
</ul>
<h5 id="样本数量"><a href="#样本数量" class="headerlink" title="样本数量"></a>样本数量</h5><pre><code>maxmemory-samples 5
</code></pre>
<p>注：数值越小越不准确，但性能消耗较小</p>
<h5 id="内存移除规则"><a href="#内存移除规则" class="headerlink" title="内存移除规则"></a>内存移除规则</h5><pre><code># volatile-lru -&gt; 使用LRU算法移除key，只对设置了过期时间的键(少用)
# allkeys-lru -&gt; 在所有集合key中，使用LRU算法移除key
# volatile-lfu -&gt; LFU算法
# allkeys-lfu -&gt; LFU算法
# volatile-random -&gt; 在过期集合中移除随机的key，只对设置了过期时间的键
# allkeys-random -&gt; 在所有集合中移除随机的key，只对设置了过期时间的键
# volatile-ttl -&gt; 移除TTL最小(快要过期)的key
# noeviction -&gt; 不进行移除
</code></pre>
<h5 id="Redis淘汰算法"><a href="#Redis淘汰算法" class="headerlink" title="Redis淘汰算法"></a>Redis淘汰算法</h5><ul>
<li>随机</li>
<li>TTL 过期时间</li>
<li>LRU（Least Recently Used，最近最少使用）</li>
<li>LFU（Least Frequently Used，最不经常使用）</li>
</ul>
<h2 id="5-发布和订阅"><a href="#5-发布和订阅" class="headerlink" title="5.发布和订阅"></a>5.发布和订阅</h2><blockquote>
<p>Redis发布订阅(pub/sub)是一种消息通信模式：发送者(pub)发送消息，订阅者(sub)接收消息</p>
</blockquote>
<blockquote>
<p>Redis客户端可以订阅任意数量的频道</p>
</blockquote>
<h5 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h5><p>客户端1：</p>
<pre><code>127.0.0.1:6379&gt; publish channel1 hello
(integer) 1
</code></pre>
<p>客户端2：</p>
<pre><code>127.0.0.1:6379&gt; subscribe channel1
Reading messages... (press Ctrl-C to quit)
1) &quot;subscribe&quot;
2) &quot;channel1&quot;
3) (integer) 1
1) &quot;message&quot;
2) &quot;channel1&quot;
3) &quot;hello&quot;
</code></pre>
<p>注：多个客户端订阅相同频道，都可以收到消息</p>
<h2 id="参考-Reference"><a href="#参考-Reference" class="headerlink" title="参考 Reference"></a>参考 Reference</h2><p>参考资料：《Redis开发与运维》</p>
<p>视频教程：<a href="https://www.bilibili.com/video/BV1Rv41177Af">https://www.bilibili.com/video/BV1Rv41177Af</a></p>
<p>参考笔记：<a href="https://zhangc233.github.io/2021/05/02/Redis/">https://zhangc233.github.io/2021/05/02/Redis/</a></p>
<p>参考笔记：<a href="https://blog.csdn.net/weixin_47872288/article/details/118410080">https://blog.csdn.net/weixin_47872288/article/details/118410080</a></p>
]]></content>
      <categories>
        <category>REDIS</category>
        <category>REDIS学习笔记</category>
      </categories>
      <tags>
        <tag>REDIS</tag>
        <tag>后端</tag>
      </tags>
  </entry>
  <entry>
    <title>04-新数据类型</title>
    <url>/2022/02/04/Redis04-%E6%96%B0%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B/</url>
    <content><![CDATA[<h2 id="6-新数据类型"><a href="#6-新数据类型" class="headerlink" title="6.新数据类型"></a>6.新数据类型</h2><h3 id="Bitmaps-位操作字符串"><a href="#Bitmaps-位操作字符串" class="headerlink" title="Bitmaps 位操作字符串"></a>Bitmaps 位操作字符串</h3><ul>
<li>Bitmaps实际上就是字符串(k-v)，但是可以对字符串进行位操作</li>
<li>可以把Bitmas想象成以位为单位，只能存0和1的数组，数组下标叫做偏移量</li>
</ul>
<h4 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h4><pre><code>setbit key offset value  //设置值

getbit key offset              //取值

bitcount （start end     //统计数值

bitop and/or/not/xor destkey key1 key2  //复合操作，交并非异或，结果保存在destkey
</code></pre>
<h4 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h4><pre><code>127.0.0.1:6379&gt; setbit k1 1 1
(integer) 0
127.0.0.1:6379&gt; setbit k1 2 1
(integer) 0
127.0.0.1:6379&gt; setbit k1 4 1
(integer) 0
127.0.0.1:6379&gt; getbit k1 1
(integer) 1
127.0.0.1:6379&gt; getbit k1 3
(integer) 0
127.0.0.1:6379&gt; bitcount k1
(integer) 3
127.0.0.1:6379&gt; setbit k2 3 1
(integer) 0
127.0.0.1:6379&gt; bitop and k1_and_k2 k1 k2 //k1和k2都有值 
(integer) 1
127.0.0.1:6379&gt; bitcount k1_and_k2
(integer) 0
127.0.0.1:6379&gt; bitop or k1_or_k2 k1 k2 //k1或k2有值 
(integer) 1
127.0.0.1:6379&gt; bitcount k1_or_k2
(integer) 4
</code></pre>
<h4 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h4><p>但 Bitmaps 并不是万金油， 假如该网站每天的独立访问用户很少， 例如只有 10 万（大量的僵尸用户） ， 那么两者的对比如下表所示， 很显然， 这时候使用 Bitmaps 就不太合适了， 因为基本上大部分位都是 0。</p>
<p>Bitmaps适合百万级以上数据量存储。</p>
<h3 id="HyperLogLog"><a href="#HyperLogLog" class="headerlink" title="HyperLogLog"></a>HyperLogLog</h3><h4 id="基数问题"><a href="#基数问题" class="headerlink" title="基数问题"></a>基数问题</h4><blockquote>
<p>求集合中不重复元素个数的问题称为基数问题</p>
</blockquote>
<p>解决方案：</p>
<ul>
<li>Mysql使用distinct count计算</li>
<li>Redis使用hash，set，bitmaos等数据结构处理</li>
</ul>
<h4 id="基数统计"><a href="#基数统计" class="headerlink" title="基数统计"></a>基数统计</h4><p>基数统计就是在误差可接受的范围内，快速计算基数。</p>
<ul>
<li>Redis HyperLogLog是用来做基数统计的算法，优点是输入大量数据时，所需空间固定(很小)。</li>
<li>在 Redis 里面，每个 HyperLogLog 键只需要花费 12 KB 内存，就可以计算接近 2^64 个不同元素的基数。</li>
<li>HyperLogLog 只会根据输入元素来计算基数，而不会储存输入元素本身，所以 HyperLogLog 不能像集合那样，返回输入的各个元素。</li>
</ul>
<h4 id="常用操作"><a href="#常用操作" class="headerlink" title="常用操作"></a>常用操作</h4><pre><code>pfadd key element //添加元素 基数变化返回1，不变返回0

pfcount key                //统计基数

pfmerge destkey key1 key2  //合并
</code></pre>
<h3 id="Geospatial-经纬度"><a href="#Geospatial-经纬度" class="headerlink" title="Geospatial 经纬度"></a>Geospatial 经纬度</h3><p>Redis3.2新增，提供经纬度设置，查询范围，距离查询等操作</p>
<blockquote>
<p>求集合中不重复元素个数的问题称为基数问题</p>
</blockquote>
<p>解决方案：</p>
<ul>
<li>Mysql使用distinct count计算</li>
<li>Redis使用hash，set，bitmaos等数据结构处理</li>
</ul>
<h4 id="常用操作-1"><a href="#常用操作-1" class="headerlink" title="常用操作"></a>常用操作</h4><pre><code>geoadd key longitude latitude member //设置经纬度

geopos key member                        //获取经纬度

geodist key member1 member2 (m km ft mi) //计算两个位置之间的直线距离

georadius key longitude latitude radius (m km ft mi) //以给定的经纬度为中心，找出某一半径的内元素
</code></pre>
<h2 id="参考-Reference"><a href="#参考-Reference" class="headerlink" title="参考 Reference"></a>参考 Reference</h2><p>参考资料：《Redis开发与运维》</p>
<p>视频教程：<a href="https://www.bilibili.com/video/BV1Rv41177Af">https://www.bilibili.com/video/BV1Rv41177Af</a></p>
<p>参考笔记：<a href="https://zhangc233.github.io/2021/05/02/Redis/">https://zhangc233.github.io/2021/05/02/Redis/</a></p>
<p>参考笔记：<a href="https://blog.csdn.net/weixin_47872288/article/details/118410080">https://blog.csdn.net/weixin_47872288/article/details/118410080</a></p>
]]></content>
      <categories>
        <category>REDIS</category>
        <category>REDIS学习笔记</category>
      </categories>
      <tags>
        <tag>REDIS</tag>
        <tag>后端</tag>
      </tags>
  </entry>
  <entry>
    <title>01-基础架构</title>
    <url>/2021/06/01/Mysql45%E8%AE%B201-%E5%9F%BA%E7%A1%80%E6%9E%B6%E6%9E%84/</url>
    <content><![CDATA[<h2 id="01基础架构"><a href="#01基础架构" class="headerlink" title="01基础架构"></a>01基础架构</h2><h3 id="逻辑架构"><a href="#逻辑架构" class="headerlink" title="逻辑架构"></a>逻辑架构</h3><h4 id="Server层"><a href="#Server层" class="headerlink" title="Server层"></a>Server层</h4><ul>
<li>包括连接器，查询缓存，分析器，优化器，执行器等</li>
<li>涵盖Mysql大多数核心服务功能，和所有内置函数（日期，时间，数学，加密等）</li>
<li>实现所有跨存储引擎的功能（存储过程，触发器，视图等）</li>
</ul>
<h4 id="存储引擎层"><a href="#存储引擎层" class="headerlink" title="存储引擎层"></a>存储引擎层</h4><ul>
<li>负责数据的存储和提取</li>
<li>架构模式是插件式的，从Mysql5.5.5开始InnoDB成为默认存储引擎</li>
</ul>
<h3 id="1-连接器"><a href="#1-连接器" class="headerlink" title="1.连接器"></a>1.连接器</h3><h5 id="连接Mysql"><a href="#连接Mysql" class="headerlink" title="连接Mysql"></a>连接Mysql</h5><pre><code>mysql -h$ip -P$port -u$user -p
</code></pre>
<p>一个用户成功建立连接后，即使你用管理员账号对这个用户的权限做了修改，也不会影响已经存在连接的权限。修改完成后，只有再新建的连接才会使用新的权限设置。</p>
<h5 id="空闲连接"><a href="#空闲连接" class="headerlink" title="空闲连接"></a>空闲连接</h5><blockquote>
<p>连接完成后，如果你没有后续的动作，这个连接就处于空闲状态</p>
</blockquote>
<pre><code>show processlist
</code></pre>
<ul>
<li>Command 列显示为“Sleep”的这一行，就表示现在系统里面有一个空闲连接。</li>
<li>客户端如果太长时间没动静，连接器就会自动将它断开。这个时间是由参数 wait_timeout 控制的，默认值是 8 小时。</li>
</ul>
<h5 id="短连接"><a href="#短连接" class="headerlink" title="短连接"></a>短连接</h5><blockquote>
<p>短连接则是指每次执行完很少的几次查询就断开连接，下次查询再重新建立一个。</p>
</blockquote>
<h5 id="长连接-推荐"><a href="#长连接-推荐" class="headerlink" title="长连接(推荐)"></a>长连接(推荐)</h5><blockquote>
<p>长连接是指连接成功后，如果客户端持续有请求，则一直使用同一个连接。</p>
</blockquote>
<p>MySQL 在执行过程中临时使用的内存是管理在连接对象里面的。这些资源会在连接断开的时候才释放。如果长连接累积下来，可能导致内存占用太大，被系统强行杀掉（OOM），从现象看就是 MySQL 异常重启了。</p>
<p>解决办法：</p>
<ol>
<li>定期断开长连接。使用一段时间，或者程序里面判断执行过一个占用内存的大查询后，断开连接，之后要查询再重连。</li>
<li>MySQL 5.7 或更新版本，可以在每次执行一个比较大的操作后，通过执行 mysql_reset_connection 来重新初始化连接资源。这个过程不需要重连和重新做权限验证，但是会将连接恢复到刚刚创建完时的状态。</li>
</ol>
<h3 id="2-查询缓存（Mysql8-0已删除）"><a href="#2-查询缓存（Mysql8-0已删除）" class="headerlink" title="2.查询缓存（Mysql8.0已删除）"></a>2.查询缓存（Mysql8.0已删除）</h3><ul>
<li>MySQL拿到一个查询请求后，会先到查询缓存看看，之前是不是执行过这条语句。</li>
<li>MySQL之前执行过的语句及其结果可能会以 key-value 对的形式，被直接缓存在内存中。</li>
<li>查询缓存的失效非常频繁，只要有对一个表的更新，这个表上所有的查询缓存都会被清空。</li>
<li>可以将参数 query_cache_type 设置成 DEMAND，这样对于默认的 SQL 语句都不使用查询缓存。</li>
</ul>
<p>强制查询缓存语句</p>
<pre><code>select SQL_CACHE * from T where ID=10；
</code></pre>
<h3 id="3-分析器"><a href="#3-分析器" class="headerlink" title="3.分析器"></a>3.分析器</h3><blockquote>
<p>分析器先会做“词法分析”。你输入的是由多个字符串和空格组成的一条 SQL 语句，MySQL 需要识别出里面的字符串分别是什么，代表什么。</p>
</blockquote>
<p>如果没有命中查询缓存，就要开始真正执行语句了。</p>
<p>语法错误提醒：ERROR 1064 (42000): You have an error in your SQL syntax;</p>
<h3 id="4-优化器"><a href="#4-优化器" class="headerlink" title="4.优化器"></a>4.优化器</h3><blockquote>
<p>优化器是在表里面有多个索引的时候，决定使用哪个索引；或者在一个语句有多表关联（join）的时候，决定各个表的连接顺序。</p>
</blockquote>
<h3 id="5-执行器"><a href="#5-执行器" class="headerlink" title="5.执行器"></a>5.执行器</h3><ul>
<li>开始执行的时候，要先判断一下有没有执行权限，如果没有，就会返回没有权限的错误</li>
<li>如果有权限，就继续执行，执行器会根据表的引擎定义，去使用这个引擎提供的接口。</li>
</ul>
<p>示例：</p>
<pre><code>mysql&gt; select * from T where ID=10;
</code></pre>
<p>执行流程：</p>
<ol>
<li>调用 InnoDB 引擎接口取这个表的第一行，判断 ID 值是不是 10，如果不是则跳过，如果是则将这行存在结果集中；</li>
<li>调用引擎接口取“下一行”，重复相同的判断逻辑，直到取到这个表的最后一行。</li>
<li>执行器将上述遍历过程中所有满足条件的行组成的记录集作为结果集返回给客户端。</li>
</ol>
<p>数据库的慢查询日志中有一个<code>rows_examined</code>字段，表示这个语句执行过程中扫描了多少行。这个值就是在执行器每次调用引擎获取数据行的时候累加的。</p>
<h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>问题：如果表 T 中没有字段 k，而你执行了这个语句 select * from T where k=1, 那肯定是会报“不存在这个列”的错误： “Unknown column ‘k’ in ‘where clause’”。你觉得这个错误是在我们上面提到的哪个阶段报出来的呢？<br>答案：分析器。Oracle会在分析阶段判断语句是否正确，表是否存在，列是否存在等。<br>其他答案：预处理器。<br>解析器处理语法和解析查询, 生成一课对应的解析树。<br>预处理器进一步检查解析树的合法。比如: 数据表和数据列是否存在, 别名是否有歧义等。如果通过则生成新的解析树，再提交给优化器。</p>
]]></content>
      <categories>
        <category>MYSQL</category>
        <category>MYSQL45讲</category>
      </categories>
      <tags>
        <tag>后端</tag>
        <tag>MYSQL</tag>
      </tags>
  </entry>
  <entry>
    <title>单词本</title>
    <url>/2022/01/01/%E5%8D%95%E8%AF%8D%E6%9C%AC/</url>
    <content><![CDATA[<table>
<thead>
<tr>
<th>单词</th>
<th>翻译</th>
<th>原文</th>
<th>翻译</th>
</tr>
</thead>
<tbody><tr>
<td>reliable</td>
<td>可信赖的</td>
<td>Build fast, reliable, and efficient software at scale 大规模构建快速、可靠、高效的软件</td>
<td><a href="https://go.dev/">go.dev</a></td>
</tr>
<tr>
<td>scale</td>
<td>规模</td>
<td></td>
<td></td>
</tr>
<tr>
<td>concurrency</td>
<td>并发性</td>
<td>Built-in concurrency and a robust standard library 内置并发性和强大的标准库</td>
<td>内置并发性和强大的标准库</td>
</tr>
<tr>
<td>robust</td>
<td>强大的</td>
<td></td>
<td></td>
</tr>
<tr>
<td>ecosystem</td>
<td>生态环境</td>
<td>Growing ecosystem of partners, communities, and tools 不断增长的合作伙伴、社区和工具生态系统</td>
<td>不断增长的合作伙伴、社区和工具生态系统</td>
</tr>
<tr>
<td>communities</td>
<td>社区</td>
<td>community 社区</td>
<td>社区</td>
</tr>
<tr>
<td>organization</td>
<td>组织机构</td>
<td></td>
<td></td>
</tr>
<tr>
<td>flexibility</td>
<td>灵活性</td>
<td></td>
<td></td>
</tr>
<tr>
<td>concept</td>
<td>概念</td>
<td></td>
<td></td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>单词本</category>
      </categories>
      <tags>
        <tag>单词本</tag>
      </tags>
  </entry>
</search>
